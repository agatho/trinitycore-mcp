# Test Plan Execution — 2026-08-30

First full run of both plans against active build `12.1.0.69497`.

**Headline:** the data is correct. Every value checked against an oracle matched. What failed was the layer around the data — serialisation, input validation, stream hygiene, and two UI bindings. Four defects were severe enough to fix during the run; the largest class of finding was left open because it needs a decision rather than a patch.

---

## Summary

| Section | Result |
|---------|--------|
| MCP preconditions P1–P7 | 7/7 PASS |
| MCP A1 protocol (155 tools) | 466/466 PASS |
| MCP A2 input validation | 68 PASS, **27 FAIL**, 38 N/A, 22 not exercised |
| MCP A4 stdout hygiene | **FAIL → fixed → PASS** |
| MCP B data correctness | 36/36 PASS |
| MCP B1.7 / B2.7 deep comparisons | baselines held exactly |
| MCP C build awareness | 6/6 PASS |
| MCP D performance | 6/7 PASS, 1 FAIL (cold start) |
| Web preconditions P1–P6 | 6/6 PASS |
| Web B API routes | **2 FAIL → fixed → PASS** |
| Web C3 settings build panel | PASS |
| Web C1 item page | **2 display FAILs → fixed → PASS** |
| Jest / Vitest | 1,695 / 320 passing |

---

## Fixed during the run

### 1. CASC diagnostics on the JSON-RPC stream (A4) — severe

One `get-minimap-tile` call put **28,580 non-protocol lines (1.9 MB)** onto stdout, which is the MCP message channel. Any client calling a minimap tool would have its protocol stream corrupted.

158 `console.log`/`console.error` calls in `src/casc/**` wrote to stdout. All now write to stderr. Re-tested against the real transport with the same minimap call: 10 JSON-RPC responses, **zero** stray lines, 1.8 MB of diagnostics on stderr.

This is the third instance of the same defect — winston's Console transport and the profile-loader banner were the first two — and by far the largest.

### 2. `get-item-info` could not be serialised — severe

Every response containing a real item failed with *"Do not know how to serialize a BigInt"*, over both the MCP transport and the web API. Only the not-found path worked, because it has no item to serialise.

`ItemSparseEntry.allowableRace` was a `BigInt`. JSON cannot carry one at all, and cannot carry 64 bits as a number without losing precision either, so it is now a decimal string.

**Why the earlier checks missed it:** they read properties off the returned object rather than serialising it, and the stdio harness counted JSON-RPC responses without inspecting whether they were errors. Counting responses is not checking them — the harness now needs to assert on content.

### 3. Item detail page showed wrong values (C1.2)

Two bindings read fields that do not exist on the response:

- `item.itemSubclass` — the API returns `itemSubClass`, so every item's subclass rendered as "N/A".
- `item.sellPrice` / `item.buyPrice` / `item.vendorPrice` — prices live on `db2Data.itemSparse`, so every item rendered as costing nothing ("Vendor Price 0 copper").

Item 25 now shows subclass 7, sell 3 copper, buy 18 copper, matching the DB2 file.

### 4. A wrong expectation in the plan itself

`/api/maps/list` returns 11 continent-level maps for the minimap viewer, not the 1,079 maps `mapextractor` produced. The route is right; the plan was wrong and has been corrected.

---

## Open — needs a decision, not a patch

### 27 of 68 exercised tools accept empty arguments (A2.1)

Tools that declare required parameters but do not enforce them. They fall into three tiers:

**Fabricates data** — the dangerous tier:
- `get-reputation-standing` returns `"currentStanding": "exalted"` for no input at all.

**Causes side effects from empty input:**
- `replay-start-recording` starts a recording session.
- `coordinate-cooldowns` creates a group (`group_1788093875516`).

**Returns `undefined`/`null`-laden output instead of rejecting** (24 tools), including `get-trinity-api` (`class "undefined" not found`), `calculate-melee-damage`, `calculate-armor-mitigation`, `get-talent-build`, `get-leveling-path`, `vmap-test-line-of-sight`, `get-knowledge-node`.

Full list in `test-results-mcp-sectionA.json`.

This is one fix applied 27 times — validate declared-required parameters and return a typed error — but it changes the contract of 27 tools, so it is a decision rather than a mechanical patch.

### Minimap tools start unbounded work on empty input

`get-map-minimap`, `get-minimap-tile` and `get-minimap-tiles-batch` respond to empty arguments by beginning a full CASC encoding-file extraction (~199 MB decompressed). During the first A2 run this exhausted the V8 heap and killed the process after 72 tools. They are recorded as A2 failures and excluded from harness invocation.

### D2 — cold lookup 345 ms against a <100 ms target

The first spell or item lookup in a process loads ~39 MB of JSON caches lazily. Steady state is fine: p95 over 1,000 lookups is **0.01 ms**, with no drift (first-250 avg 0.029 ms, last-250 avg 0.006 ms) and 22.7 MB heap growth over 10,000 lookups.

Either warm the caches at startup, or split the target so cold start is measured separately from p95. The current target conflates them.

---

## Data correctness — all PASS

Every check matched its oracle.

| Check | Result |
|-------|--------|
| Spells 133 / 2061 / 116 / 8326 | Fireball, Flash Heal, Frostbolt, Ghost |
| Spell cache | 194,187 entries, build 69497 |
| Items 25 / 19019 / 6948 / 128476 | Worn Shortsword, Thunderfury (LEGENDARY), Hearthstone, Fangs of the Devourer (ARTIFACT) |
| Item.db2 / ItemSparse | 59,675 records; 175,217 header, 175,059 catalog ids |
| SpellEffect | 629,375 records |
| Table hashes | Item `0x50238EC2`, ItemSparse `0x919BE54E`, SpellEffect `0xF04238A5` |
| Layout hashes | Item `0x996192AA`, ItemSparse `0x1C17D17F`, SpellEffect `0x5362E3D4` |
| Sparse walk | 5,000 records, 0 failures |
| Wrong-build data | refused: *"walked 329 bytes of 328"* |
| Gametables | 20 tables; xp.txt 123 rows, headers correct |
| Opcodes | 2,384; CMSG_MOVE_JUMP 4259846; SMSG_SPELL_START 6750253 |
| VMaps / MMaps | 39,903 entries; 763 `.mmap` + 27,856 `.mmtile` |

**Deep comparisons against the hotfixes database:**

- **B1.7 SpellEffect** — 394/395 on all eight columns. Exactly the documented baseline; the single `EffectAura` divergence is a hotfix override.
- **B2.7 ItemSparse** — 257/288 across 19 columns. The per-column pattern matches the documented build drift precisely: Display 287/288, Material 287/288, SellPrice and BuyPrice 259/288. The oracle is `VerifiedBuild 67186`; the data is 69497.

---

## Build awareness — all PASS

| Check | Result |
|-------|--------|
| Active build | 12.1.0.69497, Midnight, WDC5 |
| Data paths | all six present |
| Clean environment | 0 disagreements |
| Stale `DB2_PATH` / `VMAP_PATH` | both detected; manifest still wins |
| Explicit build id | validates the archived 11.2.7.65299 |
| Settings page panel | shows the active build, six ✓ paths, the archived build, and that the fields below apply only without a manifest |

---

## Not exercised, and why

- **22 side-effecting tools** (backup, restore, import/export, config write, load tests, marketplace, extraction). A tool that fails to validate its input is exactly the one that might act on garbage sent to it, so the harness never invokes them. They need fixture-based tests against a scratch database.
- **`/api/config/reset`** — writes configuration; would have overwritten the working `.env`.
- **38 tools with no required parameters** — A2.1 does not apply; they need their own case-by-case cases.
- **Page-level sweeps across all 41 pages** — dark mode, 375 px, keyboard, screen-reader labels. Two pages were exercised in a browser (settings, item detail); the rest are unrun.
- **Sections A3 (failure injection) and E10** — E10 (generator orphaning) was verified earlier in the day by killing a server mid-generation: no surviving generator, no stale lock.

---

## Artefacts

- `scripts/test-harness-mcp.cjs` — Sections A1/A2 across all registered tools
- `scripts/test-harness-mcp-data.cjs` — Sections B/C against the oracles
- `scripts/test-harness-mcp-stdio.cjs` — Section A4 over the real stdio transport
- `test-results-mcp-sectionA.json`, `test-results-mcp-sectionBC.json` — per-check results

Commits: `888ce440` (CASC stdout + harnesses), `a368e57d` (BigInt + item page bindings).
