# TrinityCore SOAP API Configuration
# Required for Live Data Inspector to connect to worldserver
TRINITY_SOAP_HOST=127.0.0.1
TRINITY_SOAP_PORT=7878
TRINITY_SOAP_USERNAME=admin
TRINITY_SOAP_PASSWORD=admin
TRINITY_SOAP_MOCK=true  # Set to false when worldserver SOAP is running

# TrinityCore Database
TRINITY_DB_HOST=localhost
TRINITY_DB_PORT=3306
TRINITY_DB_AUTH_DATABASE=acore_auth
TRINITY_DB_CHARACTERS_DATABASE=acore_characters
TRINITY_DB_WORLD_DATABASE=acore_world
TRINITY_DB_USERNAME=acore
TRINITY_DB_PASSWORD=acore

# MCP Server Path
MCP_SERVER_PATH=C:\\TrinityBots\\trinitycore-mcp\\dist\\index.js

# TrinityCore Paths
TRINITY_ROOT=C:\\TrinityBots\\TrinityCore
DBC_PATH=C:\\TrinityBots\\Server\\data\\dbc
DB2_PATH=C:\\TrinityBots\\Server\\data\\db2

# Collision Data Auto-Load (Optional)
# =============================================
# Enable automatic collision data loading from filesystem for height detection
# in the World Editor and Map Picker Enhanced.
#
# How It Works:
# - If these paths are configured, the UI will automatically attempt to load
#   files from the specified directories when you select a map.
# - If not configured, the UI will gracefully fall back to manual file upload.
#
# Extract with TrinityCore Tools:
# - mapextractor → maps/    (.map terrain height files)
# - vmap4assembler → vmaps/ (.vmtree + .vmtile collision files)
# - mmaps_generator → mmaps/ (.mmap + .mmtile navigation files)
#
# File Naming (TrinityCore Standard):
# Map Files (.map):
# - Tilelist: <mapId>.tilelist (e.g., 0001.tilelist)
# - Format: <mapId>_<x>_<y>.map (e.g., 0001_55_42.map = map 1, grid 55,42)
#
# VMap Files (.vmtree, .vmtile, .vmtileidx):
# - Subfolder: <mapId>/ (e.g., 0000/, 0001/)
# - Tree: <mapId>.vmtree (e.g., 0000.vmtree inside 0000/ folder)
# - Tiles: <mapId>_<x>_<y>.vmtile (e.g., 0000_17_42.vmtile)
# - Index: <mapId>_<x>_<y>.vmtileidx (e.g., 0000_17_42.vmtileidx)
#
# MMap Files (.mmap, .mmtile):
# - Header: <mapId>.mmap (e.g., 0000.mmap, 0001.mmap)
# - Tiles: <mapId><x><y>.mmtile (e.g., 00003248.mmtile = map 0, tile 32,48)
#
# If you don't have collision data:
# - Comment out or remove these lines
# - Use manual upload via the UI buttons
#
MAP_DATA_PATH=C:\\TrinityBots\\Server\\data\\maps
VMAP_DATA_PATH=C:\\TrinityBots\\Server\\data\\vmaps
MMAP_DATA_PATH=C:\\TrinityCore\\Server\\data\\mmaps

# Next.js
NEXT_PUBLIC_APP_NAME=TrinityCore API Explorer
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_VERSION=2.6.0
