api:
  class: InterruptCoordinatorFixed
  method: EnableRotation
  signature: void InterruptCoordinatorFixed::EnableRotation(bool enable)
documentation:
  brief: Enables or disables the rotation functionality of the InterruptCoordinatorFixed
    class.
  description: The EnableRotation method controls whether the rotation behavior is
    active within the InterruptCoordinatorFixed class. This method is typically used
    in AI systems to toggle rotational movement or targeting behaviors during combat
    or other dynamic scenarios. When enabled, the system may begin processing rotation
    logic; when disabled, it halts such operations. The method does not return any
    value and operates on a boolean flag that determines the active state of the rotation
    mechanism.
  parameters:
  - name: enable
    description: A boolean flag indicating whether to enable (true) or disable (false)
      the rotation functionality.
  returns: null
  examples:
  - title: Basic Usage Example
    code: 'InterruptCoordinatorFixed coordinator;

      coordinator.EnableRotation(true); // Enable rotation

      coordinator.EnableRotation(false); // Disable rotation'
    language: cpp
  - title: Conditional Rotation Toggle
    code: "InterruptCoordinatorFixed* coordinator = GetInterruptCoordinator();\nif\
      \ (player->IsInCombat()) {\n    coordinator->EnableRotation(true);\n} else {\n\
      \    coordinator->EnableRotation(false);\n}"
    language: cpp
  notes: This method likely modifies internal state flags within the InterruptCoordinatorFixed
    class. It should be called from a thread-safe context if used in multi-threaded
    environments. The actual behavior of rotation may depend on other system components
    or AI states.
  warnings: Ensure that enabling rotation does not cause unintended side effects such
    as infinite loops or performance degradation. Avoid calling this method excessively
    in tight loops without proper state checks.
  related:
  - IsRotationEnabled
  - ToggleRotation
metadata:
  confidence: 0.85
  generated_at: '2025-11-09T00:28:33.851104'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
