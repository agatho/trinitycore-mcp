api:
  class: ShamanAI
  method: IsShockSpell
  signature: bool ShamanAI::IsShockSpell(int spellId) const
documentation:
  brief: Determines whether a given spell ID corresponds to a shock spell used by
    the Shaman class.
  description: The IsShockSpell method evaluates if a specified spell ID belongs to
    the set of shock spells available to Shamans in World of Warcraft. This utility
    function is typically used within the ShamanAI class to make decisions about spell
    selection and targeting during combat. Shock spells are a core component of the
    Shaman's offensive capabilities, including Lightning Bolt, Chain Lightning, and
    Earth Shock. The method checks against an internal list or lookup table of known
    shock spell IDs to determine membership. It is designed to be efficient and lightweight
    for use in AI decision-making loops where rapid classification of spells is required.
  parameters: []
  returns: Returns true if the provided spell ID corresponds to a recognized shock
    spell used by Shamans; otherwise, returns false.
  examples:
  - title: Check if a Spell is a Shock Spell
    code: "if (shamanAI->IsShockSpell(8042)) {\n    // Handle Earth Shock logic\n\
      \    std::cout << \"Earth Shock detected!\" << std::endl;\n}"
    language: cpp
  - title: Use in Spell Selection Logic
    code: "void ShamanAI::SelectSpell() {\n    for (int spellId : possibleSpells)\
      \ {\n        if (IsShockSpell(spellId)) {\n            // Prioritize shock spells\n\
      \            CastSpell(spellId);\n            return;\n        }\n    }\n}"
    language: cpp
  notes: This method likely uses a predefined set of spell IDs for shock spells rather
    than dynamic lookup. The implementation may be optimized for performance by using
    a hash table or bitset for fast lookups. Developers should ensure that all new
    shock spells added to the game are properly registered in this internal mapping.
  warnings: Ensure that the spell ID passed to this method is valid and corresponds
    to an actual spell in the game database; invalid IDs may lead to incorrect behavior
    or crashes. This method does not validate spell availability or castability, only
    classification.
  related:
  - ShamanAI::IsHealSpell
  - ShamanAI::IsBuffSpell
  - ShamanAI::GetSpellCooldown
metadata:
  confidence: 0.92
  generated_at: '2025-11-07T10:03:52.678963'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
