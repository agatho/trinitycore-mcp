api:
  class: EvokerAI
  method: HasEnoughResource
  signature: bool EvokerAI::HasEnoughResource(int spellId)
documentation:
  brief: Checks if the evoker has sufficient resource cost to cast a specified spell.
  description: The HasEnoughResource method evaluates whether the current evoker AI
    instance possesses adequate magical resources to cast a given spell. This method
    is typically used during spell selection and execution logic to ensure that the
    evoker does not attempt to cast spells when it lacks the necessary resource requirements,
    such as soul shards or other evoker-specific mana types. The check considers the
    spell's resource cost against the evoker's current resource pool.
  parameters:
  - name: spellId
    description: The identifier of the spell to check resource requirements for. This
      must correspond to a valid spell entry in the game's database.
  returns: Returns true if the evoker has sufficient resources to cast the specified
    spell, false otherwise.
  examples:
  - title: Basic Resource Check
    code: "if (evokerAI->HasEnoughResource(12345)) {\n    // Proceed with casting\
      \ the spell\n    evokerAI->CastSpell(12345);\n} else {\n    // Find another\
      \ action or wait\n    evokerAI->DoNextAction();\n}"
    language: cpp
  - title: Resource Check in Combat Decision Logic
    code: "if (evokerAI->HasEnoughResource(12345) && evokerAI->GetVictim() && evokerAI->GetVictim()->IsInFront(evokerAI->GetUnit()))\
      \ {\n    evokerAI->CastSpell(12345);\n} else if (evokerAI->HasEnoughResource(12346))\
      \ {\n    evokerAI->CastSpell(12346);\n}"
    language: cpp
  notes: This method is integral to the evoker's AI decision-making process and should
    be called before attempting to cast any spell. It relies on internal resource
    tracking mechanisms within the EvokerAI class, which may vary based on the specific
    subclass or implementation of the AI.
  warnings: Ensure that the provided spellId corresponds to a valid spell in the game
    database; otherwise, behavior is undefined and could lead to incorrect resource
    calculations or crashes.
  related:
  - CastSpell
  - GetResource
  - SetResource
metadata:
  confidence: 0.9
  generated_at: '2025-11-07T03:34:08.995201'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
