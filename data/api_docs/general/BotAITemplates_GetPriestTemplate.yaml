api:
  class: BotAITemplates
  method: GetPriestTemplate
  signature: BotAIConfig BotAITemplates::GetPriestTemplate(bool healer)
documentation:
  brief: Returns the configured AI template for a priest bot, either as a healer or
    damage dealer.
  description: The GetPriestTemplate method retrieves a pre-defined BotAIConfig structure
    that contains all necessary settings and behaviors for controlling a priest bot
    in World of Warcraft's TrinityCore environment. This includes spell priorities,
    combat tactics, movement patterns, and role-specific configurations. The returned
    configuration can be used to initialize or modify the AI behavior of a priest-controlled
    bot. The method distinguishes between healer and damage dealer templates, allowing
    for specialized behavior based on the desired role. This template system ensures
    consistent and balanced AI performance across different priest specializations
    within the bot framework.
  parameters: []
  returns: A BotAIConfig object containing all configuration parameters for a priest
    bot's AI behavior. The returned structure includes spell selection criteria, combat
    strategies, movement behaviors, and role-specific settings that define how the
    bot will act in various game scenarios.
  examples:
  - title: Get healer template for priest bot
    code: 'BotAIConfig healerTemplate = BotAITemplates::GetPriestTemplate(true);

      // Use healerTemplate to configure a priest bot for healing role'
    language: cpp
  - title: Get damage dealer template for priest bot
    code: 'BotAIConfig dpsTemplate = BotAITemplates::GetPriestTemplate(false);

      // Configure a priest bot for DPS role using the returned template'
    language: cpp
  notes: This method is part of TrinityCore's bot AI system and assumes that the BotAITemplates
    class has been properly initialized with predefined configurations. The returned
    BotAIConfig objects should not be modified directly in most cases, as they are
    designed to be immutable templates for consistent bot behavior.
  warnings: The method returns a copy of the configuration template, so modifications
    to the returned BotAIConfig object will not affect other bots using the same template.
    Ensure that you're using the correct boolean parameter value (true for healer,
    false for DPS) to get the appropriate template for your intended bot role.
  related:
  - GetWarriorTemplate
  - GetMageTemplate
  - GetPaladinTemplate
metadata:
  confidence: 0.9
  generated_at: '2025-11-04T14:38:55.600924'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
