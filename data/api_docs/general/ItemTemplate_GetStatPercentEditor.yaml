api:
  class: ItemTemplate
  method: GetStatPercentEditor
  signature: int32 ItemTemplate::GetStatPercentEditor(uint32 index) const
documentation:
  brief: Retrieves the stat percent editor value for a specified index from the item
    template.
  description: The GetStatPercentEditor method retrieves a specific stat percent editor
    value associated with an item template. This value is typically used in World
    of Warcraft's item system to define how certain stats are modified or scaled during
    item generation or processing. The method accepts an index parameter that specifies
    which stat percent editor value to retrieve, and returns the corresponding integer
    value. This functionality is commonly used when working with item properties,
    enchantments, or stat modifications within the TrinityCore framework.
  parameters: []
  returns: Returns an int32 value representing the stat percent editor value for the
    specified index. If the index is out of bounds or no value exists, the method
    may return 0 or a default value depending on implementation details.
  examples:
  - title: Retrieve Stat Percent Editor Value
    code: 'ItemTemplate* itemTemplate = sObjectMgr->GetItemTemplate(12345);

      int32 statPercent = itemTemplate->GetStatPercentEditor(0);

      // Returns the first stat percent editor value for item with ID 12345'
    language: cpp
  notes: This method is part of the ItemTemplate class which represents item definitions
    in the game database. The stat percent editor values are typically used internally
    by TrinityCore to manage how item stats are calculated and applied during gameplay.
    Ensure that the index parameter is within valid bounds to avoid unexpected behavior.
  warnings: null
  related:
  - GetStatType
  - GetStatValue
  - SetStatPercentEditor
metadata:
  confidence: 0.85
  generated_at: '2025-11-04T02:15:34.179217'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
