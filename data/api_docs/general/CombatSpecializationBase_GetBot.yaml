api:
  class: CombatSpecializationBase
  method: GetBot
  signature: Player * CombatSpecializationBase::GetBot() const
documentation:
  brief: Returns the Player object associated with the combat specialization bot.
  description: The GetBot method retrieves the Player instance that represents the
    bot controlled by this combat specialization. This method is typically used in
    AI and combat system logic to access the player character that is being managed
    or simulated by the specialization. The returned Player pointer can be used to
    query various properties, states, or perform actions on the bot character within
    the game world. It's important to note that this method may return a null pointer
    if no valid bot has been assigned to the combat specialization instance.
  parameters: []
  returns: A pointer to the Player object representing the bot, or nullptr if no bot
    is assigned to this combat specialization.
  examples:
  - title: Basic Usage
    code: "CombatSpecializationBase* spec = ...;\nPlayer* bot = spec->GetBot();\n\
      if (bot != nullptr)\n{\n    // Use the bot player object\n    uint32 botLevel\
      \ = bot->GetLevel();\n}"
    language: cpp
  - title: Safe Access with Null Check
    code: "CombatSpecializationBase* spec = ...;\nPlayer* bot = spec->GetBot();\n\
      if (bot && bot->IsAlive())\n{\n    // Perform actions only if bot exists and\
      \ is alive\n    bot->CastSpell(bot, 12345, true);\n}"
    language: cpp
  notes: This method is commonly used in TrinityCore's AI system where combat specializations
    manage bot behavior. The returned Player object should not be deleted or modified
    directly by external code as it's managed by the core game systems. Ensure proper
    null checking before using the returned pointer to avoid access violations.
  warnings: Never assume that the returned Player pointer is valid without checking
    for nullptr. Accessing methods on a null return value will result in undefined
    behavior and likely crash the server. The bot instance may be destroyed or unassigned
    during runtime, so always validate the return value before use.
  related:
  - SetBot
  - IsBotActive
metadata:
  confidence: 0.9
  generated_at: '2025-11-07T04:36:56.276593'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
