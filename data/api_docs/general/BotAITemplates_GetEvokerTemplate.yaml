api:
  class: BotAITemplates
  method: GetEvokerTemplate
  signature: BotAIConfig BotAITemplates::GetEvokerTemplate(bool healer)
documentation:
  brief: Returns the BotAIConfig template for Evoker class, optionally configured
    as a healer.
  description: The GetEvokerTemplate method retrieves a pre-configured BotAIConfig
    object tailored specifically for the Evoker class in World of Warcraft's TrinityCore
    environment. This configuration includes behavior settings, spell priorities,
    and AI decision-making parameters optimized for Evoker gameplay. When the 'healer'
    parameter is set to true, the returned template emphasizes healing abilities and
    support mechanics; otherwise, it focuses on damage-dealing capabilities. The method
    serves as a factory function to provide appropriate AI configurations based on
    role requirements without requiring manual setup of individual bot behaviors.
  parameters: []
  returns: A BotAIConfig object containing predefined AI settings for Evoker class
    bots, configured either as a healer or damage dealer depending on the 'healer'
    parameter value.
  examples:
  - title: Get Evoker Template for Damage Dealer
    code: 'BotAIConfig config = botTemplates.GetEvokerTemplate(false);

      // Use config to initialize an Evoker bot for DPS role'
    language: cpp
  - title: Get Evoker Template for Healer
    code: 'BotAIConfig healerConfig = botTemplates.GetEvokerTemplate(true);

      // Use healerConfig to initialize an Evoker bot for healing role'
    language: cpp
  notes: This method is part of the BotAITemplates class which manages AI configurations
    for various classes. The returned BotAIConfig objects are typically used to initialize
    bot behavior in TrinityCore's bot system. The implementation likely caches or
    pre-defines these templates to avoid runtime overhead during bot creation.
  warnings: Ensure that the returned BotAIConfig is properly initialized before use,
    as it may contain default values that need to be overridden for specific gameplay
    scenarios. Using this method without proper context may result in unexpected bot
    behavior.
  related:
  - GetMageTemplate
  - GetWarriorTemplate
  - GetPaladinTemplate
metadata:
  confidence: 0.85
  generated_at: '2025-11-04T14:43:20.591914'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
