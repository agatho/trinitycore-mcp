api:
  class: DispelCoordinator
  method: GetDebuffData
  signature: const DebuffData * DispelCoordinator::GetDebuffData(uint32 auraId)
documentation:
  brief: Retrieves debuff data associated with a specific aura ID for dispel coordination.
  description: The GetDebuffData method retrieves a const pointer to a DebuffData
    structure that contains information about how a specific aura should be handled
    during dispel operations. This method is used internally by the DispelCoordinator
    to determine whether an aura can be removed, what type of dispel is required,
    and other relevant mechanics for debuff management in World of Warcraft combat
    systems. The method performs a lookup based on the provided aura ID and returns
    a pointer to the corresponding debuff data entry. If no matching debuff data exists
    for the given aura ID, the method may return a null pointer or a default entry
    depending on implementation details.
  parameters:
  - name: auraId
    description: The unique identifier of the aura for which to retrieve debuff data.
      This corresponds to the aura's spell ID used in the game's spell system.
  returns: A const pointer to a DebuffData structure containing the debuff information
    for the specified aura ID, or nullptr if no matching debuff data is found.
  examples:
  - title: Basic Usage Example
    code: "const DebuffData* debuffData = dispelCoordinator->GetDebuffData(12345);\n\
      if (debuffData != nullptr)\n{\n    // Process the debuff data\n    std::cout\
      \ << \"Dispel type: \" << debuffData->dispelType << std::endl;\n}"
    language: cpp
  - title: Checking for Valid Debuff Data
    code: "uint32 auraId = 98765;\nconst DebuffData* data = dispelCoordinator->GetDebuffData(auraId);\n\
      if (data && data->canDispel)\n{\n    // This aura can be dispelled\n    if (data->dispelType\
      \ == DISPEL_MAGIC)\n    {\n        // Handle magic dispel logic\n    }\n}\n\
      else\n{\n    // Aura cannot be dispelled or does not exist\n}"
    language: cpp
  notes: This method is typically used in the context of spell and aura handling within
    TrinityCore's combat system. The returned DebuffData pointer should not be modified
    directly as it represents static configuration data. Performance-wise, this method
    likely uses a hash map or similar lookup structure for O(1) access to debuff data
    entries.
  warnings: Ensure that the auraId parameter is valid and corresponds to an actual
    aura in the game's spell database. Using invalid IDs may result in null returns
    or undefined behavior depending on implementation. The returned pointer should
    be checked for null before use to prevent crashes.
  related:
  - DispelCoordinator::AddDebuffData
  - DispelCoordinator::RemoveDebuffData
  - DispelCoordinator::HasDebuffData
metadata:
  confidence: 0.85
  generated_at: '2025-11-04T09:40:52.230561'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
