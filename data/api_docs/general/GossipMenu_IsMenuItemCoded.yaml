api:
  class: GossipMenu
  method: IsMenuItemCoded
  signature: bool GossipMenu::IsMenuItemCoded(uint32 orderIndex) const
documentation:
  brief: Checks if a gossip menu item at the specified order index is coded with custom
    logic.
  description: The IsMenuItemCoded method determines whether a specific gossip menu
    item, identified by its order index, has been assigned custom coded behavior rather
    than relying on default actions. This is typically used in conjunction with custom
    gossip scripts where certain menu items require special handling or conditional
    logic. The method evaluates the internal state of the gossip menu to return true
    if the item is marked as coded, false otherwise. It's commonly employed in quest
    gating, dynamic menu generation, and scripted interactions within the World of
    Warcraft TrinityCore environment.
  parameters: []
  returns: Returns true if the gossip menu item at the specified order index has custom
    coded behavior; returns false if it uses default action handling or if the index
    is invalid.
  examples:
  - title: Check if a specific gossip menu item is coded
    code: "if (gossipMenu->IsMenuItemCoded(2))\n{\n    // Handle custom logic for\
      \ menu item at index 2\n    player->ADD_GOSSIP_ITEM(GOSSIP_ICON_CHAT, \"Custom\
      \ Option\", GOSSIP_SENDER_MAIN, 1001);\n}\nelse\n{\n    // Use default behavior\n\
      \    player->ADD_GOSSIP_ITEM(GOSSIP_ICON_CHAT, \"Default Option\", GOSSIP_SENDER_MAIN,\
      \ 1002);\n}"
    language: cpp
  notes: This method assumes that menu items are indexed sequentially starting from
    zero. The implementation likely checks internal flags or metadata associated with
    each gossip menu item to determine if custom code is attached. Performance impact
    is minimal as it's a simple lookup operation.
  warnings: Ensure the orderIndex parameter is within valid bounds (0 to total number
    of menu items minus one) to avoid undefined behavior. This method should not be
    used on uninitialized or improperly constructed GossipMenu objects.
  related:
  - GossipMenu::AddMenuItem
  - GossipMenu::GetMenuItemCount
  - GossipMenu::ClearMenu
metadata:
  confidence: 0.9
  generated_at: '2025-11-04T04:39:24.538812'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
