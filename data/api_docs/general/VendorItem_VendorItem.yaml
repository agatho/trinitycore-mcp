api:
  class: VendorItem
  method: VendorItem
  signature: void VendorItem::VendorItem()
documentation:
  brief: Initializes a new VendorItem object with default values.
  description: The VendorItem constructor initializes a new instance of the VendorItem
    class, setting up internal data structures and default property values for vendor
    items in the TrinityCore server. This method is typically called when creating
    new vendor item entries for NPCs, establishing the foundational state required
    for item transactions within the game's trading system. The constructor ensures
    that all necessary member variables are properly initialized before the object
    can be used in vendor interactions.
  parameters: []
  returns: null
  examples:
  - title: Creating a new VendorItem instance
    code: 'VendorItem* newItem = new VendorItem();

      // The newItem is now ready to be configured with item data

      newItem->SetItemId(12345);

      newItem->SetMaxCount(10);

      newItem->SetExtendedCost(0);'
    language: cpp
  notes: This constructor should only be called when creating new vendor item entries.
    It does not perform any validation of item data; it simply initializes the object
    state. The VendorItem class is typically used in conjunction with NPC vendor systems
    where items are sold to players.
  warnings: null
  related:
  - VendorItem::~VendorItem
  - VendorItem::SetItemId
  - VendorItem::GetItemId
  - VendorItem::SetMaxCount
  - VendorItem::GetMaxCount
metadata:
  confidence: 0.9
  generated_at: '2025-11-08T10:17:45.204394'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
