api:
  class: InterruptUtils
  method: IsSpellChanneled
  signature: bool InterruptUtils::IsSpellChanneled(uint32 spellId)
documentation:
  brief: Determines whether a specified spell is channeled.
  description: The IsSpellChanneled method checks if a given spell ID corresponds
    to a spell that is channeled over time rather than cast instantly. This is important
    for interrupting spells, managing spell queues, and handling combat mechanics
    in World of Warcraft. The method typically queries internal spell data structures
    or databases to determine the spell's channeling status. It returns true if the
    spell is channeled, false otherwise.
  parameters: []
  returns: Returns true if the specified spell ID represents a channeled spell; otherwise
    returns false.
  examples:
  - title: Check if a spell is channeled
    code: "if (InterruptUtils::IsSpellChanneled(12345)) {\n    // Handle channeled\
      \ spell logic\n    printf(\"Spell is channeled\\n\");\n} else {\n    printf(\"\
      Spell is not channeled\\n\");\n}"
    language: cpp
  notes: This method likely relies on internal spell database lookups or cached spell
    properties. It should be used carefully in performance-critical code paths, as
    frequent calls may impact frame rate if the underlying lookup mechanism is not
    optimized.
  warnings: null
  related: []
metadata:
  confidence: 0.9
  generated_at: '2025-11-04T02:43:38.758171'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
