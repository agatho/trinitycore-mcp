api:
  class: NearestAttackableUnitInObjectRangeCheck
  method: operator()
  signature: bool NearestAttackableUnitInObjectRangeCheck::operator()(int * u)
documentation:
  brief: Evaluates whether a unit is attackable and within range for AI targeting
    purposes.
  description: The operator() method of NearestAttackableUnitInObjectRangeCheck serves
    as a predicate function used in TrinityCore's AI system to determine if a given
    unit meets the criteria for being an attackable target within a specified range.
    This check is typically employed by AI scripts and pathfinding algorithms to identify
    valid enemies or targets for combat actions. The method evaluates the unit's attackability
    status, distance from the reference object, and other relevant conditions required
    for targeting. It is commonly used in conjunction with container-based search
    operations where multiple units are evaluated against a set of criteria.
  parameters: []
  returns: Returns true if the unit pointed to by u is attackable and within range;
    otherwise returns false.
  examples:
  - title: Basic usage in AI targeting
    code: "NearestAttackableUnitInObjectRangeCheck checker;\nUnit* target = nullptr;\n\
      for (auto& unit : units)\n{\n    if (checker(&unit->GetGUID().GetRawValue()))\n\
      \    {\n        target = unit;\n        break;\n    }\n}"
    language: cpp
  notes: This method is part of the TrinityCore's AI targeting system and relies on
    internal state tracking for range calculations. It should only be used in contexts
    where a valid reference object exists for range checking. The parameter u is expected
    to point to an integer representing a unit's GUID or similar identifier.
  warnings: The method assumes that the input pointer u points to a valid unit identifier.
    Using an invalid or null pointer may result in undefined behavior. Ensure proper
    initialization of the check object before usage.
  related:
  - NearestAttackableUnitInObjectRangeCheck::NearestAttackableUnitInObjectRangeCheck
  - NearestAttackableUnitInObjectRangeCheck::operator()
  - AI::GetNextTarget
metadata:
  confidence: 0.85
  generated_at: '2025-11-08T23:18:30.009307'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
