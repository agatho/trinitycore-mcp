api:
  class: Item
  method: GetItemStatType
  signature: int32 Item::GetItemStatType(uint32 index) const
documentation:
  brief: Retrieves the stat type value for a specified item index.
  description: This method returns the stat type associated with a given item index,
    which represents the type of stat that an item possesses. The stat type is typically
    used to determine what kind of attribute or property an item provides, such as
    attack power, critical strike rating, or haste rating. It is commonly used in
    conjunction with other item methods to analyze and modify item properties during
    gameplay.
  parameters: []
  returns: Returns the stat type value for the specified index, which corresponds
    to a specific attribute type (e.g., ItemStatType::ITEM_STAT_ATTACK_POWER = 1,
    ItemStatType::ITEM_STAT_CRITICAL_STRIKE_RATING = 2). If the index is invalid or
    exceeds the item's stat count, it returns -1.
  examples:
  - title: Basic Usage Example
    code: "Item* item = player->GetItemByPos(0, 0);\nint32 statType = item->GetItemStatType(0);\n\
      if (statType >= 0)\n{\n    std::cout << \"Stat type: \" << statType << std::endl;\n\
      }"
    language: cpp
  notes: This method is part of the Item class and relies on internal item data structures.
    It should be used carefully when accessing item stats to avoid invalid memory
    access or incorrect results.
  warnings: The index parameter must be within valid bounds; otherwise, the method
    may return unexpected values or cause undefined behavior. Be cautious when using
    this method with items that have multiple stat types.
  related:
  - GetItemStatValue
  - GetItemStatType
  - GetItemEnchantment
metadata:
  confidence: 0.95
  generated_at: '2025-10-31T04:58:54.904502'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
