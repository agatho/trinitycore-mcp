api:
  class: LootMethodChangedHandler
  method: GetHandlerName
  signature: std::string LootMethodChangedHandler::GetHandlerName() const
documentation:
  brief: Returns the name of the LootMethodChangedHandler class
  description: The GetHandlerName method is a const member function that returns a
    std::string containing the human-readable name of the LootMethodChangedHandler
    class. This method is typically used for debugging, logging, or identifying handler
    instances within the TrinityCore framework's event handling system. The returned
    string represents the handler's type and is often utilized in system diagnostics
    to track which handler is processing specific loot-related events.
  parameters: []
  returns: A std::string containing the name of the LootMethodChangedHandler class,
    typically formatted as "LootMethodChangedHandler" or similar identifier.
  examples:
  - title: Basic Usage
    code: 'LootMethodChangedHandler handler;

      std::string handlerName = handler.GetHandlerName();

      // handlerName will contain something like "LootMethodChangedHandler"'
    language: cpp
  notes: This method is implemented as a const function, meaning it does not modify
    any member variables of the class instance. It's commonly used in conjunction
    with other handler identification mechanisms within TrinityCore's event system.
  warnings: null
  related: []
metadata:
  confidence: 0.95
  generated_at: '2025-11-01T10:58:13.370946'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
