api:
  class: WorldSession
  method: HandleGuildUpdateInfoText
  signature: void WorldSession::HandleGuildUpdateInfoText(WorldPackets::Guild::GuildUpdateInfoText
    & packet)
documentation:
  brief: Updates the guild's information text field with data from a client packet
  description: The HandleGuildUpdateInfoText method processes incoming guild information
    text updates from players. It handles the synchronization of guild description
    or information text that players can modify through the guild interface. This
    method typically validates the player's permissions within the guild before applying
    any changes to the guild's info text field in the database. The update is usually
    triggered when a guild leader or officer modifies the guild's general information
    text via the client UI.
  parameters: []
  returns: null
  examples:
  - title: Basic usage in packet handling
    code: "void WorldSession::HandleGuildUpdateInfoText(WorldPackets::Guild::GuildUpdateInfoText&\
      \ packet)\n{\n    if (!GetPlayer() || !GetPlayer()->GetGuild())\n        return;\n\
      \n    // Process the guild info text update\n    GetPlayer()->GetGuild()->SetInfoText(packet.InfoText);\n\
      }"
    language: cpp
  notes: This method is part of the guild management system and requires proper authentication
    and guild membership checks. The packet contains the new information text that
    should be validated for length and content before being saved to the database.
  warnings: Improper validation of the info text could lead to SQL injection or excessive
    data storage. Ensure proper string sanitization and length limits are enforced
    before updating the database.
  related:
  - HandleGuildInfo
  - HandleGuildSetInfoText
  - GetPlayer
  - GetGuild
metadata:
  confidence: 0.9
  generated_at: '2025-11-01T19:21:46.307253'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
