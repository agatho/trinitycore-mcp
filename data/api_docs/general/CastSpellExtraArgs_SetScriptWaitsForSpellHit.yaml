api:
  class: CastSpellExtraArgs
  method: SetScriptWaitsForSpellHit
  signature: CastSpellExtraArgs & CastSpellExtraArgs::SetScriptWaitsForSpellHit(bool
    scriptWaitsForSpellHit)
documentation:
  brief: Sets whether the script should wait for the spell hit event before continuing
    execution.
  description: The SetScriptWaitsForSpellHit method configures a CastSpellExtraArgs
    object to control whether a script will pause execution until the spell's hit
    event is processed. This is particularly useful in custom spell implementations
    where timing and synchronization with spell effects are critical. When enabled,
    the script execution will be delayed until the spell has fully registered its
    hit on the target. The method returns a reference to the same CastSpellExtraArgs
    object, enabling method chaining for cleaner code structure.
  parameters: []
  returns: A reference to the CastSpellExtraArgs object, allowing for method chaining.
  examples:
  - title: Basic Usage
    code: 'CastSpellExtraArgs args;

      args.SetScriptWaitsForSpellHit(true);

      // Spell will now wait for hit event before continuing

      '
    language: cpp
  - title: Method Chaining
    code: "CastSpellExtraArgs args;\nargs.SetScriptWaitsForSpellHit(true)\n    .SetTriggered(true)\n\
      \    .SetIgnoreAuraState(AURA_STATE_HUNTER_PET)\n    ;\n// Multiple configuration\
      \ options set in sequence\n"
    language: cpp
  notes: This method is primarily used in custom spell scripts and AI logic within
    TrinityCore. It affects the execution flow of scripted spells, particularly when
    dealing with complex spell interactions or delayed effects. The setting only applies
    to spells that are explicitly handled through script-based invocation.
  warnings: Setting this flag incorrectly may cause unexpected delays or deadlocks
    in script execution if not properly synchronized with the spell's actual hit processing.
    Ensure that any waiting logic is compatible with the overall spell system architecture.
  related:
  - GetScriptWaitsForSpellHit
  - SetTriggered
  - SetIgnoreAuraState
metadata:
  confidence: 0.9
  generated_at: '2025-11-04T12:17:04.953231'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
