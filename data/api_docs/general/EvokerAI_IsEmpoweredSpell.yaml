api:
  class: EvokerAI
  method: IsEmpoweredSpell
  signature: bool EvokerAI::IsEmpoweredSpell(int spellId)
documentation:
  brief: Determines whether a specified spell is currently empowered for the evoker.
  description: The IsEmpoweredSpell method checks if a given spell ID is in an empowered
    state within the EvokerAI context. This method evaluates the current state of
    the evoker's abilities and returns true if the spell is empowered, indicating
    that it can be cast with enhanced effects or additional properties. The empowerment
    status may depend on factors such as the evoker's current mana level, cooldowns,
    or specific game mechanics related to the Evoker class in TrinityCore. This method
    is typically used during spell selection logic or when determining whether a spell
    should be prioritized or modified in behavior.
  parameters:
  - name: spellId
    description: The unique identifier of the spell to check for empowerment status.
      Valid values are integer IDs corresponding to spells defined in the TrinityCore
      spell database.
  returns: Returns true if the specified spell is currently empowered, otherwise returns
    false. An empowered spell typically has enhanced effects or special properties
    that differ from its normal casting state.
  examples:
  - title: Basic usage to check empowerment status
    code: "if (evokerAI->IsEmpoweredSpell(34767)) {\n    // Spell is empowered, perform\
      \ special logic\n    DoEmpoweredAction();\n} else {\n    // Spell is not empowered,\
      \ use normal behavior\n    DoNormalAction();\n}"
    language: cpp
  - title: Using in spell cast decision making
    code: "SpellInfo const* spellInfo = sSpellMgr->GetSpellInfo(spellId);\nif (spellInfo\
      \ && evokerAI->IsEmpoweredSpell(spellId)) {\n    // Cast empowered version of\
      \ the spell\n    me->CastSpell(target, spellId, true);\n} else {\n    // Cast\
      \ regular version\n    me->CastSpell(target, spellId, false);\n}"
    language: cpp
  notes: This method relies on internal state tracking of the evoker's empowerment
    conditions. It may be affected by external factors such as mana levels, cooldowns,
    or active buff/debuff effects that influence spell empowerment. Developers should
    ensure that the spell ID passed to this method corresponds to a valid spell defined
    in the TrinityCore database.
  warnings: Ensure that the spellId parameter is validated before calling this method
    to avoid unexpected behavior from invalid spell IDs. The method may return incorrect
    results if called outside of appropriate AI update contexts where empowerment
    states are properly updated.
  related:
  - EvokerAI::IsSpellEmpowered
  - EvokerAI::SetEmpoweredSpell
  - EvokerAI::UpdateEmpowerment
metadata:
  confidence: 0.85
  generated_at: '2025-11-07T14:05:51.546096'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
