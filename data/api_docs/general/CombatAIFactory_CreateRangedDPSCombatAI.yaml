api:
  class: CombatAIFactory
  method: CreateRangedDPSCombatAI
  signature: int CombatAIFactory::CreateRangedDPSCombatAI(int * bot)
documentation:
  brief: Creates and initializes a ranged damage-dealing combat AI for a bot
  description: The CreateRangedDPSCombatAI method instantiates a new ranged damage-per-second
    combat AI behavior for a specified bot character. This function sets up the AI
    with appropriate ranged attack patterns, targeting logic, and movement behaviors
    suitable for melee-range combat scenarios. The method typically integrates with
    TrinityCore's AI system to assign specific combat roles to NPC or player-controlled
    bots. It may return an identifier or status code indicating success or failure
    of the AI creation process.
  parameters:
  - name: bot
    description: Pointer to an integer representing the bot's unique identifier or
      index in the system. This parameter specifies which bot instance should have
      the ranged DPS AI created and assigned.
  returns: Returns an integer value indicating the outcome of the AI creation process.
    A positive value typically indicates success, while a negative value may represent
    an error condition such as invalid bot reference or insufficient resources.
  examples:
  - title: Basic usage of CreateRangedDPSCombatAI
    code: "int botId = 42;\nint result = factory->CreateRangedDPSCombatAI(&botId);\n\
      if (result >= 0) {\n    // AI successfully created for bot with ID 42\n    printf(\"\
      Ranged DPS AI created for bot %d\\n\", botId);\n} else {\n    // Handle error\n\
      \    printf(\"Failed to create AI for bot %d\\n\", botId);\n}"
    language: cpp
  notes: This method is part of TrinityCore's AI framework and assumes that the specified
    bot has been properly initialized and registered within the system. The actual
    implementation may involve allocating memory for AI state, setting up behavior
    trees, or linking to existing combat modules.
  warnings: Ensure that the bot parameter points to a valid bot instance before calling
    this method. Passing an invalid or uninitialized bot reference could lead to undefined
    behavior or crashes.
  related:
  - CreateMeleeDPSCombatAI
  - CreateHealerCombatAI
  - CreateTankCombatAI
metadata:
  confidence: 0.85
  generated_at: '2025-11-02T09:36:42.467538'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
