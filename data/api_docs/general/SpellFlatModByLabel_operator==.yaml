api:
  class: SpellFlatModByLabel
  method: operator==
  signature: bool SpellFlatModByLabel::operator==(const SpellFlatModByLabel & right)
    const
documentation:
  brief: Compares two SpellFlatModByLabel objects for equality based on their internal
    data members.
  description: The operator== method performs a comparison between this SpellFlatModByLabel
    instance and another SpellFlatModByLabel object to determine if they are equivalent.
    This comparison typically involves checking the underlying spell modifier data,
    such as the label identifier, modifier type, and value, to ensure both objects
    represent the same spell flat modification. In the context of TrinityCore's spell
    system, this method is used to identify duplicate or identical spell modifiers
    that may be applied to a spell effect. The comparison is performed in a const
    context, meaning it does not modify either object during evaluation.
  parameters: []
  returns: Returns true if both SpellFlatModByLabel objects have identical internal
    data members, indicating they represent the same spell flat modification; otherwise
    returns false.
  examples:
  - title: Basic equality check between two SpellFlatModByLabel instances
    code: 'SpellFlatModByLabel mod1;

      SpellFlatModByLabel mod2;

      bool areEqual = (mod1 == mod2); // Returns true if both objects are identical'
    language: cpp
  - title: Using equality operator in spell modifier validation
    code: 'std::vector<SpellFlatModByLabel> modifiers;

      SpellFlatModByLabel newMod;

      bool exists = std::find(modifiers.begin(), modifiers.end(), newMod) != modifiers.end();

      // Checks if a similar modifier already exists in the collection'
    language: cpp
  notes: This operator is likely implemented using member-wise comparison of key data
    members such as spell ID, label, and modifier value. It's important to note that
    this comparison may be used during spell processing to prevent duplicate application
    of modifiers or to identify redundant spell effects.
  warnings: null
  related:
  - operator!=
  - SpellFlatModByLabel::SpellFlatModByLabel()
  - SpellFlatModByLabel::~SpellFlatModByLabel()
metadata:
  confidence: 0.9
  generated_at: '2025-11-08T21:36:20.393257'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
