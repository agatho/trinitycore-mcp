api:
  class: Player
  method: RemoveMail
  signature: void Player::RemoveMail(uint64 id)
documentation:
  brief: Removes a specific mail item from the player's mailbox.
  description: The RemoveMail method removes a mail item identified by its unique
    ID from the player's mailbox. This method is typically used to delete mail items
    that have been read or processed, ensuring that the player's mailbox remains clean
    and organized. The operation affects the mail's status in the database, removing
    it from the player's inbox and potentially freeing up space for new mail items.
  parameters:
  - name: id
    description: The unique identifier of the mail item to be removed. This is typically
      a uint64 value representing the mail ID as stored in the database.
  returns: null
  examples:
  - title: Basic Mail Removal Example
    code: 'Player* player = ObjectAccessor::GetPlayer("player_name");

      uint64 mailId = 12345;

      player->RemoveMail(mailId);'
    language: cpp
  notes: This method should be used carefully as it directly affects database records.
    It is recommended to ensure the mail ID exists before calling this method, otherwise
    it may lead to database inconsistencies or errors.
  warnings: Improper use of RemoveMail can result in database corruption if the mail
    ID does not exist or has already been removed. Always verify that the mail item
    exists before calling this method.
  related:
  - GetMail
  - MailItem
  - Mailbox
metadata:
  confidence: 0.9
  generated_at: '2025-10-31T00:20:13.293894'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
