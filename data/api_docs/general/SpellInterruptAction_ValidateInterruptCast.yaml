api:
  class: SpellInterruptAction
  method: ValidateInterruptCast
  signature: bool SpellInterruptAction::ValidateInterruptCast(BotAI * ai, ::Unit *
    target, uint32 interruptSpell) const
documentation:
  brief: Validates whether a spell interrupt can be cast on a target unit.
  description: The ValidateInterruptCast method checks if a specified interrupt spell
    can be legally cast against a given target unit within the context of a bot AI.
    This validation typically ensures that the target is valid, the spell is available,
    and the conditions for casting the interrupt are met. It's used in spell selection
    logic to prevent invalid or harmful spell casts during combat. The method operates
    within the TrinityCore framework and integrates with the BotAI system for automated
    gameplay decisions.
  parameters:
  - name: ai
    description: Pointer to the BotAI instance managing the bot's behavior and decision-making
      process.
  - name: target
    description: Pointer to the Unit object representing the target on which the interrupt
      spell is to be cast.
  - name: interruptSpell
    description: The spell ID of the interrupt spell being validated for casting against
      the target.
  returns: Returns true if the interrupt spell can be cast successfully on the target;
    otherwise returns false.
  examples:
  - title: Basic usage in bot AI logic
    code: "if (spellInterruptAction.ValidateInterruptCast(ai, target, interruptSpellId))\
      \ {\n    // Proceed with casting the interrupt spell\n    ai->GetUnit()->CastSpell(target,\
      \ interruptSpellId, true);\n}"
    language: cpp
  - title: Using within a conditional interrupt decision
    code: "uint32 interruptSpell = 1766; // Example: Kick\nif (ValidateInterruptCast(ai,\
      \ target, interruptSpell)) {\n    ai->GetUnit()->CastSpell(target, interruptSpell,\
      \ true);\n} else {\n    // Handle invalid cast case\n    ai->TellMaster(\"Cannot\
      \ interrupt target\");\n}"
    language: cpp
  notes: This method likely performs checks such as target validity, spell availability,
    line of sight, and combat state. It is part of a larger system for automated spell
    casting in bot AI behavior.
  warnings: Ensure that the BotAI instance and target are valid before calling this
    method to avoid crashes or undefined behavior. The interruptSpell ID must correspond
    to an actual interrupt spell in the game database.
  related:
  - SpellInterruptAction::Execute
  - BotAI::GetUnit
  - Unit::CastSpell
metadata:
  confidence: 0.85
  generated_at: '2025-11-07T09:27:58.992367'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
