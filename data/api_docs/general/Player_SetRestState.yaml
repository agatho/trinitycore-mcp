api:
  class: Player
  method: SetRestState
  signature: void Player::SetRestState(RestTypes type, PlayerRestState state)
documentation:
  brief: Sets the player's rest state to indicate whether they are resting or not.
  description: This method controls the player's rest state within the game world,
    determining whether the player is currently in a resting state or not. It affects
    various game mechanics including experience gains, resource regeneration, and
    movement speed. The method is typically used during gameplay events such as entering
    or leaving a rest area, or when performing actions related to resting. The RestTypes
    parameter defines the type of rest being set, while PlayerRestState specifies
    whether the player is actually resting or not.
  parameters:
  - name: type
    description: The type of rest state being set, typically indicating whether the
      player is resting in a city, inn, or other location. Valid values include RestTypes::RestTypeInn
      and RestTypes::RestTypeCity.
  - name: state
    description: The actual rest state of the player, specifying whether they are
      currently resting or not. Valid values include PlayerRestState::RESTSTATE_RESTING
      and PlayerRestState::RESTSTATE_NOT_RESTING.
  returns: null
  examples:
  - title: Basic usage example
    code: 'Player* player = new Player();

      player->SetRestState(RestTypes::RestTypeInn, PlayerRestState::RESTSTATE_RESTING);

      '
    language: cpp
  notes: This method is typically called during gameplay when a player enters or leaves
    a resting area. It affects the player's experience gain rate and resource regeneration
    speed. The method should be used carefully as it can impact game balance and player
    progression.
  warnings: Improper use of this method may cause unexpected behavior in player experience
    gains or resource regeneration, potentially leading to game imbalance issues.
    Avoid calling this method without proper context.
  related:
  - GetRestState
  - IsResting
metadata:
  confidence: 0.95
  generated_at: '2025-10-30T14:46:59.450340'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
