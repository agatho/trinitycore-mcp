api:
  class: Channel
  method: SetMute
  signature: void Channel::SetMute(const Player * player, const std::string & newname)
documentation:
  brief: Mutes a specified player in the channel.
  description: The SetMute method is used to mute a specific player within a channel,
    preventing them from sending messages or participating in chat. This functionality
    is typically used by channel moderators or administrators to enforce rules and
    maintain order. The method takes a player object and a new name string as parameters,
    which may be used for identifying the target player and potentially updating internal
    state or logging. Muting a player does not remove them from the channel but restricts
    their ability to communicate.
  parameters:
  - name: player
    description: A pointer to the Player object representing the player to be muted.
      This parameter must not be null and should point to a valid player instance
      in the game world.
  - name: newname
    description: A string containing the name of the player to be muted. This is used
      for identification purposes, potentially for logging or updating channel state
      information.
  returns: null
  examples:
  - title: Basic Mute Usage
    code: "Channel* channel = sChannelMgr->GetChannel(\"General\", player);\nif (channel)\n\
      {\n    channel->SetMute(targetPlayer, targetPlayer->GetName());\n}"
    language: cpp
  notes: This method likely updates internal channel state and may send a notification
    to other channel members about the mute action. It's important to ensure that
    the player pointer is valid before calling this method to avoid undefined behavior.
  warnings: Calling this method with a null player pointer can lead to crashes or
    undefined behavior. Ensure proper validation of inputs before invoking the method.
  related:
  - Channel::SetUnmute
  - Channel::IsMuted
  - Channel::GetPlayerCount
metadata:
  confidence: 0.9
  generated_at: '2025-11-04T12:04:59.370702'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
