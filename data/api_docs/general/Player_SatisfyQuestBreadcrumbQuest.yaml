api:
  class: Player
  method: SatisfyQuestBreadcrumbQuest
  signature: bool Player::SatisfyQuestBreadcrumbQuest(const Quest * qInfo, bool msg)
    const
documentation:
  brief: Satisfies a quest breadcrumb quest by marking it as completed
  description: This method marks a quest as completed for the player, effectively
    satisfying the quest requirements. It is used in conjunction with quest breadcrumb
    mechanics to ensure proper quest progression and completion. The method handles
    the internal quest tracking and updates the player's quest log accordingly. This
    function typically occurs when a player has fulfilled all quest objectives related
    to a specific quest.
  parameters: []
  returns: Returns true if the quest was successfully satisfied, false otherwise
  examples:
  - title: Basic usage example
    code: 'Player* player = GetPlayer();

      Quest* quest = sObjectMgr->GetQuestTemplate(12345);

      bool result = player->SatisfyQuestBreadcrumbQuest(quest, true);'
    language: cpp
  notes: This method should only be called when the player has completed all required
    objectives for the quest. The method internally updates the player's quest log
    and may trigger related events or rewards.
  warnings: This method should not be used to bypass quest requirements. It is intended
    only for properly completing quests that have been fulfilled by the player.
  related:
  - Quest::GetQuestId
  - Player::GetQuestStatus
  - Player::CompleteQuest
metadata:
  confidence: 0.85
  generated_at: '2025-10-31T00:00:21.105653'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
