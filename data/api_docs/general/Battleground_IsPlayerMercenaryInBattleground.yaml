api:
  class: Battleground
  method: IsPlayerMercenaryInBattleground
  signature: bool Battleground::IsPlayerMercenaryInBattleground(int guid) const
documentation:
  brief: Checks whether a player with the specified GUID is registered as a mercenary
    in the battleground.
  description: This method determines if a given player, identified by their global
    unique identifier (GUID), is currently participating in the battleground as a
    mercenary. In World of Warcraft's TrinityCore implementation, mercenaries are
    players who have joined a battleground through specific mechanisms such as the
    Mercenary system or special PvP events. The method performs a lookup within the
    battleground's internal data structures to verify the player's mercenary status.
    It is typically used during battleground operations to enforce rules, manage rewards,
    or track player participation in mercenary-based gameplay modes.
  parameters:
  - name: guid
    description: The global unique identifier (GUID) of the player to check for mercenary
      status within the battleground.
  returns: Returns true if the player with the specified GUID is registered as a mercenary
    in the battleground; otherwise, returns false.
  examples:
  - title: Check if Player is Mercenary
    code: "if (bg->IsPlayerMercenaryInBattleground(playerGuid)) {\n    // Handle mercenary-specific\
      \ logic\n    sLog->outString(\"Player %u is a mercenary in this battleground.\"\
      , playerGuid);\n} else {\n    sLog->outString(\"Player %u is not a mercenary\
      \ in this battleground.\", playerGuid);\n}"
    language: cpp
  notes: This method assumes that the battleground object is properly initialized
    and that the GUID provided corresponds to an existing player. It may perform internal
    lookups or checks against cached data, so repeated calls should be considered
    for performance impact in high-frequency contexts.
  warnings: Ensure that the GUID passed to this method is valid and corresponds to
    a known player; invalid or malformed GUIDs could lead to undefined behavior or
    incorrect results. This method should not be called on battlegrounds that are
    not active or have been destroyed.
  related:
  - GetPlayerCountByTeam
  - IsPlayerInBattleground
  - AddPlayerToBattleground
metadata:
  confidence: 0.9
  generated_at: '2025-10-31T17:03:36.742861'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
