api:
  class: Item
  method: GetOwner
  signature: Player * Item::GetOwner() const
documentation:
  brief: Returns the player owner of the item
  description: The GetOwner method retrieves the Player object that owns the current
    item. This method is commonly used in item-related game mechanics to determine
    which player has possession of an item, particularly when handling item interactions,
    loot distribution, or when performing operations on items that require player
    context. The method returns a pointer to the player who owns the item, and may
    return nullptr if the item has no owner (such as when it's not associated with
    any player). This functionality is essential for maintaining proper game state
    and ensuring correct handling of item ownership in TrinityCore.
  parameters: []
  returns: Returns a pointer to the Player object that owns the item. If the item
    has no owner, this method returns nullptr. The returned pointer may be null if
    the item is not associated with any player, such as when it's dropped on the ground
    or created without a proper owner.
  examples:
  - title: Basic usage example
    code: "Player* owner = item->GetOwner();\nif (owner)\n{\n    // Item has an owner\n\
      \    sLog->outString(\"Item owned by player: %s\", owner->GetName().c_str());\n\
      }\nelse\n{\n    // Item has no owner\n    sLog->outString(\"Item has no owner\"\
      );\n}"
    language: cpp
  notes: The method is thread-safe and does not require any special considerations
    for usage. It is typically used in conjunction with other item-related methods
    to determine ownership context. Performance-wise, this method is lightweight and
    does not involve complex operations.
  warnings: This method should be used carefully when checking for null values, as
    returning nullptr can lead to crashes if not handled properly. Always ensure proper
    checks are in place before using the returned pointer.
  related:
  - GetGUID
  - GetOwner
  - GetUInt32Value
  - GetUInt64Value
metadata:
  confidence: 0.95
  generated_at: '2025-10-31T04:56:51.206925'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
