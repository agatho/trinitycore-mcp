api:
  class: Item
  method: SetArtifactPower
  signature: void Item::SetArtifactPower(uint16 artifactPowerId, uint8 purchasedRank,
    uint8 currentRankWithBonus)
documentation:
  brief: Sets the artifact power for an item.
  description: This method configures the artifact power associated with an item,
    allowing developers to control the artifact power ID, purchased rank, and current
    rank with bonus. It is used primarily in the context of artifact weapons and gear
    within World of Warcraft's artifact system. The method updates internal data structures
    to reflect the new artifact power state.
  parameters:
  - name: artifactPowerId
    description: The unique identifier for the artifact power being set, typically
      representing a specific artifact power type or slot.
  - name: purchasedRank
    description: The rank that has been purchased for this artifact power, indicating
      how many ranks have been bought in the artifact system.
  - name: currentRankWithBonus
    description: The current rank including any bonus from artifact upgrades or effects,
      representing the total effective rank of the artifact power.
  returns: null
  examples:
  - title: Basic Usage Example
    code: 'Item* item = new Item();

      item->SetArtifactPower(12345, 3, 5);'
    language: cpp
  - title: Advanced Usage Example
    code: 'Item* item = GetItemByGuid("item_123456789");

      item->SetArtifactPower(artifactPowerId, purchasedRank, currentRankWithBonus);

      // Update artifact power data for item'
    language: cpp
  notes: This method should only be called when the item is in a valid state for artifact
    power modification. It's recommended to verify that the item has been properly
    initialized before calling this method.
  warnings: Improper use of this method can lead to incorrect artifact power states,
    potentially causing game-breaking issues or invalid behavior in artifact systems.
    Ensure all parameters are within valid ranges.
  related:
  - Item::GetArtifactPower
  - Item::SetArtifactPower
  - Item::GetArtifactPowerId
metadata:
  confidence: 0.9
  generated_at: '2025-10-31T04:38:37.050502'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
