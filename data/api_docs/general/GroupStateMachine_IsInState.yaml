api:
  class: GroupStateMachine
  method: IsInState
  signature: bool GroupStateMachine::IsInState(GroupState state) const
documentation:
  brief: Checks if the group state machine is currently in the specified group state.
  description: The IsInState method determines whether the GroupStateMachine object
    is currently operating within a particular GroupState. This functionality is essential
    for managing group behavior and transitions in World of Warcraft's TrinityCore
    framework, particularly when handling group events such as joining, leaving, or
    changing group roles. The method performs a direct comparison against the current
    internal state of the group state machine and returns true if the specified state
    matches the current state, otherwise false. This check is commonly used in conditional
    logic to determine appropriate actions based on the group's current operational
    mode.
  parameters: []
  returns: Returns true if the GroupStateMachine is currently in the specified GroupState;
    otherwise returns false.
  examples:
  - title: Basic State Check
    code: "if (groupStateMachine.IsInState(GroupState::GROUP_STATE_IDLE))\n{\n   \
      \ // Perform idle group actions\n    LOG_DEBUG(\"group\", \"Group is currently\
      \ idle.\");\n}"
    language: cpp
  - title: Conditional Group Action Based on State
    code: "if (groupStateMachine.IsInState(GroupState::GROUP_STATE_RAID))\n{\n   \
      \ // Execute raid-specific logic\n    group->SendRaidUpdate();\n}\nelse if (groupStateMachine.IsInState(GroupState::GROUP_STATE_PARTY))\n\
      {\n    // Execute party-specific logic\n    group->SendPartyUpdate();\n}"
    language: cpp
  notes: This method is a constant function and does not modify the state of the GroupStateMachine.
    It should be used in performance-critical sections as it performs only a simple
    state comparison operation. The GroupState enum values are typically defined in
    the TrinityCore codebase and represent different operational modes of a group
    such as idle, party, raid, etc.
  warnings: Ensure that the GroupState parameter passed to this method is valid and
    corresponds to an actual defined state within the GroupStateMachine's state enumeration.
    Using an invalid or undefined GroupState value could lead to unexpected behavior
    or incorrect return values.
  related:
  - GroupStateMachine::GetState
  - GroupStateMachine::SetState
  - GroupStateMachine::TransitionToState
metadata:
  confidence: 0.95
  generated_at: '2025-11-04T03:52:56.469422'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
