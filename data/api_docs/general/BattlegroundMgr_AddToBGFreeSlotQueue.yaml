api:
  class: BattlegroundMgr
  method: AddToBGFreeSlotQueue
  signature: void BattlegroundMgr::AddToBGFreeSlotQueue(Battleground * bg)
documentation:
  brief: Adds a battleground to the free slot queue for player assignment.
  description: The AddToBGFreeSlotQueue method is responsible for placing a battleground
    instance into a queue that tracks which battlegrounds have available slots for
    players to join. This mechanism supports the automatic assignment of players to
    battlegrounds when they queue, ensuring balanced matchmaking and efficient resource
    utilization. The method typically gets invoked during battleground initialization
    or when a new player joins an existing battleground that has room. It plays a
    key role in maintaining the integrity of the battleground system's player distribution
    logic.
  parameters: []
  returns: null
  examples:
  - title: Adding a Battleground to Free Slot Queue
    code: "Battleground* bg = sBattlegroundMgr->GetBattlegroundTemplate(BG_TYPE_WSG);\n\
      if (bg)\n{\n    sBattlegroundMgr->AddToBGFreeSlotQueue(bg);\n}"
    language: cpp
  notes: This method is typically called internally by the battleground management
    system. Developers should not need to call this directly unless implementing custom
    battleground logic or extending existing battleground functionality.
  warnings: null
  related:
  - BattlegroundMgr::RemoveFromBGFreeSlotQueue
  - BattlegroundMgr::GetBattleground
  - BattlegroundMgr::CreateBattleground
metadata:
  confidence: 0.9
  generated_at: '2025-11-02T12:40:23.982017'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
