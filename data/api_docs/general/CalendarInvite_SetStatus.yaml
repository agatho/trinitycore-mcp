api:
  class: CalendarInvite
  method: SetStatus
  signature: void CalendarInvite::SetStatus(CalendarInviteStatus status)
documentation:
  brief: Updates the status of a calendar invite.
  description: The SetStatus method modifies the current status of a calendar invite,
    typically used to reflect user responses such as accepted, declined, or tentative.
    This method is part of the TrinityCore's calendar system and is essential for
    managing event participation. The status change affects how the invite appears
    in the player's calendar interface and influences event coordination within the
    game world.
  parameters:
  - name: status
    description: The new CalendarInviteStatus to set for this invite. Valid values
      include CALENDAR_INVITE_STATUS_UNKNOWN, CALENDAR_INVITE_STATUS_ACCEPTED, CALENDAR_INVITE_STATUS_DECLINED,
      CALENDAR_INVITE_STATUS_TENTATIVE, and CALENDAR_INVITE_STATUS_CONFIRMED.
  returns: null
  examples:
  - title: Setting invite status to accepted
    code: 'CalendarInvite* invite = new CalendarInvite();

      invite->SetStatus(CALENDAR_INVITE_STATUS_ACCEPTED);'
    language: cpp
  - title: Updating invite status based on user input
    code: "void HandleUserResponse(uint32 inviteId, bool accepted)\n{\n    CalendarInvite*\
      \ invite = sCalendarMgr->GetInvite(inviteId);\n    if (invite)\n    {\n    \
      \    if (accepted)\n            invite->SetStatus(CALENDAR_INVITE_STATUS_ACCEPTED);\n\
      \        else\n            invite->SetStatus(CALENDAR_INVITE_STATUS_DECLINED);\n\
      \    }\n}"
    language: cpp
  notes: This method likely triggers internal updates to the calendar event system
    and may send notifications to other participants. It should be called only when
    the invite object is properly initialized and valid.
  warnings: Calling SetStatus with an invalid CalendarInviteStatus value can lead
    to undefined behavior or client-side display issues. Ensure that the status parameter
    conforms to the expected enum values defined in the calendar system.
  related:
  - GetStatus
  - GetInviteId
  - GetEventId
metadata:
  confidence: 0.9
  generated_at: '2025-11-04T12:21:04.780663'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
