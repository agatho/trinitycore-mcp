api:
  class: Player
  method: TakeQuestSourceItem
  signature: bool Player::TakeQuestSourceItem(uint32 questId, bool msg)
documentation:
  brief: TakeQuestSourceItem retrieves the quest source item for a player's current
    quest.
  description: This method handles the retrieval and removal of quest source items
    from a player's inventory when they accept a quest. It ensures that the appropriate
    item is removed from the player's inventory based on the quest ID provided, allowing
    the player to progress with the quest. The method also supports optional messaging
    to inform the player about the item removal. This functionality is crucial for
    maintaining game mechanics related to quest progression and item management within
    World of Warcraft.
  parameters:
  - name: questId
    description: The unique identifier of the quest being accepted by the player.
      This ID determines which quest source item will be retrieved from the player's
      inventory.
  - name: msg
    description: A boolean flag indicating whether to send a message to the player
      about the item removal. If true, the player receives notification about the
      item being removed.
  returns: Returns true if the quest source item was successfully removed from the
    player's inventory; otherwise returns false if the item could not be found or
    removed.
  examples:
  - title: Basic usage example
    code: "Player* player = new Player();\nbool success = player->TakeQuestSourceItem(12345,\
      \ true);\nif (success) {\n    // Item successfully removed\n}"
    language: cpp
  - title: Advanced usage example
    code: "Player* player = new Player();\nbool success = player->TakeQuestSourceItem(12345,\
      \ false);\nif (!success) {\n    // Item not found or removal failed\n}"
    language: cpp
  notes: 'Implementation details: This method likely interacts with the player''s
    inventory system to locate and remove the quest source item. It may involve checking
    for specific item IDs in the player''s inventory, and could be affected by game
    mechanics such as item stacking or quest completion conditions.'
  warnings: This method should only be called when a player is actually accepting
    a quest; calling it outside of a quest acceptance context can lead to incorrect
    item removal or game state issues. Also, if the quest source item does not exist
    in the player's inventory, the method may return false without any error message.
  related:
  - GetQuestSourceItem
  - QuestAccept
  - Player::AddItem
metadata:
  confidence: 0.85
  generated_at: '2025-10-30T23:24:08.756463'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
