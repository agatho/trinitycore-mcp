api:
  class: Player
  method: GetTrader
  signature: Player * Player::GetTrader() const
documentation:
  brief: Returns the player object of the trader in the current trade window
  description: The GetTrader method retrieves the Player object associated with the
    current trade window. This method is typically used in the context of trading
    mechanics where a player is engaged in a trade with another player. The method
    returns a pointer to the Player object representing the trader, which can be used
    for various operations such as checking if the trader is a valid player, accessing
    their properties, or performing actions related to the trade. This method is part
    of the trade window functionality and should only be called when a trade is active
    between two players.
  parameters: []
  returns: Returns a pointer to the Player object representing the trader in the current
    trade window. If no trade is currently active, or if the trader is not a valid
    player, this method may return nullptr. The returned Player object can be used
    to access various player properties and perform operations related to the trade.
  examples:
  - title: Basic usage example
    code: "Player* trader = player->GetTrader();\nif (trader)\n{\n    // Perform actions\
      \ with the trader\n    uint32_t traderGuid = trader->GetGUID();\n}"
    language: cpp
  - title: Advanced usage example
    code: "Player* trader = player->GetTrader();\nif (trader && trader->IsInWorld())\n\
      {\n    // Check if trader is in the same zone\n    if (trader->GetZoneId() ==\
      \ player->GetZoneId())\n    {\n        // Proceed with trade operations\n  \
      \  }\n}"
    language: cpp
  notes: This method should only be called when a trade is active between two players.
    The returned Player object may be nullptr if the trade window is not properly
    initialized or if the trader is not a valid player. Implementation details include
    checking that the trade window is in a valid state before calling this method.
  warnings: This method should not be used outside of trade context as it may return
    nullptr or cause undefined behavior. Avoid calling this method when no trade is
    active, as it could lead to memory access violations.
  related:
  - GetTrade
  - GetTradeWindow
  - GetTrader
metadata:
  confidence: 0.95
  generated_at: '2025-10-30T16:13:48.473894'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
