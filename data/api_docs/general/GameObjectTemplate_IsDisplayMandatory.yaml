api:
  class: GameObjectTemplate
  method: IsDisplayMandatory
  signature: bool GameObjectTemplate::IsDisplayMandatory() const
documentation:
  brief: Determines whether the game object's display is mandatory and cannot be hidden.
  description: The IsDisplayMandatory method checks if a GameObjectTemplate has a
    mandatory display that must always be shown to players, regardless of the object's
    state or visibility settings. This is typically used for objects that are essential
    to the game world's visual representation or gameplay mechanics. The method returns
    true when the display cannot be suppressed, and false when the display can be
    hidden or disabled under certain conditions.
  parameters: []
  returns: Returns true if the GameObject's display is mandatory and must always be
    visible; returns false if the display can be hidden or disabled.
  examples:
  - title: Check if GameObject Display is Mandatory
    code: "GameObjectTemplate* goTemplate = sObjectMgr->GetGameObjectTemplate(12345);\n\
      if (goTemplate && goTemplate->IsDisplayMandatory())\n{\n    // This object's\
      \ display cannot be hidden\n    sLog->outString(\"GameObject %u has mandatory\
      \ display.\", goTemplate->entry);\n}"
    language: cpp
  notes: This method is commonly used in visibility checks and rendering logic to
    ensure that essential game objects are always displayed. The implementation likely
    checks internal flags or properties set during database loading or template initialization.
  warnings: null
  related:
  - GetDisplayId
  - SetDisplayId
  - IsInvisible
metadata:
  confidence: 0.9
  generated_at: '2025-11-08T17:52:03.316277'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
