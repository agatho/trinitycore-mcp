api:
  class: Map
  method: IsBattleground
  signature: bool Map::IsBattleground() const
documentation:
  brief: Determines whether the current map instance is a battleground.
  description: This method checks if the map object represents a battleground, which
    is a special type of zone in World of Warcraft where players can engage in player-versus-player
    combat. The method returns true when the map is specifically designated as a battleground,
    and false otherwise. It is commonly used to verify game mechanics such as whether
    players are in a valid battleground area for activities like queueing, scoring,
    or combat. This check is essential for determining if a map supports battleground-specific
    gameplay features.
  parameters: []
  returns: Returns true if the map instance is a battleground, false otherwise. The
    method does not return null or error values as it simply checks a boolean flag.
  examples:
  - title: Basic Usage Example
    code: "if (map->IsBattleground()) {\n    // Handle battleground-specific logic\n\
      }\n"
    language: cpp
  - title: Advanced Usage Example
    code: "Map* map = player->GetMap();\nif (map && map->IsBattleground()) {\n   \
      \ // Perform actions specific to battlegrounds\n    // e.g., check if player\
      \ can participate in PVP activities\n}\n"
    language: cpp
  notes: This method is thread-safe and does not require any special considerations.
    It simply checks a flag that is set during map initialization, making it fast
    and efficient for frequent use.
  warnings: This method should not be used to determine if a player is in a battleground
    zone without checking the map's instance type, as it only returns true for maps
    explicitly designated as battlegrounds. Players may be in a battleground zone
    but not have the correct map instance type.
  related:
  - IsBattleground
  - IsBattlefield
  - IsInstance
  - IsDungeon
metadata:
  confidence: 0.95
  generated_at: '2025-10-31T04:04:26.552711'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
