api:
  class: Player
  method: SatisfyQuestDay
  signature: bool Player::SatisfyQuestDay(const Quest * qInfo, bool msg) const
documentation:
  brief: Satisfies a quest day requirement for the player.
  description: This method checks if the player has completed the daily quest requirements
    for the specified quest. It verifies whether the player's quest log contains the
    necessary entries to satisfy the daily quest conditions, including any prerequisites
    or completion requirements. The method returns true if the quest is satisfied
    and false otherwise, with optional messaging support.
  parameters:
  - name: qInfo
    description: Pointer to the Quest object that represents the quest being checked
      for daily requirement satisfaction.
  - name: msg
    description: Boolean flag indicating whether to send a message to the player if
      the quest is not satisfied. If true, the method will display a message to the
      player when the quest cannot be satisfied.
  returns: Returns true if the player has completed the daily quest requirements for
    the specified quest; otherwise returns false.
  examples:
  - title: Basic usage example
    code: "if (player->SatisfyQuestDay(quest, true)) {\n    // Quest is satisfied\n\
      \    // Do something\n}\nelse {\n    // Quest is not satisfied\n    // Handle\
      \ accordingly\n}"
    language: cpp
  notes: This method is typically used in conjunction with quest completion checks
    and may be called during quest handling or when evaluating player progress. It's
    important to note that this method does not directly modify the player's quest
    log but rather evaluates existing entries.
  warnings: This method should only be called when the player is actively engaged
    in quest progression, as calling it outside of proper context may lead to incorrect
    evaluation results. Also, ensure that the Quest object passed is valid and not
    null before calling this method.
  related:
  - Quest::GetQuestLevel
  - Player::GetQuestStatus
  - Player::CompleteQuest
  - Player::HasQuest
metadata:
  confidence: 0.85
  generated_at: '2025-10-30T23:59:11.856676'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
