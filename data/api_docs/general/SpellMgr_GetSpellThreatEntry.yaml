api:
  class: SpellMgr
  method: GetSpellThreatEntry
  signature: const SpellThreatEntry * SpellMgr::GetSpellThreatEntry(uint32 spellID)
    const
documentation:
  brief: Retrieves the threat entry data associated with a specific spell ID.
  description: The GetSpellThreatEntry method fetches the SpellThreatEntry structure
    that defines how a given spell affects threat generation for NPCs. This is used
    internally by the core to determine the amount of threat a spell generates when
    cast on targets, which influences NPC aggro behavior and combat mechanics. The
    method performs a lookup in an internal data structure containing pre-loaded spell
    threat configurations. It returns a pointer to the entry if found, or nullptr
    if no threat data exists for the specified spell ID.
  parameters: []
  returns: A pointer to a const SpellThreatEntry structure that contains the threat-related
    data for the specified spell, or nullptr if no such entry exists.
  examples:
  - title: Retrieve Threat Entry for a Spell
    code: "const SpellThreatEntry* entry = sSpellMgr->GetSpellThreatEntry(12345);\n\
      if (entry)\n{\n    // Use the threat data\n    uint32 threat = entry->threat;\n\
      \    // Process threat value\n}"
    language: cpp
  notes: This method is typically used during spell execution or threat calculation
    phases. The SpellThreatEntry structures are loaded at startup from database tables
    and cached for fast lookup. This method is thread-safe as it only performs read
    operations on pre-loaded data.
  warnings: null
  related:
  - GetSpellInfo
  - IsSpellThreatSpell
metadata:
  confidence: 0.95
  generated_at: '2025-11-01T06:12:03.171685'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
