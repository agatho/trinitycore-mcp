api:
  class: EvokerAI
  method: IsAtOptimalRange
  signature: bool EvokerAI::IsAtOptimalRange(int * target)
documentation:
  brief: Checks if the evoker is within optimal range of a target.
  description: The IsAtOptimalRange method determines whether the EvokerAI instance's
    current position is within the optimal distance range to its assigned target.
    This method is typically used in AI decision-making logic to decide when to cast
    spells or move closer to an enemy. The method evaluates the distance between the
    evoker and its target, comparing it against predefined optimal range thresholds.
    It returns true if the evoker is at or within the optimal range, and false otherwise.
    This functionality is crucial for proper spell casting behavior in the context
    of Evoker class mechanics.
  parameters:
  - name: target
    description: Pointer to an integer representing the target's identifier or index.
      The method uses this to retrieve the target object from the AI system's internal
      target list.
  returns: Returns true if the evoker is within the optimal range of the specified
    target, false otherwise. This boolean value indicates whether the AI should proceed
    with actions that require proximity to the target.
  examples:
  - title: Basic usage in AI decision logic
    code: "if (evokerAI->IsAtOptimalRange(&targetId)) {\n    evokerAI->CastSpell(SpellId::Fireball);\n\
      } else {\n    evokerAI->MoveTowardsTarget(targetId);\n}"
    language: cpp
  - title: Using with conditional spell casting
    code: "int target = evokerAI->GetTarget();\nif (target != 0 && evokerAI->IsAtOptimalRange(&target))\
      \ {\n    if (evokerAI->HasSpellReady(SpellId::FrostNova)) {\n        evokerAI->CastSpell(SpellId::FrostNova);\n\
      \    }\n}"
    language: cpp
  notes: This method relies on the AI system's internal target tracking and distance
    calculations. The optimal range is determined by the spell's properties and the
    evoker's class-specific mechanics. The method does not perform any movement or
    action itself, but rather evaluates conditions for other methods to act upon.
  warnings: Ensure that the target parameter points to a valid target before calling
    this method, as invalid targets may lead to undefined behavior or incorrect range
    calculations. This method assumes the AI has been properly initialized with a
    valid target.
  related:
  - GetTarget
  - MoveTowardsTarget
  - CastSpell
  - HasSpellReady
metadata:
  confidence: 0.85
  generated_at: '2025-11-07T14:06:57.901154'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
