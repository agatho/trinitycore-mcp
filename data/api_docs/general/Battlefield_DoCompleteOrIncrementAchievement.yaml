api:
  class: Battlefield
  method: DoCompleteOrIncrementAchievement
  signature: void Battlefield::DoCompleteOrIncrementAchievement(int param0, Player
    * param1, int param2)
documentation:
  brief: Updates or completes an achievement for a player in the battlefield.
  description: The DoCompleteOrIncrementAchievement method is responsible for managing
    the progression of achievements within a battlefield context. It evaluates whether
    a given achievement should be incremented based on player actions or completed
    when all criteria are met. This method typically gets invoked during battlefield
    events such as capturing objectives, defeating players, or achieving specific
    goals that contribute to achievement tracking. The method interacts with the player's
    achievement data and updates their progress accordingly, potentially awarding
    rewards or unlocking new achievements.
  parameters:
  - name: param0
    description: An integer identifier representing the achievement ID or type to
      be updated or completed.
  - name: param1
    description: A pointer to the Player object whose achievement progress is being
      modified.
  - name: param2
    description: An integer value that may represent a counter, score, or modifier
      related to the achievement criteria.
  returns: null
  examples:
  - title: Incrementing a Kill Achievement
    code: 'Battlefield* bf = sBattlefieldMgr->GetBattlefieldByInstanceId(1234);

      bf->DoCompleteOrIncrementAchievement(5678, player, 1);'
    language: cpp
  - title: Completing a Capture Achievement
    code: 'Battlefield* bf = sBattlefieldMgr->GetBattlefieldByInstanceId(5678);

      bf->DoCompleteOrIncrementAchievement(9012, player, 5);'
    language: cpp
  notes: This method is typically called internally by the battlefield system during
    event handling. It assumes that the provided Player object is valid and currently
    participating in the battlefield. The achievement tracking logic depends on the
    specific implementation of the achievement system within TrinityCore.
  warnings: Ensure that the achievement ID passed as param0 corresponds to a valid
    achievement in the database, otherwise the method may silently fail or cause unexpected
    behavior. Also, avoid calling this method with invalid player pointers which could
    lead to crashes or undefined behavior.
  related:
  - Battlefield::UpdatePlayerAchievement
  - Battlefield::CheckAchievementCriteria
  - Player::CompletedAchievement
metadata:
  confidence: 0.85
  generated_at: '2025-11-06T20:44:50.330196'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
