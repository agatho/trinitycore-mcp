api:
  class: DB2Manager
  method: GetSpellProcsPerMinuteMods
  signature: int DB2Manager::GetSpellProcsPerMinuteMods(uint32 spellprocsPerMinuteId)
    const
documentation:
  brief: Retrieves the spell procs per minute modifier value for a given ID.
  description: The GetSpellProcsPerMinuteMods method fetches the modifier value associated
    with a specific spell procs per minute ID from the database. This is used in World
    of Warcraft's combat system to determine how often certain spells or abilities
    can proc, typically affecting the frequency of effects like damage over time,
    healing, or special abilities. The returned value represents a multiplier or base
    rate that influences proc mechanics for spells defined in the SpellProcsPerMinute.db2
    file. This method is part of the DB2Manager class which handles loading and accessing
    various game data tables from the TrinityCore database.
  parameters: []
  returns: Returns an integer representing the spell procs per minute modifier value.
    A negative or zero value may indicate invalid ID or missing data, while positive
    values represent valid modifiers used in proc calculations.
  examples:
  - title: Retrieve Spell Proc Modifier
    code: "int modifier = sDB2Manager->GetSpellProcsPerMinuteMods(12345);\nif (modifier\
      \ > 0)\n{\n    // Use the modifier value in proc calculations\n    printf(\"\
      Proc modifier: %d\\n\", modifier);\n}"
    language: cpp
  notes: This method accesses data from SpellProcsPerMinute.db2 which is loaded during
    server startup. Ensure that the DB2 files are properly updated and loaded before
    calling this method to avoid incorrect or missing data returns.
  warnings: null
  related: []
metadata:
  confidence: 0.85
  generated_at: '2025-11-01T14:38:53.788332'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
