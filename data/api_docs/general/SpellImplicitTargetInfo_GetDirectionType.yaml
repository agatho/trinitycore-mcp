api:
  class: SpellImplicitTargetInfo
  method: GetDirectionType
  signature: SpellTargetDirectionTypes SpellImplicitTargetInfo::GetDirectionType()
    const
documentation:
  brief: Retrieves the direction type associated with the spell implicit target information.
  description: The GetDirectionType method returns the SpellTargetDirectionTypes enumeration
    value that defines the directional behavior for a spell's implicit target. This
    is used in spell targeting logic to determine how the spell's direction is calculated
    or interpreted, such as whether it targets in front of the caster, behind, or
    in a specific angular orientation. The returned value helps determine the spell's
    targeting mechanics and can influence the spell's effectiveness and positioning
    requirements within the game world.
  parameters: []
  returns: A SpellTargetDirectionTypes enumeration value indicating the direction
    behavior for the spell target. Possible values include types like FRONT, BACK,
    LEFT, RIGHT, or other directional specifications depending on the spell's targeting
    definition.
  examples:
  - title: Basic Usage Example
    code: "SpellImplicitTargetInfo targetInfo;\n// Assume targetInfo is initialized\
      \ with relevant data\nSpellTargetDirectionTypes directionType = targetInfo.GetDirectionType();\n\
      if (directionType == SPELL_TARGET_DIRECTION_FRONT)\n{\n    // Handle front-facing\
      \ spell targeting logic\n}"
    language: cpp
  notes: This method is typically used during spell processing and targeting calculations
    within the TrinityCore framework. It should be called after the SpellImplicitTargetInfo
    object has been properly initialized with target data. The direction type returned
    may affect how the spell's visual effects or hit detection are handled in the
    game world.
  warnings: null
  related:
  - GetTargetType
  - GetRadius
  - IsAreaTarget
metadata:
  confidence: 0.9
  generated_at: '2025-11-03T17:56:25.626768'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
