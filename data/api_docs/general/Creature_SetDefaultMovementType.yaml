api:
  class: Creature
  method: SetDefaultMovementType
  signature: void Creature::SetDefaultMovementType(MovementGeneratorType mgt)
documentation:
  brief: Sets the default movement type for a creature.
  description: This method configures the movement behavior of a creature based on
    the specified MovementGeneratorType. It determines how the creature will move
    when not under direct control, such as during combat or idle states. The method
    affects the creature's AI behavior and movement patterns within the game world.
    This is typically used during creature initialization or when changing movement
    types for specific scenarios.
  parameters:
  - name: mgt
    description: The MovementGeneratorType value that defines how the creature should
      move. Valid values include IDLE, RANDOM, DISTRIButed, and others depending on
      the game mechanics.
  returns: null
  examples:
  - title: Setting idle movement type
    code: 'Creature* creature = new Creature();

      creature->SetDefaultMovementType(IDLE);

      // Creature will now use idle movement when not in combat'
    language: cpp
  notes: This method is typically called during creature initialization or when modifying
    movement behavior. It affects the creature's AI state and can influence how it
    moves through the world. The method should be used carefully as it may impact
    performance if applied incorrectly.
  warnings: Improper use of this method could lead to unexpected creature behavior,
    including movement issues or AI malfunctions. Care should be taken when setting
    movement types for creatures that are already in combat or active.
  related:
  - GetDefaultMovementType
  - SetMovementGenerator
metadata:
  confidence: 0.9
  generated_at: '2025-10-30T18:48:04.895893'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
