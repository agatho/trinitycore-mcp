api:
  class: VendorInteraction
  method: NeedsItem
  signature: bool VendorInteraction::NeedsItem(Player * bot, uint32 itemId) const
documentation:
  brief: Checks if a player needs a specific item from the vendor interaction.
  description: The NeedsItem method determines whether a given player requires a specific
    item within the context of a vendor interaction. This typically involves checking
    the player's inventory, quest requirements, or other conditions that might affect
    whether they need to purchase or acquire the specified item. The method is commonly
    used in AI-driven bot behavior to make decisions about which items to buy or sell
    during vendor interactions.
  parameters:
  - name: bot
    description: Pointer to the Player object representing the bot that is being evaluated
      for needing the item.
  - name: itemId
    description: The unique identifier of the item being checked for necessity. This
      corresponds to the Item's entry ID in the database.
  returns: Returns true if the player needs the specified item, false otherwise. The
    decision may be based on inventory state, quest status, or other game logic.
  examples:
  - title: Basic usage to check if bot needs an item
    code: "if (vendorInteraction.NeedsItem(bot, 12345)) {\n    // Bot needs item with\
      \ ID 12345\n    bot->BuyItemFromVendor(12345);\n}"
    language: cpp
  - title: Using NeedsItem in a conditional vendor interaction
    code: "uint32 itemId = 67890;\nif (vendorInteraction.NeedsItem(bot, itemId)) {\n\
      \    // Perform logic to acquire the item\n    if (bot->HasItemCount(itemId,\
      \ 1)) {\n        // Already has the item\n        return;\n    }\n    bot->BuyItemFromVendor(itemId);\n\
      }"
    language: cpp
  notes: This method likely interacts with internal vendor data structures and may
    involve checking multiple conditions such as quest dependencies, item availability,
    and player inventory state. It is designed to be used in AI decision-making processes
    where bots interact with vendors.
  warnings: Ensure that the Player pointer is valid before calling this method, as
    passing a null pointer could lead to undefined behavior. Also, make sure the itemId
    corresponds to an actual item in the game database.
  related:
  - VendorInteraction::CanBuyItem
  - VendorInteraction::GetItemCost
  - Player::HasItemCount
metadata:
  confidence: 0.85
  generated_at: '2025-11-08T20:54:20.816314'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
