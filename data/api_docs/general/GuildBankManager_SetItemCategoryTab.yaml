api:
  class: GuildBankManager
  method: SetItemCategoryTab
  signature: void GuildBankManager::SetItemCategoryTab(uint32 guildId, GuildBankItemType
    itemType, uint32 tabId)
documentation:
  brief: Sets the tab ID for a specific item type in the guild bank.
  description: The SetItemCategoryTab method assigns a tab identifier to a specific
    guild bank item type, determining which tab an item will appear in within the
    guild bank interface. This method is typically used during guild bank configuration
    or when modifying item placement rules. The operation affects the visual organization
    of items within the guild bank and may trigger UI updates for guild members. It
    requires valid guild ID, item type, and tab ID parameters to execute properly.
  parameters:
  - name: guildId
    description: Unique identifier of the guild whose bank tab configuration is being
      modified. Must correspond to an existing guild in the database.
  - name: itemType
    description: The type of guild bank item to assign to a tab. This value typically
      corresponds to predefined item categories or types within the game's item system.
  - name: tabId
    description: The tab identifier that the specified item type should be associated
      with. Must be a valid tab ID for the given guild, usually ranging from 0 to
      the maximum number of tabs configured for the guild.
  returns: null
  examples:
  - title: Assigning a weapon category to tab 2
    code: GuildBankManager::SetItemCategoryTab(12345, GUILD_BANK_ITEM_TYPE_WEAPON,
      2);
    language: cpp
  - title: Configuring multiple item types for different tabs
    code: 'GuildBankManager::SetItemCategoryTab(12345, GUILD_BANK_ITEM_TYPE_ARMOR,
      1);

      GuildBankManager::SetItemCategoryTab(12345, GUILD_BANK_ITEM_TYPE_CONSUMABLE,
      3);

      GuildBankManager::SetItemCategoryTab(12345, GUILD_BANK_ITEM_TYPE_TRADE_GOODS,
      4);'
    language: cpp
  notes: This method likely updates internal data structures within the GuildBankManager
    class to reflect new item-to-tab mappings. Changes made using this function may
    not be immediately visible to all guild members until a UI refresh or reload occurs.
    The implementation should validate that the provided tab ID exists for the specified
    guild before applying changes.
  warnings: Calling this method with invalid parameters such as a non-existent guild
    ID, an invalid item type, or a tab ID outside the valid range may result in undefined
    behavior or database corruption. Ensure all inputs are validated prior to calling
    this function.
  related:
  - GetItemCategoryTab
  - GetGuildBankTabInfo
  - UpdateGuildBankTab
metadata:
  confidence: 0.85
  generated_at: '2025-11-01T13:02:37.062177'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
