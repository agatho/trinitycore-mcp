api:
  class: GameObjectTemplate
  method: GetAutoCloseTime
  signature: uint32 GameObjectTemplate::GetAutoCloseTime() const
documentation:
  brief: Returns the auto-close time value for the game object template.
  description: The GetAutoCloseTime method retrieves the time interval in seconds
    after which a game object will automatically close. This is commonly used for
    doors, chests, and other interactive objects that should close or reset after
    being activated. The returned value represents the duration before automatic closure
    occurs, with a value of zero indicating no auto-close behavior. This method is
    typically called during object activation or when determining the object's interaction
    properties.
  parameters: []
  returns: A uint32 value representing the auto-close time in seconds. A return value
    of 0 indicates that the game object does not have automatic closing behavior enabled.
  examples:
  - title: Basic Usage to Check Auto-Close Time
    code: "GameObjectTemplate* goTemplate = sObjectMgr->GetGameObjectTemplate(12345);\n\
      uint32 autoCloseTime = goTemplate->GetAutoCloseTime();\nif (autoCloseTime >\
      \ 0)\n{\n    printf(\"This object will auto-close after %u seconds\\n\", autoCloseTime);\n\
      }\nelse\n{\n    printf(\"This object has no auto-close behavior\\n\");\n}"
    language: cpp
  - title: Using Auto-Close Time in Object Activation Logic
    code: "void HandleGameObjectActivation(Player* player, GameObject* go)\n{\n  \
      \  GameObjectTemplate const* goInfo = go->GetGOInfo();\n    uint32 autoCloseTime\
      \ = goInfo->GetAutoCloseTime();\n    \n    if (autoCloseTime > 0)\n    {\n \
      \       // Schedule auto-close behavior\n        go->SetRespawnTime(autoCloseTime);\n\
      \        go->SetLootState(GO_JUST_DEACTIVATED);\n    }\n}"
    language: cpp
  notes: This method accesses a cached value from the GameObjectTemplate's internal
    data structure. The auto-close time is typically defined in the game object's
    database entry and is used to control the timing of automatic state transitions
    for interactive objects.
  warnings: null
  related:
  - GetScriptName
  - GetGOInfo
  - GetDisplayId
metadata:
  confidence: 0.95
  generated_at: '2025-11-08T17:59:49.188128'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
