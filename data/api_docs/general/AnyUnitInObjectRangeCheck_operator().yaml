api:
  class: AnyUnitInObjectRangeCheck
  method: operator()
  signature: bool AnyUnitInObjectRangeCheck::operator()(int * u) const
documentation:
  brief: Checks if a unit is within object range and returns true if found.
  description: The AnyUnitInObjectRangeCheck::operator() method evaluates whether
    a given unit (represented by an integer pointer) is within the specified object
    range. This method is typically used in TrinityCore's AI and targeting systems
    to determine if units are close enough to be considered for actions or interactions.
    It serves as a predicate function that can be used with STL algorithms or custom
    iterators to filter units based on their proximity to a reference object. The
    method returns true when a unit is found within range, and false otherwise.
  parameters: []
  returns: Returns true if a unit is found within the specified object range; otherwise
    returns false.
  examples:
  - title: Basic usage in AI targeting
    code: "AnyUnitInObjectRangeCheck checker(10.0f); // 10 yard range\nif (checker(&unitGuid))\
      \ {\n    // Unit is within range\n}"
    language: cpp
  notes: This method is commonly used in AI logic for determining valid targets for
    spells or abilities. The implementation likely uses the object's position and
    the specified range to perform a distance check.
  warnings: Ensure that the integer pointer passed to this method points to a valid
    unit GUID; otherwise behavior may be undefined.
  related:
  - AnyUnitInObjectRangeCheck::AnyUnitInObjectRangeCheck
  - AnyUnitInObjectRangeCheck::operator bool
metadata:
  confidence: 0.85
  generated_at: '2025-11-09T04:16:37.089434'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
