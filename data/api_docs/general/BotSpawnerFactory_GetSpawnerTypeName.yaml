api:
  class: BotSpawnerFactory
  method: GetSpawnerTypeName
  signature: int BotSpawnerFactory::GetSpawnerTypeName(SpawnerType type)
documentation:
  brief: Returns the string name of a spawner type given its enumerated value.
  description: The GetSpawnerTypeName method retrieves the human-readable string identifier
    associated with a specific SpawnerType enumeration value. This function is typically
    used within the TrinityCore bot system to map internal spawner type identifiers
    to their corresponding names for logging, debugging, or user interface purposes.
    The method takes a single SpawnerType parameter and returns an integer representing
    the index or identifier of the name string in an internal lookup table. It's important
    to note that this function assumes valid input values and does not perform bounds
    checking on the provided SpawnerType.
  parameters:
  - name: type
    description: The SpawnerType enumeration value for which to retrieve the corresponding
      name string identifier
  returns: An integer representing the index or identifier of the spawner type name
    in an internal lookup table. Returns -1 if the provided SpawnerType is invalid
    or not recognized.
  examples:
  - title: Basic Usage Example
    code: 'int spawnerTypeNameId = botSpawnerFactory->GetSpawnerTypeName(SPAWNER_TYPE_NPC);

      // Returns the identifier for the NPC spawner type name'
    language: cpp
  - title: Using Returned Identifier to Get Actual Name
    code: 'int typeNameId = botSpawnerFactory->GetSpawnerTypeName(SPAWNER_TYPE_ITEM);

      std::string name = GetSpawnerTypeNameById(typeNameId);

      // Uses the returned identifier to fetch the actual string name'
    language: cpp
  notes: This method is part of a factory pattern implementation for bot spawners
    in TrinityCore. The internal lookup table mapping SpawnerType values to their
    string representations should be initialized at system startup. Performance is
    generally good as it's a simple lookup operation, but repeated calls with invalid
    parameters may cause unnecessary overhead.
  warnings: Ensure that the SpawnerType parameter passed to this method is valid and
    within expected range, as invalid values might return unexpected results or -1.
    This function should not be used for validation of spawner types; it's purely
    a mapping utility.
  related:
  - GetSpawnerTypeByName
  - CreateSpawner
  - GetAllSpawnerTypes
metadata:
  confidence: 0.85
  generated_at: '2025-11-02T10:40:16.926517'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
