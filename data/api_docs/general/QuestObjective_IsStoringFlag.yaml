api:
  class: QuestObjective
  method: IsStoringFlag
  signature: bool QuestObjective::IsStoringFlag() const
documentation:
  brief: Determines whether the quest objective is currently storing a flag state.
  description: The IsStoringFlag method checks if the QuestObjective instance has
    a flag state that is being actively stored or tracked. This typically relates
    to objectives that involve capturing, holding, or managing flags in World of Warcraft's
    quest system. The method returns true when the objective is in a state where it
    maintains a flag-related status, and false otherwise. This functionality is commonly
    used in battlegrounds or scenario-based quests where flag capture mechanics are
    involved.
  parameters: []
  returns: Returns true if the quest objective is currently storing a flag state;
    otherwise returns false.
  examples:
  - title: Check if Quest Objective Stores Flag
    code: "if (questObjective->IsStoringFlag()) {\n    // Handle logic for when flag\
      \ is being stored\n    printf(\"Flag is currently being stored by this objective.\\\
      n\");\n} else {\n    // Handle logic for when no flag is stored\n    printf(\"\
      No flag is currently being stored by this objective.\\n\");\n}"
    language: cpp
  notes: This method is likely used in conjunction with other quest objective tracking
    mechanisms to manage complex flag-based objectives such as those found in battlegrounds
    like Warsong Gulch or Arathi Basin.
  warnings: null
  related:
  - GetFlagState
  - SetFlagState
  - IsCompleted
metadata:
  confidence: 0.85
  generated_at: '2025-11-08T15:23:50.983447'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
