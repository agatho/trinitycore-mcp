api:
  class: BotWorldSessionMgrOptimized
  method: HasBotSession
  signature: bool BotWorldSessionMgrOptimized::HasBotSession(int playerGuid) const
documentation:
  brief: Checks whether a bot session exists for the specified player GUID.
  description: The HasBotSession method determines if a bot session is currently active
    for the given player identifier. This functionality is essential in TrinityCore's
    bot management system, where each bot session is associated with a specific player
    GUID. The method performs a lookup within the internal session manager to verify
    the existence of a corresponding bot session. It is commonly used before attempting
    to interact with or manipulate a bot session to prevent errors from accessing
    non-existent sessions.
  parameters:
  - name: playerGuid
    description: The unique identifier (GUID) of the player whose bot session status
      is being checked. This should be a valid player GUID within the TrinityCore
      database.
  returns: Returns true if a bot session exists for the specified player GUID; otherwise,
    returns false.
  examples:
  - title: Basic usage to check if a bot session exists
    code: "if (botWorldSessionMgr->HasBotSession(12345)) {\n    // Bot session exists,\
      \ proceed with bot operations\n    std::cout << \"Bot session found for player\
      \ 12345\" << std::endl;\n} else {\n    // No bot session found\n    std::cout\
      \ << \"No bot session for player 12345\" << std::endl;\n}"
    language: cpp
  - title: Using in a conditional context to initialize bot session
    code: "uint32 playerId = 67890;\nif (!botWorldSessionMgr->HasBotSession(playerId))\
      \ {\n    // Initialize new bot session for this player\n    BotSession* newSession\
      \ = new BotSession(playerId);\n    botWorldSessionMgr->AddBotSession(newSession);\n\
      \    std::cout << \"New bot session created for player \" << playerId << std::endl;\n\
      } else {\n    std::cout << \"Bot session already exists for player \" << playerId\
      \ << std::endl;\n}"
    language: cpp
  notes: This method performs a lookup operation in the internal session manager,
    which may involve locking mechanisms depending on implementation. For high-frequency
    checks, consider caching results or using thread-safe access patterns to avoid
    performance bottlenecks.
  warnings: null
  related:
  - AddBotSession
  - RemoveBotSession
  - GetBotSession
metadata:
  confidence: 0.9
  generated_at: '2025-11-07T15:08:16.890475'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
