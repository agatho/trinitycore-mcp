api:
  class: ShamanAI
  method: HandleAoEDecisions
  signature: bool ShamanAI::HandleAoEDecisions(int * target)
documentation:
  brief: Evaluates and selects optimal area-of-effect spell targets for the shaman
    AI.
  description: The HandleAoEDecisions method is responsible for determining the most
    effective targets for area-of-effect spells within the ShamanAI class. This method
    analyzes the current combat situation, including enemy positions, health states,
    and spell range limitations, to select an appropriate target or set of targets
    for AoE abilities such as Chain Lightning, Healing Rain, or Earthquake. It considers
    factors like threat levels, proximity to other enemies, and the shaman's current
    mana state to make intelligent decisions about when and where to cast these spells.
    The method returns true if a valid target was identified and processed, false
    otherwise. This functionality is crucial for maintaining optimal spell usage and
    maximizing damage or healing output in group combat scenarios.
  parameters: []
  returns: Returns true if a suitable area-of-effect target was identified and the
    decision process completed successfully; returns false if no appropriate targets
    were found or if the decision-making process failed.
  examples:
  - title: Basic usage in AI update loop
    code: "void ShamanAI::UpdateAI(const uint32 diff)\n{\n    // ... other AI logic\
      \ ...\n    int target = 0;\n    if (HandleAoEDecisions(&target))\n    {\n  \
      \      // Proceed with casting AoE spell\n        DoCast(target, SPELL_CHAIN_LIGHTNING);\n\
      \    }\n    // ... rest of AI logic ...\n}"
    language: cpp
  notes: This method is typically called during the AI update cycle and integrates
    with the shaman's overall decision-making framework. It assumes that the shaman
    has access to a valid list of potential targets and that the spell system is properly
    initialized. The method may modify the target parameter to reflect the chosen
    AoE target, which should be used for subsequent spell casting operations.
  warnings: This method relies on accurate target selection logic and may fail if
    the AI's target list is empty or improperly maintained. Developers should ensure
    that all relevant combat state information (such as threat levels and enemy positions)
    is properly updated before calling this method to avoid incorrect targeting decisions.
  related:
  - ShamanAI::HandleSingleTargetDecisions
  - ShamanAI::GetSpellTargets
  - ShamanAI::CanCastSpell
metadata:
  confidence: 0.85
  generated_at: '2025-11-07T19:58:07.818326'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
