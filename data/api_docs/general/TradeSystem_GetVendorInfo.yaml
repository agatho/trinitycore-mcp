api:
  class: TradeSystem
  method: GetVendorInfo
  signature: VendorInfo TradeSystem::GetVendorInfo(uint32 creatureGuid)
documentation:
  brief: Retrieves vendor information for a specified creature GUID from the trade
    system.
  description: The GetVendorInfo method fetches detailed vendor data associated with
    a given creature GUID within the TrinityCore framework. This function is typically
    used to access item listings, pricing structures, and vendor behavior configurations
    for NPCs that act as vendors in World of Warcraft. The returned VendorInfo object
    contains all relevant data needed to display or interact with the vendor's inventory.
    The method assumes that the creature GUID corresponds to a valid vendor NPC within
    the game world and will return appropriate data if found.
  parameters:
  - name: creatureGuid
    description: The unique identifier (GUID) of the creature NPC acting as a vendor.
      This value must correspond to an existing vendor in the database, otherwise
      the method may return invalid or empty vendor information.
  returns: A VendorInfo object containing all relevant data about the specified vendor,
    including item listings, prices, and vendor-specific properties. Returns an empty
    or default-initialized VendorInfo if no valid vendor is found for the given GUID.
  examples:
  - title: Basic Usage Example
    code: "uint32 vendorGuid = 12345;\nVendorInfo vendorData = tradeSystem.GetVendorInfo(vendorGuid);\n\
      if (!vendorData.IsEmpty()) {\n    // Process vendor data\n}"
    language: cpp
  - title: Checking Vendor Validity
    code: "uint32 npcGuid = player->GetTarget();\nVendorInfo info = tradeSystem.GetVendorInfo(npcGuid);\n\
      if (info.IsValid()) {\n    // Proceed with vendor interaction\n    for (auto&\
      \ item : info.GetItems()) {\n        // Display item details\n    }\n} else\
      \ {\n    // Handle invalid vendor case\n}"
    language: cpp
  notes: This method performs a lookup in the internal vendor database using the provided
    GUID. It is recommended to validate that the creature GUID corresponds to an actual
    vendor before calling this method to avoid unnecessary overhead. The returned
    VendorInfo object should be checked for validity before accessing its contents.
  warnings: Calling this method with an invalid or non-existent creature GUID may
    result in undefined behavior or return empty data structures. Ensure proper validation
    of input parameters and check the returned VendorInfo's validity before using
    its contents.
  related:
  - GetVendorItems
  - IsVendorValid
  - UpdateVendorInfo
metadata:
  confidence: 0.9
  generated_at: '2025-11-03T16:07:55.527722'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
