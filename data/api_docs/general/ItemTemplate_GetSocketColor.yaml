api:
  class: ItemTemplate
  method: GetSocketColor
  signature: SocketColor ItemTemplate::GetSocketColor(uint32 index) const
documentation:
  brief: Retrieves the socket color of an item at the specified index.
  description: The GetSocketColor method retrieves the color of a gem socket on an
    item template. This method is used to determine what type of gem can be inserted
    into a specific socket slot. The returned SocketColor value indicates whether
    the socket accepts red, yellow, blue, or any type of gem. This functionality is
    essential for implementing proper socketing mechanics in World of Warcraft-style
    games where items have colored sockets that accept specific gem types.
  parameters: []
  returns: Returns a SocketColor enum value indicating the type of gem that can be
    inserted into the socket at the specified index. Possible values include SOCKET_COLOR_RED,
    SOCKET_COLOR_YELLOW, SOCKET_COLOR_BLUE, and SOCKET_COLOR_ANY.
  examples:
  - title: Check Socket Color for First Socket
    code: "ItemTemplate* itemTemplate = sObjectMgr->GetItemTemplate(12345);\nSocketColor\
      \ color = itemTemplate->GetSocketColor(0);\nif (color == SOCKET_COLOR_RED)\n\
      {\n    // Handle red socket logic\n}"
    language: cpp
  notes: This method assumes that the item template has been properly loaded and initialized.
    The index parameter should be validated to ensure it does not exceed the number
    of available sockets in the item. Implementation may involve accessing internal
    data structures that store socket information for each item template.
  warnings: Ensure that the item template exists before calling this method, as accessing
    a non-existent template could lead to undefined behavior. The index parameter
    must be within valid bounds (typically 0 to number_of_sockets - 1) to avoid memory
    access violations.
  related:
  - GetSocketCount
  - SetSocketColor
metadata:
  confidence: 0.9
  generated_at: '2025-11-04T02:16:36.053708'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
