api:
  class: ItemTemplate
  method: GetContainerSlots
  signature: uint32 ItemTemplate::GetContainerSlots() const
documentation:
  brief: Returns the number of slots available in a container item.
  description: The GetContainerSlots method retrieves the total number of slots that
    a container item can hold. This value is determined by the item's template data
    and represents how many items can be stored within the container. For example,
    a 16-slot bag will return 16, while a 32-slot backpack will return 32. The method
    is commonly used in inventory management systems to determine container capacity
    and validate item placement logic.
  parameters: []
  returns: A uint32 value representing the number of slots available in the container
    item. This value is typically between 1 and 32, depending on the container type.
  examples:
  - title: Basic Usage
    code: 'ItemTemplate* itemTemplate = sObjectMgr->GetItemTemplate(12345);

      uint32 slots = itemTemplate->GetContainerSlots();

      printf("Container has %u slots\n", slots);'
    language: cpp
  - title: Inventory Capacity Check
    code: "ItemTemplate* bagTemplate = sObjectMgr->GetItemTemplate(1000);\nuint32\
      \ capacity = bagTemplate->GetContainerSlots();\nif (capacity > 0)\n{\n    printf(\"\
      This is a container with %u slots\\n\", capacity);\n}"
    language: cpp
  notes: The method returns cached data from the item template, so it's safe to call
    multiple times without performance concerns. The returned value corresponds directly
    to the 'ContainerSlots' field in the item_template database table.
  warnings: null
  related:
  - GetMaxStackCount
  - GetItemLevel
  - GetClass
  - GetSubClass
metadata:
  confidence: 0.95
  generated_at: '2025-11-04T02:34:27.285834'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
