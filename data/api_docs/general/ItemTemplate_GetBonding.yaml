api:
  class: ItemTemplate
  method: GetBonding
  signature: ItemBondingType ItemTemplate::GetBonding() const
documentation:
  brief: Returns the bonding type of the item template, determining how the item behaves
    when equipped or picked up.
  description: The GetBonding method retrieves the ItemBondingType enum value that
    defines how an item interacts with players and the game world. This includes whether
    the item is soulbound, bound to account, or has no binding at all. The bonding
    type affects gameplay mechanics such as whether an item can be traded, mailed,
    or dropped on death. This method is commonly used in inventory management, item
    interaction logic, and crafting systems where binding behavior is relevant.
  parameters: []
  returns: Returns an ItemBondingType enum value indicating the item's binding behavior.
    Possible values include NO_BIND (0), BIND_WHEN_EQUIPED (1), BIND_WHEN_USE (2),
    BIND_ON_ACQUIRE (3), and BIND_QUEST_ITEM (4).
  examples:
  - title: Check if an item is soulbound
    code: "ItemTemplate* itemTemplate = sObjectMgr->GetItemTemplate(12345);\nif (itemTemplate->GetBonding()\
      \ == BIND_WHEN_EQUIPED)\n{\n    // Item is soulbound when equipped\n    printf(\"\
      This item is soulbound when equipped.\\n\");\n}"
    language: cpp
  - title: Determine binding behavior for item processing
    code: "ItemTemplate* template = sObjectMgr->GetItemTemplate(itemId);\nswitch (template->GetBonding())\n\
      {\n    case NO_BIND:\n        // Item can be freely traded\n        break;\n\
      \    case BIND_WHEN_EQUIPED:\n        // Item is soulbound when equipped\n \
      \       break;\n    case BIND_ON_ACQUIRE:\n        // Item is bound immediately\
      \ upon acquisition\n        break;\n    default:\n        break;\n}"
    language: cpp
  notes: This method accesses cached data from the item template database and does
    not perform any expensive operations. The returned value directly corresponds
    to the 'bonding' field in the game's item template database table.
  warnings: null
  related:
  - GetItemId
  - GetQuality
  - GetClass
  - GetSubClass
metadata:
  confidence: 0.95
  generated_at: '2025-11-04T02:36:35.840857'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
