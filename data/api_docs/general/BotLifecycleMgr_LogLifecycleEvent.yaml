api:
  class: BotLifecycleMgr
  method: LogLifecycleEvent
  signature: void BotLifecycleMgr::LogLifecycleEvent(const LifecycleEventInfo & eventInfo)
documentation:
  brief: Logs a bot lifecycle event with detailed information about the event.
  description: The LogLifecycleEvent method records bot lifecycle events within the
    TrinityCore framework, capturing essential details such as event type, timestamp,
    and associated bot data. This method is typically invoked during key stages of
    a bot's existence, including creation, destruction, or state transitions. The
    logged information helps in debugging, monitoring, and analyzing bot behavior
    patterns. It ensures that all lifecycle activities are tracked for administrative
    and analytical purposes.
  parameters:
  - name: eventInfo
    description: A constant reference to a LifecycleEventInfo structure containing
      the details of the event to be logged, including event type, timestamp, and
      related bot identifiers.
  returns: null
  examples:
  - title: Basic Usage Example
    code: 'LifecycleEventInfo event;

      event.type = BOT_EVENT_CREATED;

      event.timestamp = time(nullptr);

      event.botGuid = 12345;

      botLifecycleMgr.LogLifecycleEvent(event);'
    language: cpp
  notes: This method is designed to be thread-safe and should not block execution.
    It uses internal logging mechanisms that may be configurable via the TrinityCore
    configuration files.
  warnings: Ensure that the LifecycleEventInfo structure passed to this method contains
    valid data; invalid or uninitialized fields might lead to incorrect log entries
    or runtime errors.
  related:
  - GetBotLifecycleEvents
  - ClearBotLifecycleLogs
metadata:
  confidence: 0.9
  generated_at: '2025-11-07T05:38:34.284712'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
