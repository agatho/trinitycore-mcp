api:
  class: WorldSession
  method: HandleAuctionSetFavoriteItem
  signature: void WorldSession::HandleAuctionSetFavoriteItem(WorldPackets::AuctionHouse::AuctionSetFavoriteItem
    & setFavoriteItem)
documentation:
  brief: Sets or unsets an auction item as favorite in the player's auction house
    interface.
  description: The HandleAuctionSetFavoriteItem method processes a client request
    to mark an auction item as a favorite or remove that designation. This functionality
    allows players to quickly identify and revisit items they are interested in purchasing
    or monitoring. The method typically receives a packet containing the item's GUID
    and a boolean flag indicating whether to set or unset the favorite status. It
    updates the player's local favorites list and may synchronize this change with
    the server-side auction house data. This method is part of the auction house system
    and interacts with the WorldSession's packet handling mechanism to process client
    commands.
  parameters:
  - name: setFavoriteItem
    description: A reference to a WorldPackets::AuctionHouse::AuctionSetFavoriteItem
      packet containing the item GUID and favorite status flag.
  returns: null
  examples:
  - title: Setting an Item as Favorite
    code: "void WorldSession::HandleAuctionSetFavoriteItem(WorldPackets::AuctionHouse::AuctionSetFavoriteItem\
      \ & setFavoriteItem)\n{\n    // Process the packet to mark item as favorite\n\
      \    if (setFavoriteItem.IsFavorite)\n        _player->GetAuctionHouseMgr()->AddFavoriteItem(setFavoriteItem.ItemGUID);\n\
      \    else\n        _player->GetAuctionHouseMgr()->RemoveFavoriteItem(setFavoriteItem.ItemGUID);\n\
      }"
    language: cpp
  notes: This method is typically invoked from the packet handling system when a client
    sends an auction house favorite update. It should be thread-safe as it operates
    within the session's context, and may trigger UI updates on the client side to
    reflect the new favorite status.
  warnings: null
  related:
  - HandleAuctionListItems
  - HandleAuctionPlaceBid
  - HandleAuctionSellItem
metadata:
  confidence: 0.9
  generated_at: '2025-11-02T19:47:15.686743'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
