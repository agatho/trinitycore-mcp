api:
  class: MovementPriorityMapper
  method: GetCategoryName
  signature: std::string MovementPriorityMapper::GetCategoryName(PlayerBotMovementPriority
    priority)
documentation:
  brief: Retrieves the human-readable name of a movement priority category.
  description: The GetCategoryName method maps a PlayerBotMovementPriority enumeration
    value to its corresponding string representation. This allows developers to obtain
    descriptive names for different movement priorities used in bot behavior logic,
    which is essential for debugging, logging, and user-facing displays. The method
    provides a consistent way to translate internal priority codes into meaningful
    labels that reflect their purpose within the game's AI system. It assumes valid
    input values from the PlayerBotMovementPriority enum and returns an empty string
    for invalid or undefined priorities.
  parameters:
  - name: priority
    description: The PlayerBotMovementPriority enumeration value to convert into a
      category name. Valid values are typically defined within the PlayerBotMovementPriority
      enum, representing different movement behaviors such as 'Idle', 'Combat', 'Follow',
      etc.
  returns: A std::string containing the human-readable name of the specified movement
    priority category. Returns an empty string if the provided priority value is not
    recognized or valid.
  examples:
  - title: Basic Usage Example
    code: 'PlayerBotMovementPriority priority = PlayerBotMovementPriority::Combat;

      std::string categoryName = MovementPriorityMapper::GetCategoryName(priority);

      // categoryName will contain "Combat" or similar descriptive text'
    language: cpp
  - title: Logging Priority Names
    code: "for (int i = 0; i < 5; ++i)\n{\n    PlayerBotMovementPriority priority\
      \ = static_cast<PlayerBotMovementPriority>(i);\n    std::string name = MovementPriorityMapper::GetCategoryName(priority);\n\
      \    if (!name.empty())\n        LOG_DEBUG(\"bot\", \"Priority {} has name:\
      \ {}\", i, name);\n}"
    language: cpp
  notes: This method likely uses an internal mapping table or switch statement to
    translate enumeration values into strings. It is designed for performance-critical
    environments where string lookups are expected to be fast and reliable. The implementation
    should handle all valid enum values gracefully, though undefined values may return
    empty strings.
  warnings: Ensure that the input priority value is from the correct PlayerBotMovementPriority
    enum; passing invalid or out-of-range values may result in an empty string being
    returned without error indication.
  related:
  - GetCategoryName
  - GetPriorityFromName
metadata:
  confidence: 0.9
  generated_at: '2025-11-03T23:23:02.650403'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
