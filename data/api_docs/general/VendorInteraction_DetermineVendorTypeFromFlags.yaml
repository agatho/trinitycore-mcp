api:
  class: VendorInteraction
  method: DetermineVendorTypeFromFlags
  signature: VendorType VendorInteraction::DetermineVendorTypeFromFlags(uint32 npcFlags)
documentation:
  brief: Determines the vendor type based on NPC flags for interaction handling.
  description: The DetermineVendorTypeFromFlags method analyzes the provided NPC flags
    to identify and return the appropriate VendorType enum value. This function is
    used within the TrinityCore framework to categorize NPCs as different types of
    vendors, such as normal merchants, food vendors, or weapon sellers. The method
    evaluates specific bit flags set in the npcFlags parameter to determine which
    vendor type applies. It serves as a core utility for routing NPC interactions
    and determining what items or services are offered by an NPC.
  parameters:
  - name: npcFlags
    description: A 32-bit unsigned integer containing NPC flags that define the vendor's
      behavior and category. These flags are typically retrieved from the creature's
      database entry or runtime data.
  returns: Returns a VendorType enum value indicating the specific type of vendor
    represented by the NPC. Possible return values may include VENDOR_TYPE_NORMAL,
    VENDOR_TYPE_FOOD, VENDOR_TYPE_WEAPON, etc., depending on how the flags are interpreted.
  examples:
  - title: Basic Usage
    code: "VendorType type = vendorInteraction.DetermineVendorTypeFromFlags(0x00000010);\
      \ // Example with specific NPC flags\nif (type == VENDOR_TYPE_FOOD)\n{\n   \
      \ // Handle food vendor logic\n}"
    language: cpp
  - title: Integration in Vendor Interaction Logic
    code: "uint32 npcFlags = creature->GetUInt32Value(UNIT_NPC_FLAGS);\nVendorType\
      \ vendorType = DetermineVendorTypeFromFlags(npcFlags);\nswitch (vendorType)\n\
      {\n    case VENDOR_TYPE_NORMAL:\n        // Normal merchant logic\n        break;\n\
      \    case VENDOR_TYPE_FOOD:\n        // Food vendor specific handling\n    \
      \    break;\n    default:\n        // Default vendor behavior\n        break;\n\
      }"
    language: cpp
  notes: This method is typically used in conjunction with NPC interaction systems
    to determine appropriate vendor behaviors. The exact mapping of npcFlags to VendorType
    values may vary depending on the version of TrinityCore and custom modifications.
    It's recommended to verify flag definitions against current core documentation
    or source code.
  warnings: Ensure that npcFlags parameter is properly validated before calling this
    method, as invalid or unexpected flag combinations might result in undefined behavior
    or incorrect vendor type determination.
  related:
  - GetVendorType
  - SetVendorType
  - IsVendorType
metadata:
  confidence: 0.9
  generated_at: '2025-11-07T17:47:00.315370'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
