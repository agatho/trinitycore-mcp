api:
  class: BotWorldPositioner
  method: GetCapitalCity
  signature: ZoneChoice BotWorldPositioner::GetCapitalCity(int faction)
documentation:
  brief: Returns the capital city zone choice for the specified faction
  description: The GetCapitalCity method retrieves the primary capital city zone information
    for a given faction in the World of Warcraft game. This method is typically used
    by bot AI systems to determine appropriate starting locations or target zones
    based on faction alignment. The returned ZoneChoice object contains data about
    the geographical location, map ID, and other relevant properties of the faction's
    capital city. This functionality is essential for bots that need to navigate or
    position themselves strategically within the game world based on faction-based
    geography.
  parameters:
  - name: faction
    description: The faction identifier (Alliance=0, Horde=1) for which to retrieve
      the capital city information
  returns: A ZoneChoice object containing the capital city's zone data including map
    ID, position coordinates, and other relevant geographical properties. Returns
    an invalid ZoneChoice if the specified faction is not recognized or supported.
  examples:
  - title: Retrieve Alliance Capital City
    code: "BotWorldPositioner positioner;\nZoneChoice allianceCapital = positioner.GetCapitalCity(0);\n\
      if (allianceCapital.IsValid()) {\n    // Use the capital city data for bot positioning\n\
      \    std::cout << \"Alliance capital map ID: \" << allianceCapital.mapId <<\
      \ std::endl;\n}"
    language: cpp
  - title: Retrieve Horde Capital City with Error Handling
    code: "BotWorldPositioner positioner;\nZoneChoice hordeCapital = positioner.GetCapitalCity(1);\n\
      if (!hordeCapital.IsValid()) {\n    // Handle case where capital city data is\
      \ not available\n    LOG_ERROR(\"world\", \"Failed to retrieve Horde capital\
      \ city data\");\n} else {\n    // Proceed with using the retrieved capital city\
      \ information\n    Position pos = hordeCapital.GetPosition();\n    // Use position\
      \ for bot navigation\n}"
    language: cpp
  notes: This method likely caches faction capital city data to improve performance.
    The ZoneChoice object returned should not be modified directly as it may be shared
    across multiple calls. Implementation details depend on the internal database
    or configuration files that define faction capital cities in the TrinityCore instance.
  warnings: Calling this method with an invalid faction ID (not 0 or 1) may result
    in undefined behavior or return invalid ZoneChoice objects. Always validate the
    returned ZoneChoice using IsValid() before accessing its properties to prevent
    runtime errors.
  related:
  - GetZoneChoice
  - GetFactionCapital
  - GetStartingArea
metadata:
  confidence: 0.85
  generated_at: '2025-11-04T12:47:52.435721'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
