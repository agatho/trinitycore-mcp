api:
  class: ObjectMgr
  method: LoadCreatureTemplateGossip
  signature: void ObjectMgr::LoadCreatureTemplateGossip()
documentation:
  brief: Loads and initializes creature template gossip data from the database into
    memory.
  description: The LoadCreatureTemplateGossip method is responsible for retrieving
    all creature template gossip entries from the database and loading them into memory
    for use during runtime. This method ensures that NPCs (non-player characters)
    have their associated gossip options properly configured when players interact
    with them. It typically runs during server startup or reload operations to populate
    internal data structures that store gossip information for each creature template.
    The method handles parsing of gossip menu entries, text options, and related quest
    associations tied to specific creatures.
  parameters: []
  returns: null
  examples:
  - title: Basic Usage During Server Initialization
    code: "void WorldServer::Initialize()\n{\n    // Other initialization code...\n\
      \    sObjectMgr->LoadCreatureTemplateGossip();\n    // Continue with other loading\
      \ processes\n}"
    language: cpp
  notes: This method is typically called during server startup or database reload
    operations. It's important to note that this method doesn't directly handle player
    interaction but prepares the data structures needed for gossip functionality.
    The loaded data is used by the Gossip Manager when players initiate conversations
    with NPCs.
  warnings: null
  related:
  - LoadGossipMenu
  - LoadGossipMenuItems
  - GetCreatureTemplate
  - GetGossipMenu
metadata:
  confidence: 0.95
  generated_at: '2025-10-31T23:26:34.229036'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
