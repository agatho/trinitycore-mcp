api:
  class: BotAITemplates
  method: GetCasterDPSTemplate
  signature: BotAIConfig BotAITemplates::GetCasterDPSTemplate()
documentation:
  brief: Returns the caster DPS bot AI configuration template.
  description: Retrieves a predefined BotAIConfig object that defines the behavior
    and decision-making logic for caster DPS bots within the TrinityCore framework.
    This template specifies how the bot should prioritize targets, manage spell casting,
    handle movement, and interact with the game environment while fulfilling its role
    as a caster damage dealer. The returned configuration is typically used to initialize
    or reset the AI state of a caster DPS bot.
  parameters: []
  returns: A BotAIConfig object containing the predefined settings and behaviors for
    caster DPS bots, including target selection rules, spell priorities, movement
    patterns, and combat strategies.
  examples:
  - title: Retrieve Caster DPS Template
    code: 'BotAIConfig casterDPSConfig = botAiTemplates.GetCasterDPSTemplate();

      // Use the configuration to initialize a new caster DPS bot

      bot->SetAIConfig(casterDPSConfig);'
    language: cpp
  notes: This method is part of the BotAITemplates class which manages predefined
    AI configurations for different bot roles. The returned BotAIConfig object should
    not be modified directly; instead, it should be used as a base to create custom
    configurations if needed.
  warnings: null
  related:
  - GetMeleeDPSTemplate
  - GetHealerTemplate
  - GetTankTemplate
metadata:
  confidence: 0.9
  generated_at: '2025-11-04T14:45:33.562359'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
