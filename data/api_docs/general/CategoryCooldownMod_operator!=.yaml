api:
  class: CategoryCooldownMod
  method: operator!=
  signature: bool CategoryCooldownMod::operator!=(const CategoryCooldownMod & right)
    const
documentation:
  brief: Compares two CategoryCooldownMod objects for inequality.
  description: The operator!= method performs a comparison between this CategoryCooldownMod
    instance and another CategoryCooldownMod object to determine if they are not equal.
    This comparison is typically used to check whether two cooldown modification entries
    have different properties or values. In the context of World of Warcraft's TrinityCore,
    CategoryCooldownMod likely represents a modifier for spell or ability cooldowns
    based on category, and this operator allows for determining when such modifiers
    differ. The method returns true if the objects are not equal and false if they
    are equal.
  parameters: []
  returns: Returns true if the current CategoryCooldownMod object is not equal to
    the provided right object; otherwise, returns false.
  examples:
  - title: Basic Inequality Check
    code: 'CategoryCooldownMod mod1;

      CategoryCooldownMod mod2;

      bool result = (mod1 != mod2); // Returns true if mod1 and mod2 are different'
    language: cpp
  - title: Using in Conditional Logic
    code: "CategoryCooldownMod currentMod = player->GetCategoryCooldownMod(categoryId);\n\
      CategoryCooldownMod defaultMod = CategoryCooldownMod();\nif (currentMod != defaultMod)\
      \ {\n    // Apply custom cooldown modification\n}"
    language: cpp
  notes: This operator is likely implemented using a member-wise comparison of the
    relevant fields within CategoryCooldownMod. It's important to note that for proper
    functionality, all member variables should be comparable for equality and inequality
    operations.
  warnings: null
  related:
  - operator==
  - CategoryCooldownMod::operator=
metadata:
  confidence: 0.9
  generated_at: '2025-11-09T03:19:47.165237'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
