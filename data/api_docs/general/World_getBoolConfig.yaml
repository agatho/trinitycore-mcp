api:
  class: World
  method: getBoolConfig
  signature: bool World::getBoolConfig(WorldBoolConfigs index) const
documentation:
  brief: Retrieves the boolean configuration value associated with the specified WorldBoolConfigs
    index.
  description: The getBoolConfig method is used to retrieve boolean configuration
    values from the World object's internal configuration storage. It takes a WorldBoolConfigs
    enumeration value as input and returns the corresponding boolean setting. This
    method is commonly used to check various server-wide settings such as whether
    PvP is enabled, if certain game mechanics are active, or if specific features
    are toggled on or off. The method performs no validation on the input index, so
    it's important to ensure that only valid WorldBoolConfigs values are passed to
    avoid undefined behavior. This configuration system allows for runtime modification
    of server behavior without requiring a restart.
  parameters: []
  returns: Returns a boolean value representing the configuration setting associated
    with the provided WorldBoolConfigs index. True indicates the feature or setting
    is enabled, while false indicates it is disabled.
  examples:
  - title: Check if PvP is enabled
    code: "if (sWorld->getBoolConfig(WORLD_ENABLE_PVP)) {\n    // PvP is enabled\n\
      \    sLog->outString(\"PvP is currently enabled\");\n} else {\n    // PvP is\
      \ disabled\n    sLog->outString(\"PvP is currently disabled\");\n}"
    language: cpp
  - title: Verify if instance locking is enabled
    code: "bool instanceLockingEnabled = sWorld->getBoolConfig(WORLD_ENABLE_INSTANCE_LOCKING);\n\
      if (instanceLockingEnabled) {\n    // Handle logic for locked instances\n}"
    language: cpp
  notes: This method accesses a global configuration cache that is initialized during
    server startup. The configuration values are typically loaded from the worldserver.conf
    file or database settings. The method is thread-safe as it only reads from immutable
    configuration data structures.
  warnings: Passing an invalid WorldBoolConfigs index may result in undefined behavior
    or incorrect return values. Always ensure that the index corresponds to a valid
    configuration option defined in the WorldBoolConfigs enumeration.
  related:
  - setBoolConfig
  - getIntConfig
  - getFloatConfig
metadata:
  confidence: 0.95
  generated_at: '2025-11-05T23:27:24.632064'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
