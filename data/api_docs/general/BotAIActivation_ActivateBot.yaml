api:
  class: BotAIActivation
  method: ActivateBot
  signature: BotAI * BotAIActivation::ActivateBot(int * bot, const BotAIConfig * config)
documentation:
  brief: Initializes and returns a new BotAI instance for the specified bot with the
    given configuration.
  description: The ActivateBot method creates and initializes a BotAI instance for
    a given bot, using the provided configuration settings. This method is responsible
    for setting up the AI behavior and parameters for the bot within the TrinityCore
    framework. It typically involves allocating memory for the AI structure, applying
    the configuration data, and initializing internal state variables. The method
    assumes that the bot identifier and configuration are valid and properly initialized
    before invocation. The returned BotAI pointer can be used to control or query
    the bot's AI behavior in subsequent operations.
  parameters:
  - name: bot
    description: Pointer to an integer representing the unique identifier of the bot
      to be activated. This value must correspond to a valid bot instance within the
      system.
  - name: config
    description: Pointer to a BotAIConfig structure containing configuration parameters
      for the AI behavior. Must not be null and should contain valid settings for
      the bot's operation.
  returns: Returns a pointer to the newly created BotAI instance if successful, or
    nullptr if activation fails due to invalid input parameters or allocation errors.
  examples:
  - title: Basic Bot Activation
    code: "int botId = 12345;\nBotAIConfig config;\nconfig.aiType = BOT_AI_TYPE_COMBAT;\n\
      config.level = 80;\nBotAI* botAI = BotAIActivation::ActivateBot(&botId, &config);\n\
      if (botAI != nullptr) {\n    // Bot AI successfully activated\n}"
    language: cpp
  - title: Error Handling for Activation
    code: "int botId = 0;\nBotAIConfig config;\nconfig.aiType = BOT_AI_TYPE_NONE;\n\
      BotAI* botAI = BotAIActivation::ActivateBot(&botId, &config);\nif (botAI ==\
      \ nullptr) {\n    // Handle activation failure\n    printf(\"Failed to activate\
      \ bot AI\\n\");\n}"
    language: cpp
  notes: The method may perform memory allocation and should be called from a context
    where such operations are safe. The configuration object must remain valid for
    the lifetime of the returned BotAI instance. This method is typically used during
    bot initialization or reactivation scenarios.
  warnings: Calling this method with a null pointer for 'bot' or 'config' will likely
    result in undefined behavior or crashes. Ensure that the bot identifier corresponds
    to an existing bot and that the configuration values are within valid ranges before
    calling.
  related:
  - DeactivateBot
  - GetBotAI
  - UpdateBotAI
metadata:
  confidence: 0.85
  generated_at: '2025-11-06T19:26:36.321079'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
