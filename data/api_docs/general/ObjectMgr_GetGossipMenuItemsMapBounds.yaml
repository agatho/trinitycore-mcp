api:
  class: ObjectMgr
  method: GetGossipMenuItemsMapBounds
  signature: int ObjectMgr::GetGossipMenuItemsMapBounds(uint32 uiMenuId) const
documentation:
  brief: Retrieves the number of gossip menu items associated with a specified menu
    ID.
  description: The GetGossipMenuItemsMapBounds method returns the count of gossip
    menu items linked to a given menu identifier. This function is typically used
    within the World of Warcraft TrinityCore server to determine how many options
    are available in a specific gossip interaction menu. It serves as a utility for
    managing and iterating through gossip menus, particularly when constructing or
    validating menu structures. The method does not modify any data but provides read-only
    access to the internal mapping structure that stores gossip menu item associations.
  parameters: []
  returns: An integer representing the number of gossip menu items associated with
    the specified menu ID. Returns 0 if no items are found for the given menu ID or
    if the menu ID is invalid.
  examples:
  - title: Check Gossip Menu Item Count
    code: "uint32 menuId = 1234;\nint itemCount = sObjectMgr->GetGossipMenuItemsMapBounds(menuId);\n\
      if (itemCount > 0)\n{\n    printf(\"Menu %u contains %d items\\n\", menuId,\
      \ itemCount);\n}\nelse\n{\n    printf(\"No items found for menu %u\\n\", menuId);\n\
      }"
    language: cpp
  notes: This method accesses internal data structures that map gossip menus to their
    associated items. It's important to ensure that the provided menu ID is valid
    before calling this function, as invalid IDs may result in unexpected behavior
    or return values. The implementation likely uses a map or similar container for
    efficient lookups.
  warnings: null
  related:
  - GetGossipMenuItems
  - AddGossipMenuItem
  - RemoveGossipMenuItem
metadata:
  confidence: 0.9
  generated_at: '2025-11-01T09:36:24.506961'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
