api:
  class: TalentLoadout
  method: GetTalentCount
  signature: uint32 TalentLoadout::GetTalentCount() const
documentation:
  brief: Returns the total number of talents in the talent loadout
  description: The GetTalentCount method retrieves the total count of talents currently
    assigned to a specific talent loadout. This method is typically used in the context
    of World of Warcraft's talent system, where players can have multiple talent trees
    and loadouts that define their character's specialization. The returned value
    represents how many talent entries are active within this particular loadout configuration.
    This count includes all talents that have been selected or allocated, regardless
    of their tier or tree classification.
  parameters: []
  returns: A uint32 value representing the total number of talents currently in the
    talent loadout. Returns 0 if the loadout contains no talents or is invalid.
  examples:
  - title: Basic Usage
    code: "uint32 talentCount = playerLoadout->GetTalentCount();\nif (talentCount\
      \ > 0)\n{\n    printf(\"Player has %u talents in this loadout\\n\", talentCount);\n\
      }"
    language: cpp
  - title: Checking Loadout Validity
    code: "TalentLoadout* currentLoadout = player->GetTalentLoadout(0);\nif (currentLoadout\
      \ && currentLoadout->GetTalentCount() > 0)\n{\n    // Proceed with talent-based\
      \ logic\n    uint32 count = currentLoadout->GetTalentCount();\n    // Use count\
      \ for calculations or UI updates\n}"
    language: cpp
  notes: This method is likely implemented as a simple accessor that returns a cached
    value rather than recalculating the talent count each time. The performance impact
    should be negligible. This method may return 0 if the loadout has not been initialized
    or contains no valid talent entries.
  warnings: Ensure that the TalentLoadout object being called upon is valid before
    invoking this method to avoid potential null pointer dereferences. In some cases,
    an invalid loadout might still return a non-zero count if it's in a transitional
    state.
  related:
  - GetTalentInfo
  - HasTalent
  - AddTalent
  - RemoveTalent
metadata:
  confidence: 0.9
  generated_at: '2025-11-08T14:37:03.615810'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
