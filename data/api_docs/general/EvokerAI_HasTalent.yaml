api:
  class: EvokerAI
  method: HasTalent
  signature: bool EvokerAI::HasTalent(int talentId)
documentation:
  brief: Checks if the evoker has a specific talent unlocked.
  description: The HasTalent method determines whether the current evoker AI instance
    possesses a particular talent by its unique identifier. This functionality is
    essential for evoker-specific AI behavior logic, allowing the AI to make decisions
    based on which talents are active or available. The method typically checks against
    the evoker's current talent tree state and returns true if the specified talent
    is unlocked, false otherwise. This check is commonly used in spell selection,
    ability prioritization, and combat decision-making within the evoker's AI routines.
  parameters:
  - name: talentId
    description: The unique identifier of the talent to check for. This value corresponds
      to a specific talent in the evoker's talent tree as defined in the game's database
      or configuration files.
  returns: Returns true if the evoker has the specified talent unlocked, false otherwise.
  examples:
  - title: Basic Talent Check
    code: "if (evokerAI->HasTalent(12345)) {\n    // Perform action only if talent\
      \ is active\n    evokerAI->CastSpell(target, SPELL_ENERGY_BOLT);\n}"
    language: cpp
  - title: Conditional Ability Usage
    code: "if (evokerAI->HasTalent(67890)) {\n    // Use a specialized ability only\
      \ available with this talent\n    evokerAI->CastSpell(target, SPELL_TIME_WARP);\n\
      } else {\n    // Fallback to standard attack\n    evokerAI->AttackStart(target);\n\
      }"
    language: cpp
  notes: This method likely relies on internal talent tracking data that is updated
    when the evoker levels up or gains access to new talents. The talentId parameter
    should correspond to valid entries in the game's talent database, otherwise behavior
    may be undefined.
  warnings: Ensure that the talentId passed to this method corresponds to an actual
    talent in the evoker's class tree; invalid IDs may cause unexpected behavior or
    return incorrect results. This method is typically used within combat AI logic
    where performance matters, so avoid calling it excessively in tight loops.
  related:
  - GetTalentPoints
  - LearnTalent
  - HasSpell
metadata:
  confidence: 0.9
  generated_at: '2025-11-07T14:07:21.652271'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
