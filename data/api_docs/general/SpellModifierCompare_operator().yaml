api:
  class: SpellModifierCompare
  method: operator()
  signature: bool SpellModifierCompare::operator()(const SpellModifier * left, const
    SpellModifier * right) const
documentation:
  brief: Compares two SpellModifier pointers for ordering in a sorted container.
  description: The SpellModifierCompare::operator() method is a comparison function
    object used to define the ordering of SpellModifier pointers within sorted containers
    such as std::set or std::map. It typically compares the priority, type, or other
    identifying characteristics of the spell modifiers to determine their relative
    ordering. This functionality is essential for managing spell modifications in
    TrinityCore's spell system, where different modifiers may need to be applied in
    a specific sequence based on their properties. The method returns true if the
    left operand should precede the right operand in the sorted order.
  parameters: []
  returns: Returns true if the left SpellModifier should be ordered before the right
    SpellModifier; otherwise, returns false.
  examples:
  - title: Sorting SpellModifiers in a set
    code: "std::set<SpellModifier*, SpellModifierCompare> spellMods;\n// Insert SpellModifier\
      \ objects here\n// The set will automatically order them using operator()\n\
      for (const auto& mod : spellMods) {\n    // Process sorted modifiers\n}"
    language: cpp
  notes: This method is typically used internally by TrinityCore's spell system to
    maintain ordered collections of spell modifiers. It's important that the comparison
    logic remains consistent with the intended application behavior to ensure predictable
    modifier application order.
  warnings: null
  related: []
metadata:
  confidence: 0.95
  generated_at: '2025-11-08T21:36:01.317097'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
