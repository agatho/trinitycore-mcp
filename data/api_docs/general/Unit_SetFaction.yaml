api:
  class: Unit
  method: SetFaction
  signature: void Unit::SetFaction(uint32 faction)
documentation:
  brief: Sets the faction identifier for a unit, determining its relationship with
    other factions in the game world.
  description: The SetFaction method allows developers to assign a specific faction
    ID to a Unit object, which controls how the unit interacts with other factions
    within the game. This method is commonly used in NPC AI scripts or when creating
    custom creatures behavior. The faction ID determines the unit's standing with
    various factions, such as friendly, hostile, or neutral, and affects interactions
    like combat, gossip, and quest rewards. When a unit's faction is set, it typically
    changes the unit's behavior toward other players or NPCs based on that faction's
    configuration in the database.
  parameters:
  - name: faction
    description: The faction ID to assign to the unit. This value corresponds to entries
      in the faction table within the database and determines how the unit interacts
      with other factions.
  returns: null
  examples:
  - title: Basic Usage Example
    code: "Unit* pTarget = GetVictim();\nif (pTarget)\n{\n    pTarget->SetFaction(14);\n\
      }\n"
    language: cpp
  notes: This method should be used carefully as changing a unit's faction may affect
    its behavior in combat or with NPCs. It is recommended to use this method only
    when necessary, such as during NPC AI setup or when creating custom behaviors.
  warnings: Improper use of SetFaction can lead to unexpected behavior changes in
    NPCs, including incorrect combat behavior or failure to interact properly with
    players. Always ensure that the faction ID corresponds to valid entries in the
    database.
  related:
  - GetFaction
  - SetFaction
  - SetFaction
  - SetFaction
metadata:
  confidence: 0.95
  generated_at: '2025-10-30T01:16:16.292436'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
