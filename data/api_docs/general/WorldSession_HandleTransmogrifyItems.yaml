api:
  class: WorldSession
  method: HandleTransmogrifyItems
  signature: void WorldSession::HandleTransmogrifyItems(WorldPackets::Transmogrification::TransmogrifyItems
    & transmogrifyItems)
documentation:
  brief: Processes transmogrification requests for player items
  description: Handles the transmogrify items packet sent by the client, allowing
    players to change the visual appearance of their equipped items without altering
    item stats. This method validates the transmogrification request, checks if the
    player has sufficient currency, and applies the visual changes to the specified
    items. The method operates within the WorldSession context and interacts with
    the transmogrification system to manage item appearance transformations.
  parameters: []
  returns: null
  examples:
  - title: Basic transmogrify request handling
    code: "void WorldSession::HandleTransmogrifyItems(WorldPackets::Transmogrification::TransmogrifyItems&\
      \ transmogrifyItems)\n{\n    // Validate player and items\n    if (!GetPlayer()\
      \ || !transmogrifyItems.Items.empty())\n        return;\n    \n    // Process\
      \ transmogrification logic\n    // ... implementation details ...\n}"
    language: cpp
  notes: This method is part of the transmogrification system and requires proper
    validation of item ownership, transmogrification rules, and currency requirements.
    The actual appearance changes are applied server-side and synchronized with the
    client.
  warnings: Improper handling of this method can lead to visual inconsistencies or
    exploit opportunities. Always validate that items being transmogrified are owned
    by the player and meet the required criteria before applying changes.
  related:
  - HandleTransmogrifyItem
  - HandleTransmogrifyRemove
  - SendTransmogrifyResult
metadata:
  confidence: 0.9
  generated_at: '2025-11-04T20:10:16.265638'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
