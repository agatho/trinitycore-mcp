api:
  class: AuctionsBucketData
  method: BuildBucketInfo
  signature: void AuctionsBucketData::BuildBucketInfo(WorldPackets::AuctionHouse::BucketInfo
    * bucketInfo, const Player * player) const
documentation:
  brief: Populates an AuctionHouse bucket info packet with data from the auction bucket.
  description: The BuildBucketInfo method is responsible for filling a WorldPackets::AuctionHouse::BucketInfo
    structure with relevant information about an auction bucket. This typically includes
    details such as item class, subclass, quality, and other metadata that defines
    the characteristics of items within the bucket. The method uses the provided player
    context to ensure proper data filtering or localization where applicable. It is
    commonly used when sending auction house data to clients, particularly during
    auction house browse operations or when updating bucket information dynamically.
  parameters:
  - name: bucketInfo
    description: Pointer to a WorldPackets::AuctionHouse::BucketInfo object that will
      be populated with auction bucket data.
  - name: player
    description: Pointer to the Player object representing the client who is requesting
      or receiving the auction bucket information. Used for context-sensitive data
      handling.
  returns: null
  examples:
  - title: Basic Usage in Auction House Handler
    code: 'AuctionsBucketData bucketData;

      WorldPackets::AuctionHouse::BucketInfo bucketInfo;

      Player* player = ...; // Assume valid player pointer

      bucketData.BuildBucketInfo(&bucketInfo, player);

      // Send bucketInfo to player via packet system'
    language: cpp
  notes: This method is part of the internal auction house system and should only
    be called when preparing data for transmission to players. It assumes that the
    bucketData object has been properly initialized with valid auction bucket information
    before calling.
  warnings: null
  related:
  - AuctionsBucketData::GetBucketInfo
  - AuctionsBucketData::SetBucketInfo
metadata:
  confidence: 0.9
  generated_at: '2025-11-08T08:59:04.923265'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
