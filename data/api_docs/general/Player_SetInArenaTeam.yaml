api:
  class: Player
  method: SetInArenaTeam
  signature: void Player::SetInArenaTeam(uint32 ArenaTeamId, uint8 slot, uint8 type)
documentation:
  brief: Sets the player's arena team membership for a specific arena type and slot.
  description: This method assigns a player to a specific arena team based on the
    provided team ID, slot, and arena type. It updates the player's arena team information
    in the database and ensures proper synchronization with the game world. The method
    is typically used during arena team registration or when changing arena team assignments.
    This function does not directly handle team creation or deletion, but rather updates
    existing team membership for the player.
  parameters:
  - name: ArenaTeamId
    description: The unique identifier of the arena team to assign the player to.
      Must be a valid team ID from the database.
  - name: slot
    description: The slot position within the arena team (0-5). This determines which
      team the player belongs to in the context of the specified arena type.
  - name: type
    description: The arena type (2v2, 3v3, 5v5) for which the team assignment is being
      made. Valid values are 2, 3, or 5.
  returns: null
  examples:
  - title: Basic Usage Example
    code: 'Player* player = GetPlayer();

      player->SetInArenaTeam(12345, 0, 2); // Assign player to 2v2 arena team 12345
      in slot 0'
    language: cpp
  - title: Advanced Usage Example
    code: 'Player* player = GetPlayer();

      uint32 teamId = GetArenaTeamId();

      uint8 slot = 1;

      uint8 type = 5;

      player->SetInArenaTeam(teamId, slot, type);

      // This will update the player''s arena team membership for 5v5'
    language: cpp
  notes: This method likely updates database records for the player's arena team assignments.
    It should be called with proper validation of parameters to avoid invalid team
    assignments. The method may trigger synchronization with the game world, ensuring
    the player's arena team status is properly updated in the database.
  warnings: Improper use of this method can lead to incorrect arena team assignments
    or database corruption. Care must be taken to ensure valid team IDs and slot positions
    are used. This method should not be called without proper validation of the arena
    team ID and slot.
  related:
  - GetArenaTeamId
  - GetArenaTeamSlot
  - GetArenaType
metadata:
  confidence: 0.9
  generated_at: '2025-10-30T15:04:44.113135'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
