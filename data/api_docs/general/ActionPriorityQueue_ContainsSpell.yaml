api:
  class: ActionPriorityQueue
  method: ContainsSpell
  signature: bool ActionPriorityQueue::ContainsSpell(uint32 spellId) const
documentation:
  brief: Checks if a specific spell is contained within the action priority queue.
  description: The ContainsSpell method determines whether a given spell ID exists
    in the ActionPriorityQueue. This functionality is typically used during AI decision-making
    processes to verify if a particular spell has been added to the queue for execution.
    The method performs a lookup operation on the internal data structure that stores
    spell entries, returning true if the spell ID is found and false otherwise. It's
    commonly invoked before attempting to execute or modify spells within the priority
    queue.
  parameters: []
  returns: Returns true if the specified spell ID exists in the action priority queue;
    otherwise returns false.
  examples:
  - title: Basic Usage
    code: "if (actionPriorityQueue.ContainsSpell(12345)) {\n    // Spell is present\
      \ in the queue\n    // Proceed with further logic\n}"
    language: cpp
  - title: Integration with AI Logic
    code: "uint32 spellToCheck = 98765;\nif (!actionPriorityQueue.ContainsSpell(spellToCheck))\
      \ {\n    actionPriorityQueue.AddSpell(spellToCheck, 1.0f);\n    // Add the spell\
      \ to queue if not already present\n}"
    language: cpp
  notes: This method likely uses an optimized lookup mechanism such as a hash table
    or set for performance reasons, especially when dealing with large numbers of
    spells in the priority queue. The implementation assumes that spell IDs are unique
    identifiers within the game context.
  warnings: null
  related:
  - AddSpell
  - RemoveSpell
  - GetSpellPriority
  - Clear
metadata:
  confidence: 0.9
  generated_at: '2025-11-06T22:28:31.926182'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
