api:
  class: HolyPowerSystem
  method: Generate
  signature: void HolyPowerSystem::Generate(uint32 amount)
documentation:
  brief: Generates holy power for the player or unit associated with the HolyPowerSystem
    instance.
  description: The Generate method is responsible for increasing the holy power amount
    stored within a HolyPowerSystem instance. This method typically corresponds to
    game mechanics where players or units gain holy power through specific actions,
    abilities, or events in World of Warcraft such as casting certain spells, dealing
    damage with holy attacks, or using items that provide holy power. The method accepts
    a uint32 parameter representing the amount of holy power to be added. It modifies
    the internal state of the HolyPowerSystem instance directly without returning
    any value.
  parameters:
  - name: amount
    description: The quantity of holy power to generate and add to the current holy
      power total. Must be a non-negative integer value.
  returns: null
  examples:
  - title: Basic Usage
    code: 'HolyPowerSystem* hps = player->GetHolyPowerSystem();

      hps->Generate(10); // Adds 10 holy power to the system'
    language: cpp
  - title: Using with Spell Effects
    code: "if (spellInfo->HasEffect(SPELL_EFFECT_ENERGIZE)) {\n    HolyPowerSystem*\
      \ hps = player->GetHolyPowerSystem();\n    hps->Generate(spellInfo->Effects[0].BasePoints);\
      \ // Generate holy power based on spell effect value\n}"
    language: cpp
  notes: This method is typically called during spell execution or combat events.
    It's important to note that the actual behavior may depend on the underlying implementation
    of the HolyPowerSystem class and how it interacts with other systems like player
    stats, cooldowns, or aura effects.
  warnings: Ensure that the amount passed to Generate is valid and within expected
    ranges to prevent unexpected behavior or overflow issues. This method should not
    be called directly from outside the system unless proper access controls are in
    place.
  related:
  - GetHolyPower
  - SetHolyPower
  - ConsumeHolyPower
metadata:
  confidence: 0.9
  generated_at: '2025-11-09T01:09:34.550433'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
