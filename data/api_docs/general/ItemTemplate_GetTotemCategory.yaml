api:
  class: ItemTemplate
  method: GetTotemCategory
  signature: uint32 ItemTemplate::GetTotemCategory() const
documentation:
  brief: Returns the totem category ID associated with the item template.
  description: The GetTotemCategory method retrieves the totem category identifier
    that is linked to this item template. This value determines which type of totem
    an item represents, such as Earth, Fire, Water, or Air totems. The totem category
    is used by the game engine to enforce rules about totem placement, stacking, and
    interaction with other totems. For items that are not totems, this method will
    typically return 0 or a default value indicating no totem category.
  parameters: []
  returns: A uint32 representing the totem category ID of the item template. A value
    of 0 usually indicates that the item is not a totem or does not belong to any
    specific totem category.
  examples:
  - title: Check if an item is a totem
    code: "ItemTemplate* itemTemplate = sObjectMgr->GetItemTemplate(12345);\nif (itemTemplate->GetTotemCategory()\
      \ != 0)\n{\n    printf(\"This item is a totem with category ID: %u\\n\", itemTemplate->GetTotemCategory());\n\
      }"
    language: cpp
  notes: The method is a simple getter and does not perform any computation or database
    lookups. It directly returns a cached value from the item template data structure,
    making it efficient to call.
  warnings: null
  related: []
metadata:
  confidence: 0.95
  generated_at: '2025-11-04T02:14:29.723011'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
