api:
  class: BotAITemplates
  method: GetDeathKnightTemplate
  signature: BotAIConfig BotAITemplates::GetDeathKnightTemplate(bool tank)
documentation:
  brief: Retrieves the configured AI template for Death Knights, optionally tailored
    for tanking roles.
  description: The GetDeathKnightTemplate method returns a BotAIConfig object that
    defines the behavior and settings for Death Knight bots within the TrinityCore
    framework. This configuration includes abilities, spells, gear preferences, and
    combat strategies specific to Death Knights. The optional 'tank' parameter allows
    retrieval of a specialized template designed for tanking roles, which may include
    different priority settings for threat generation, defensive cooldowns, and target
    selection compared to standard DPS or support templates. The method is typically
    used during bot initialization or when switching class roles dynamically.
  parameters: []
  returns: A BotAIConfig object containing all necessary parameters and settings for
    configuring a Death Knight bot's AI behavior. This includes spell priorities,
    item usage rules, combat tactics, and role-specific configurations. Returns an
    empty or default configuration if no valid template is found.
  examples:
  - title: Retrieve Standard Death Knight Template
    code: 'BotAIConfig dkTemplate = botAiTemplates.GetDeathKnightTemplate(false);

      // Use the template to initialize a Death Knight bot

      bot->SetAIConfig(dkTemplate);'
    language: cpp
  - title: Retrieve Tanking-Specific Death Knight Template
    code: 'BotAIConfig tankTemplate = botAiTemplates.GetDeathKnightTemplate(true);

      // Apply tank-specific configuration for a Death Knight in a raid

      bot->SetAIConfig(tankTemplate);'
    language: cpp
  notes: This method likely accesses pre-defined AI templates stored in configuration
    files or databases within the TrinityCore system. The returned BotAIConfig object
    should be treated as immutable after retrieval to prevent unexpected behavior
    during active bot operation. Template loading may involve caching mechanisms for
    performance optimization.
  warnings: Calling this method without proper initialization of the BotAITemplates
    class may result in undefined behavior or return invalid configurations. Ensure
    that the TrinityCore AI subsystem is fully initialized before invoking this method,
    especially in multi-threaded environments where concurrent access to template
    data might occur.
  related:
  - GetMageTemplate
  - GetWarriorTemplate
  - GetPriestTemplate
  - SetDeathKnightTemplate
metadata:
  confidence: 0.85
  generated_at: '2025-11-04T14:45:13.260947'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
