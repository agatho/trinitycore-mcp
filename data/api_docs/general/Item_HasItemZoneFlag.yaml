api:
  class: Item
  method: HasItemZoneFlag
  signature: bool Item::HasItemZoneFlag(ItemZoneFlags flag) const
documentation:
  brief: Checks if the item has a specific zone flag set.
  description: This method determines whether a given item possesses a particular
    zone flag, which is used to identify items that are restricted to specific zones
    or areas. The function returns true if the item's zone flag matches the specified
    flag, false otherwise. This functionality is commonly used in World of Warcraft
    client-side logic and server-side item management to enforce zone-based restrictions
    on items.
  parameters: []
  returns: Returns true if the item has the specified zone flag set, false otherwise.
  examples:
  - title: Basic usage example
    code: "Item* item = player->GetItemByGUID(12345);\nif (item->HasItemZoneFlag(ItemZoneFlags::ITEM_ZONE_FLAG_1))\
      \ {\n    // Do something\n}"
    language: cpp
  notes: This method is typically used in conjunction with other item zone-related
    functions to manage item restrictions and ensure proper gameplay mechanics. It's
    important to note that the flag value may vary depending on the specific implementation
    of the zone flag system.
  warnings: The method should be used carefully as it can affect item behavior in
    certain zones, particularly when dealing with items that have multiple zone flags
    or when using zone-specific item restrictions.
  related:
  - Item::GetZoneFlag
  - Item::SetZoneFlag
metadata:
  confidence: 0.85
  generated_at: '2025-10-31T04:46:59.602303'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
