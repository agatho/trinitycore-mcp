api:
  class: BotProfiler
  method: GetSessionTypeName
  signature: std::string BotProfiler::GetSessionTypeName(ProfilingSessionType type)
    const
documentation:
  brief: Returns the string name of a profiling session type.
  description: The GetSessionTypeName method translates a ProfilingSessionType enumeration
    value into its corresponding human-readable string representation. This is useful
    for logging, debugging, or displaying session type information in user interfaces.
    The method is const, meaning it does not modify the state of the BotProfiler instance.
    It relies on an internal mapping between enumeration values and their string equivalents
    to provide accurate type names.
  parameters: []
  returns: A std::string containing the name of the profiling session type, such as
    'Combat', 'Movement', or 'AI'. Returns an empty string if the input type is invalid
    or unrecognized.
  examples:
  - title: Basic Usage
    code: 'BotProfiler profiler;

      ProfilingSessionType type = ProfilingSessionType::COMBAT;

      std::string typeName = profiler.GetSessionTypeName(type);

      // typeName will be "Combat"'
    language: cpp
  - title: Logging Session Type
    code: "void LogSessionStart(BotProfiler& profiler, ProfilingSessionType type)\
      \ {\n    std::string sessionName = profiler.GetSessionTypeName(type);\n    LOG_INFO(\"\
      bot\", \"Starting {} profiling session\", sessionName);\n}"
    language: cpp
  notes: This method is designed to be efficient and should not introduce significant
    performance overhead. It is intended for use in logging or UI display scenarios
    where readable names are needed instead of raw enumeration values.
  warnings: null
  related:
  - GetSessionType
  - IsProfilingActive
metadata:
  confidence: 0.9
  generated_at: '2025-11-04T13:33:26.211492'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
