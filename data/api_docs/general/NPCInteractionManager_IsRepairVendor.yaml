api:
  class: NPCInteractionManager
  method: IsRepairVendor
  signature: bool NPCInteractionManager::IsRepairVendor(Creature * npc) const
documentation:
  brief: Determines whether a creature is a repair vendor.
  description: The IsRepairVendor method checks if the specified NPC creature is classified
    as a repair vendor within the TrinityCore framework. This determination is typically
    based on the creature's entry ID and its flags or npcflag values, which indicate
    special vendor behaviors in World of Warcraft. The method returns true if the
    NPC can repair items for players, false otherwise. This functionality is essential
    for interaction systems that need to differentiate between regular NPCs and those
    capable of providing repair services.
  parameters: []
  returns: Returns true if the creature is a repair vendor; otherwise, returns false.
  examples:
  - title: Basic Usage
    code: "if (npcInteractionManager.IsRepairVendor(npc)) {\n    player->SendVendorList(npc);\n\
      }"
    language: cpp
  - title: Conditional Repair Interaction
    code: "Creature* targetNpc = player->GetNPCIfCanInteractWith();\nif (targetNpc\
      \ && npcInteractionManager.IsRepairVendor(targetNpc)) {\n    player->GetSession()->SendShowBank(player,\
      \ targetNpc);\n}"
    language: cpp
  notes: This method likely relies on internal lookup tables or database entries that
    define which creatures are capable of repairing items. It may also consider the
    creature's faction and other behavioral flags to determine if it should be treated
    as a repair vendor.
  warnings: null
  related:
  - IsVendor
  - IsTrainer
  - IsGossipNPC
metadata:
  confidence: 0.9
  generated_at: '2025-11-08T23:21:30.261660'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
