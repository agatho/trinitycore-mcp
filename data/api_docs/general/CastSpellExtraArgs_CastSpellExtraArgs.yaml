api:
  class: CastSpellExtraArgs
  method: CastSpellExtraArgs
  signature: void CastSpellExtraArgs::CastSpellExtraArgs(CastSpellExtraArgs && other)
documentation:
  brief: Moves resources from another CastSpellExtraArgs object to this instance,
    effectively transferring ownership of its data members.
  description: The CastSpellExtraArgs move constructor transfers all member data from
    the source CastSpellExtraArgs object to the current object. This operation is
    typically used when creating a new CastSpellExtraArgs instance by moving an existing
    one, avoiding unnecessary copying. The source object becomes invalid after this
    operation and should not be used. This method is part of the standard C++ move
    semantics implementation for the CastSpellExtraArgs class, which is commonly used
    in TrinityCore's spell casting system to manage additional parameters passed during
    spell execution. It allows efficient resource management when temporary or intermediate
    spell argument objects are created and consumed.
  parameters: []
  returns: null
  examples:
  - title: Basic Move Construction
    code: 'CastSpellExtraArgs args1;

      // ... populate args1 with spell data ...

      CastSpellExtraArgs args2 = std::move(args1); // Move constructor called

      // args1 is now in a valid but unspecified state'
    language: cpp
  - title: Move in Spell Casting Context
    code: "void CastSpell(Unit* caster, SpellInfo const* spellInfo) {\n    CastSpellExtraArgs\
      \ args;\n    args.TriggerFlags = TRIGGERED_FULL_MASK;\n    args.CastItem = caster->GetItemByGuid(itemGuid);\n\
      \    \n    // Move the args to avoid copying\n    caster->CastSpell(target,\
      \ spellInfo, std::move(args));\n}"
    language: cpp
  notes: This constructor is typically invoked implicitly when assigning a temporary
    CastSpellExtraArgs object or explicitly using std::move(). The move constructor
    ensures efficient transfer of resources without deep copying. It's important to
    note that after moving, the source object should not be accessed as its contents
    are in a valid but unspecified state.
  warnings: Using the moved-from object after this operation results in undefined
    behavior. This constructor is designed for move semantics and should only be used
    when you no longer need the original object's data.
  related:
  - CastSpellExtraArgs::CastSpellExtraArgs()
  - CastSpellExtraArgs& CastSpellExtraArgs::operator=(CastSpellExtraArgs&& other)
  - CastSpellExtraArgs::~CastSpellExtraArgs()
metadata:
  confidence: 0.95
  generated_at: '2025-11-02T18:18:01.759339'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
