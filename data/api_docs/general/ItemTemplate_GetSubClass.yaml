api:
  class: ItemTemplate
  method: GetSubClass
  signature: uint32 ItemTemplate::GetSubClass() const
documentation:
  brief: Retrieves the subclass identifier of the item template.
  description: The GetSubClass method returns the subclass value associated with this
    item template. In World of Warcraft's item system, items are categorized by class
    and subclass, where the subclass defines more specific item types within a given
    class (e.g., weapons, armor, consumables). This value is typically used to determine
    visual properties, usability restrictions, and game mechanics related to how the
    item behaves in the game. The returned value corresponds to predefined constants
    defined in the game's database or codebase for each item subclass.
  parameters: []
  returns: A uint32 representing the subclass identifier of the item template. This
    value maps to specific item subtypes such as 'Dagger', 'Sword', 'Leather', etc.,
    depending on the item's class. Returns 0 if the subclass is not defined or invalid.
  examples:
  - title: Retrieve and log an item's subclass
    code: "ItemTemplate* itemTemplate = sObjectMgr->GetItemTemplate(12345);\nif (itemTemplate)\n\
      {\n    uint32 subClass = itemTemplate->GetSubClass();\n    TC_LOG_INFO(\"server\"\
      , \"Item 12345 has subclass ID: %u\", subClass);\n}"
    language: cpp
  notes: This method is commonly used in item processing logic, such as determining
    if an item can be equipped by a specific class or if it belongs to a certain category
    like 'Consumable' or 'Weapon'. The subclass ID should be validated against known
    constants before use to avoid unexpected behavior.
  warnings: null
  related:
  - GetClass
  - GetSubClass
  - GetQuality
metadata:
  confidence: 0.95
  generated_at: '2025-11-04T02:14:51.148782'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
