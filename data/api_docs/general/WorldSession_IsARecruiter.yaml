api:
  class: WorldSession
  method: IsARecruiter
  signature: bool WorldSession::IsARecruiter() const
documentation:
  brief: Determines whether the player associated with this session is a recruiter.
  description: The IsARecruiter method checks if the player connected through this
    WorldSession has the recruiter status. In World of Warcraft, recruiters are players
    who have been granted special permissions to recruit new players into the game,
    often through referral programs or promotional activities. This method typically
    accesses internal session flags or player data to determine if the recruiter status
    is active for the current player. The result can be used to control access to
    recruiter-specific features, rewards, or game mechanics.
  parameters: []
  returns: Returns true if the player associated with this session is a recruiter;
    otherwise returns false.
  examples:
  - title: Basic Usage Check
    code: "if (session->IsARecruiter()) {\n    // Grant recruiter-specific benefits\n\
      \    session->SendNotification(\"Welcome, Recruiter!\");\n}"
    language: cpp
  - title: Conditional Feature Access
    code: "void Player::HandleRecruiterMenu() {\n    if (GetSession()->IsARecruiter())\
      \ {\n        // Show recruiter-only menu options\n        SendRecruiterOptions();\n\
      \    } else {\n        // Show regular player menu\n        SendPlayerOptions();\n\
      \    }\n}"
    language: cpp
  notes: This method likely relies on cached or stored recruiter status information
    within the session object. It may not reflect real-time changes to recruiter status
    without explicit updates. The implementation might involve checking internal flags
    or database entries related to player recruitment privileges.
  warnings: Be cautious when using this method for access control, as the recruiter
    status may not be updated in real-time. Ensure that any permissions granted based
    on this check are appropriately validated and refreshed as needed to prevent unauthorized
    access.
  related:
  - GetPlayer
  - IsRecruiter
  - SetRecruiterStatus
metadata:
  confidence: 0.85
  generated_at: '2025-11-02T19:08:14.267263'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
