api:
  class: AccountMgr
  method: ChangeRegEmail
  signature: AccountOpResult AccountMgr::ChangeRegEmail(uint32 accountId, std::string
    newEmail)
documentation:
  brief: Changes the registered email address for a specified account ID.
  description: The ChangeRegEmail method updates the email address associated with
    a given account in the database. This function is typically used when players
    wish to update their account's registered email, which may be required for account
    recovery or communication purposes. The method performs validation on the new
    email format and ensures that the account exists before attempting the update.
    It returns an AccountOpResult indicating success or failure of the operation.
  parameters:
  - name: accountId
    description: The unique identifier of the account for which the email address
      is to be changed. Must correspond to an existing account in the database.
  - name: newEmail
    description: The new email address to associate with the account. Should be a
      valid email format string, typically validated by the system before updating.
  returns: An AccountOpResult enum value indicating whether the operation succeeded
    or failed. Possible values include SUCCESS, FAILED, INVALID_EMAIL, ACCOUNT_NOT_FOUND,
    and others depending on implementation details.
  examples:
  - title: Basic usage of ChangeRegEmail
    code: "AccountOpResult result = AccountMgr::ChangeRegEmail(12345, \"newemail@example.com\"\
      );\nif (result == AccountOpResult::SUCCESS) {\n    printf(\"Email updated successfully.\\\
      n\");\n} else {\n    printf(\"Failed to update email.\\n\");\n}"
    language: cpp
  notes: This method likely involves database operations and should be used carefully
    in performance-critical sections of code. Ensure that the accountId is valid and
    that the newEmail adheres to standard email formatting rules before calling this
    function.
  warnings: Calling this method with an invalid accountId or malformed email may result
    in undefined behavior or database inconsistencies. Always validate inputs prior
    to invoking this function.
  related:
  - GetAccountEmail
  - SetAccountEmail
  - ChangePassword
metadata:
  confidence: 0.9
  generated_at: '2025-11-02T14:38:49.144667'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
