api:
  class: SpellEffectInfo
  method: SpellEffectInfo
  signature: void SpellEffectInfo::SpellEffectInfo(SpellEffectInfo && param0)
documentation:
  brief: Moves a SpellEffectInfo object, transferring ownership of its resources to
    the new instance.
  description: The SpellEffectInfo move constructor transfers all resources from the
    source SpellEffectInfo object to the newly created object. This operation is performed
    efficiently without deep copying data, making it a fast way to transfer spell
    effect information between objects. The source object becomes invalid after the
    move and should not be used. This method is part of the standard C++ move semantics
    implementation for the SpellEffectInfo class, enabling efficient resource management
    in spell handling systems within TrinityCore.
  parameters: []
  returns: null
  examples:
  - title: Move SpellEffectInfo to new variable
    code: 'SpellEffectInfo effect1;

      // ... initialize effect1 ...

      SpellEffectInfo effect2 = std::move(effect1); // Moves resources from effect1
      to effect2

      // effect1 is now in a valid but unspecified state'
    language: cpp
  - title: Move in function parameter
    code: "void ProcessSpellEffect(SpellEffectInfo&& effect) {\n    // Use the moved\
      \ effect\n    // effect is now owned by this function\n}\n\nSpellEffectInfo\
      \ myEffect;\nProcessSpellEffect(std::move(myEffect)); // Transfers ownership\
      \ to function"
    language: cpp
  notes: This constructor follows standard C++ move semantics. After moving, the source
    object should not be accessed as its state is undefined but valid. The move constructor
    is typically called implicitly when assigning a temporary or rvalue to a variable
    of the same type.
  warnings: Using the moved object after calling this constructor results in undefined
    behavior. Ensure that no code attempts to access the source object's members after
    the move operation.
  related:
  - SpellEffectInfo::SpellEffectInfo(const SpellEffectInfo&)
  - SpellEffectInfo::~SpellEffectInfo
  - SpellEffectInfo& SpellEffectInfo::operator=(SpellEffectInfo&&)
metadata:
  confidence: 0.9
  generated_at: '2025-11-02T15:23:23.334810'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
