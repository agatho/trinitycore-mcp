api:
  class: Item
  method: GetBagSlot
  signature: uint8 Item::GetBagSlot() const
documentation:
  brief: Returns the bag slot position of the item within its container.
  description: The GetBagSlot method retrieves the specific slot position of an item
    within a bag, returning the uint8 value representing the bag slot. This method
    is essential for determining where in a container an item resides, particularly
    when dealing with inventory management and item positioning. The returned value
    corresponds to the slot number within the bag's inventory, with values ranging
    from 0 to 35. This method is commonly used in conjunction with other inventory-related
    functions to manage item placement and retrieval within player inventories.
  parameters: []
  returns: Returns a uint8 value representing the bag slot position of the item within
    its container. The value ranges from 0 to 35, where 0 indicates the first slot
    in the bag and 35 indicates the last slot. This value is used to determine the
    exact location of an item within a bag's inventory.
  examples:
  - title: Basic Usage Example
    code: 'Item* item = player->GetItemByGuid(guid);

      uint8 bagSlot = item->GetBagSlot();

      // bagSlot now contains the slot position within the bag'
    language: cpp
  notes: This method is typically used in conjunction with GetBagSlot() to determine
    item placement within a container. It's important to note that this method returns
    the slot position within a bag, not the item's overall inventory position. The
    method is thread-safe and does not require any special considerations for usage.
  warnings: This method should be used carefully when dealing with inventory management
    as it may return invalid values if the item is not properly placed in a bag. Ensure
    that the item actually belongs to a bag before calling this method, otherwise
    it may return unexpected results.
  related:
  - GetBagSlot
  - GetSlot
  - GetOwner
metadata:
  confidence: 0.95
  generated_at: '2025-10-31T05:16:42.035748'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
