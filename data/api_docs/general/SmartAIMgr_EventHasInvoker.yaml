api:
  class: SmartAIMgr
  method: EventHasInvoker
  signature: bool SmartAIMgr::EventHasInvoker(SMART_EVENT event)
documentation:
  brief: Determines whether a given SMART_EVENT type requires an invoker object to
    be valid.
  description: The EventHasInvoker method checks if a specified SMART_EVENT type necessitates
    the presence of an invoker object for its execution. In the context of TrinityCore's
    SmartAI system, certain events require a specific invoker (such as a player or
    creature) to be properly triggered and executed. This method is used internally
    by the SmartAI manager to validate event configurations and ensure that events
    which require an invoker are not improperly set up without one. The method returns
    true if the event type requires an invoker, and false otherwise.
  parameters: []
  returns: Returns true if the specified SMART_EVENT type requires an invoker object
    for execution; otherwise returns false.
  examples:
  - title: Check if an event requires an invoker
    code: "if (sSmartAIMgr->EventHasInvoker(SMART_EVENT_UPDATE_IN_BG)) {\n    // This\
      \ event requires an invoker\n    printf(\"Event requires invoker\\n\");\n} else\
      \ {\n    // This event does not require an invoker\n    printf(\"Event does\
      \ not require invoker\\n\");\n}"
    language: cpp
  notes: This method is primarily used internally by the SmartAI system to validate
    event configurations and ensure proper execution context. It helps prevent runtime
    errors when events are triggered without required invoker objects.
  warnings: null
  related:
  - GetEventInvokerType
  - IsSmartEventValid
metadata:
  confidence: 0.9
  generated_at: '2025-11-06T02:36:31.443288'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
