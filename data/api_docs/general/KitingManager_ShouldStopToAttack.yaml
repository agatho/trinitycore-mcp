api:
  class: KitingManager
  method: ShouldStopToAttack
  signature: bool KitingManager::ShouldStopToAttack(Unit * target)
documentation:
  brief: Determines whether a unit should stop kiting to attack a target.
  description: The ShouldStopToAttack method evaluates whether an AI-controlled unit
    should cease its kiting behavior and initiate an attack against a specified target.
    This decision is based on various factors such as distance, threat level, target
    health, and the unit's current combat state. The method is typically used in NPC
    AI logic to determine optimal timing for transitioning from evasive movement to
    aggressive engagement. It ensures that units do not waste time kiting when it
    would be more beneficial to attack directly.
  parameters: []
  returns: Returns true if the unit should stop kiting and begin attacking the target;
    otherwise returns false if kiting should continue.
  examples:
  - title: Basic Usage in AI Update
    code: "if (kitingManager->ShouldStopToAttack(targetUnit)) {\n    // Stop kiting\
      \ and start attacking\n    unit->SetReactState(REACT_AGGRESSIVE);\n    unit->Attack(targetUnit,\
      \ true);\n}"
    language: cpp
  notes: This method likely considers the unit's current movement state, target position,
    and combat mechanics. It may integrate with the unit's threat table and positioning
    logic to make decisions.
  warnings: null
  related:
  - KitingManager::Update
  - KitingManager::IsKiting
  - Unit::Attack
metadata:
  confidence: 0.85
  generated_at: '2025-11-01T11:56:06.439190'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
