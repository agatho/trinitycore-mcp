api:
  class: AuctionBotConfig
  method: GetHouseTypeName
  signature: const char * AuctionBotConfig::GetHouseTypeName(AuctionHouseType houseType)
documentation:
  brief: Returns the string name of an auction house type.
  description: The GetHouseTypeName method retrieves the human-readable name associated
    with a given AuctionHouseType enumeration value. This function is typically used
    to display user-friendly labels for different types of auction houses in the game
    interface or logs. The method maps internal numeric auction house type identifiers
    to their corresponding string representations, which may include names like 'Alliance',
    'Horde', or 'Neutral'. It serves as a lookup utility for converting enum values
    into descriptive text for UI elements, debugging output, or configuration displays.
  parameters: []
  returns: A null-terminated C-style string representing the name of the specified
    auction house type. Returns a valid string pointer for known auction house types;
    behavior is undefined for invalid enum values.
  examples:
  - title: Basic Usage
    code: 'AuctionHouseType type = AUCTION_HOUSE_NEUTRAL;

      const char* name = AuctionBotConfig::GetHouseTypeName(type);

      printf("Auction house type: %s\n", name);'
    language: cpp
  - title: In Configuration Context
    code: "if (strcmp(AuctionBotConfig::GetHouseTypeName(AUCTION_HOUSE_ALLIANCE),\
      \ \"Alliance\") == 0) {\n    // Proceed with Alliance-specific auction house\
      \ logic\n}"
    language: cpp
  notes: This method is likely implemented as a lookup table or switch statement internally.
    It assumes valid AuctionHouseType values are passed to it; invalid inputs may
    result in undefined behavior or return unexpected strings. The returned string
    pointer points to static memory and should not be modified or freed by the caller.
  warnings: Do not pass invalid AuctionHouseType enum values to this method, as doing
    so may lead to crashes or incorrect output. Since the returned pointer refers
    to internal static storage, avoid storing or modifying the result.
  related:
  - GetAuctionHouseId
  - GetAuctionHouseType
metadata:
  confidence: 0.9
  generated_at: '2025-11-04T16:38:27.823646'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
