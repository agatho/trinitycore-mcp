api:
  class: ScheduledChangeAI
  method: JustEnteredCombat
  signature: void ScheduledChangeAI::JustEnteredCombat(int * param0)
documentation:
  brief: Invoked when the AI object enters combat, triggering scheduled behavior changes.
  description: The JustEnteredCombat method is a callback function in the ScheduledChangeAI
    class that is executed whenever the associated creature or unit transitions into
    combat state. This method typically handles logic related to changing AI behaviors,
    activating specific combat strategies, or scheduling future AI modifications based
    on combat conditions. The method may be used to implement dynamic AI systems where
    behavior evolves during combat, such as switching to aggressive modes, initiating
    cooldown timers, or preparing for specific combat phases. It is part of the broader
    AI framework in TrinityCore and integrates with the game's combat system to ensure
    proper AI response timing.
  parameters:
  - name: param0
    description: Pointer to an integer parameter that may be used to pass additional
      context or state information related to the combat entry. This could include
      combat duration, target type, or other relevant flags affecting AI behavior.
  returns: null
  examples:
  - title: Basic Usage in Custom AI
    code: "class MyCustomAI : public ScheduledChangeAI {\npublic:\n    void JustEnteredCombat(int*\
      \ param0) override {\n        // Schedule a behavior change after 5 seconds\n\
      \        scheduler.Schedule(5s, [this](TaskContext)\n        {\n           \
      \ // Switch to aggressive mode\n            SetAggressiveMode(true);\n     \
      \   });\n    }\n};"
    language: cpp
  - title: Using Parameter for Combat Context
    code: "void ScheduledChangeAI::JustEnteredCombat(int* param0) {\n    if (param0\
      \ && *param0 > 0) {\n        // Adjust AI based on combat context\n        switch\
      \ (*param0) {\n            case 1: \n                // Boss fight - enable\
      \ special abilities\n                EnableBossAbilities();\n              \
      \  break;\n            case 2:\n                // Elite mob - increase threat\
      \ generation\n                IncreaseThreatGeneration();\n                break;\n\
      \        }\n    }\n}"
    language: cpp
  notes: This method is typically called from within the core combat system when a
    unit's combat state transitions to true. Developers should ensure that any scheduled
    changes or behavior modifications do not interfere with existing AI states or
    cause race conditions. The parameter 'param0' may be null, so proper null checking
    is recommended.
  warnings: Improper handling of the parameter can lead to undefined behavior. Avoid
    modifying core AI state directly within this method without proper synchronization,
    as it may conflict with other ongoing AI processes.
  related:
  - EnterCombat
  - UpdateAI
  - OnCombatStart
  - ScheduledChangeAI::JustExitedCombat
metadata:
  confidence: 0.85
  generated_at: '2025-11-06T03:12:09.823650'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
