api:
  class: PriestAI
  method: CastFade
  signature: void PriestAI::CastFade()
documentation:
  brief: Initiates the casting of the Fade spell for a priest AI unit
  description: The CastFade method is responsible for triggering the Fade spell cast
    by a PriestAI-controlled NPC or player. This method typically handles the logic
    required to begin the spellcasting process, including checking if the spell can
    be cast, managing cooldowns, and initiating the appropriate visual and auditory
    effects. In the context of TrinityCore's AI system, this method is part of the
    priest-specific AI behavior implementation and is usually invoked during combat
    or specific scripted events where fading is required. The method does not directly
    handle the spell's damage or effects, but rather serves as a gateway to initiate
    the casting sequence.
  parameters: []
  returns: null
  examples:
  - title: Basic usage in priest AI context
    code: "void PriestAI::UpdateAI(uint32 diff)\n{\n    if (me->GetVictim() && me->IsWithinMeleeRange(me->GetVictim()))\n\
      \    {\n        // Some condition met, trigger fade\n        CastFade();\n \
      \   }\n}"
    language: cpp
  notes: This method is typically called from within the AI update loop or other event
    handlers. It's important to ensure that the target unit and spell conditions are
    valid before calling this method to prevent errors or unexpected behavior. The
    actual spell effect implementation is handled separately by the core TrinityCore
    spell system.
  warnings: Calling CastFade without verifying that the spell can be cast (e.g., checking
    for line of sight, mana availability, or cooldowns) may result in failed spell
    casts or client-side errors. This method should not be used outside of proper
    AI context as it relies on internal AI state management.
  related:
  - CastSpell
  - IsCasting
  - CanCastSpell
metadata:
  confidence: 0.85
  generated_at: '2025-11-07T22:01:59.569558'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
