api:
  class: Player
  method: RewardPlayerWithRewardPack
  signature: void Player::RewardPlayerWithRewardPack(const RewardPackEntry * rewardPackEntry)
documentation:
  brief: Reward player with specified reward pack entry
  description: This method allows a player to receive rewards from a predefined reward
    pack entry, typically used for quest rewards or special events. The method processes
    the reward pack entry and applies the appropriate rewards to the player's inventory,
    including items, experience points, and honor points. It is commonly used in conjunction
    with quest completion or event handling within the TrinityCore framework.
  parameters:
  - name: rewardPackEntry
    description: Pointer to a RewardPackEntry structure that defines the rewards to
      be given to the player. This includes item rewards, experience points, and honor
      points.
  returns: null
  examples:
  - title: Basic usage example
    code: 'Player* player = GetPlayer();

      RewardPackEntry* rewardPack = sRewardPackMgr->GetRewardPack(12345);

      player->RewardPlayerWithRewardPack(rewardPack);'
    language: cpp
  notes: This method is typically called during quest completion or event processing.
    The reward pack entry must be valid and contain proper reward data for the method
    to function correctly.
  warnings: Improper use of reward pack entries can lead to incorrect rewards being
    applied, potentially causing game balance issues or player complaints. Ensure
    that reward pack entries are properly validated before calling this method.
  related:
  - GetPlayer
  - RewardPlayerWithRewardPack
  - QuestComplete
metadata:
  confidence: 0.95
  generated_at: '2025-10-31T00:02:44.385100'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
