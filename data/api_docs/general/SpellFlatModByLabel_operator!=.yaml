api:
  class: SpellFlatModByLabel
  method: operator!=
  signature: bool SpellFlatModByLabel::operator!=(const SpellFlatModByLabel & right)
    const
documentation:
  brief: Compares two SpellFlatModByLabel objects for inequality.
  description: The operator!= method performs a comparison between this SpellFlatModByLabel
    instance and another SpellFlatModByLabel object to determine if they are not equal.
    This comparison is typically used in spell modifier systems within TrinityCore
    where spell flat modifiers are tracked by label. The method evaluates the internal
    state of both objects, including their label identifiers and associated modifier
    values, returning true if any difference is detected between the two instances.
  parameters: []
  returns: Returns true if the two SpellFlatModByLabel objects are not equal; otherwise
    returns false. Two objects are considered unequal if their label identifiers or
    modifier values differ.
  examples:
  - title: Basic inequality check
    code: 'SpellFlatModByLabel mod1;

      SpellFlatModByLabel mod2;

      bool result = (mod1 != mod2); // Returns true if mods are different'
    language: cpp
  - title: Using in spell system logic
    code: "SpellFlatModByLabel currentMod;\nSpellFlatModByLabel previousMod;\nif (currentMod\
      \ != previousMod)\n{\n    // Apply new modifier logic\n    ApplySpellModifier(currentMod);\n\
      }"
    language: cpp
  notes: This operator is typically used in container comparisons or when checking
    if a spell modifier has changed during spell processing. The comparison may involve
    multiple internal fields depending on the implementation, including label identifiers
    and modifier values.
  warnings: null
  related:
  - operator==
  - operator<
  - operator>
  - operator<=
  - operator>=
metadata:
  confidence: 0.95
  generated_at: '2025-11-08T21:36:36.419482'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
