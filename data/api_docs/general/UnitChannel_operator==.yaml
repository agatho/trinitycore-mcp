api:
  class: UnitChannel
  method: operator==
  signature: bool UnitChannel::operator==(const UnitChannel & right) const
documentation:
  brief: Compares two UnitChannel objects for equality based on their internal state
    and properties.
  description: The operator== method performs a comparison between this UnitChannel
    instance and another UnitChannel object to determine if they are equivalent. In
    the context of TrinityCore's World of Warcraft emulation, UnitChannel typically
    represents an ongoing channeling spell or effect that a unit is performing. This
    comparison checks whether both objects represent the same channeling state, including
    the spell ID, caster, target, and other relevant properties. The method ensures
    that two channeling operations are considered equal only if they refer to the
    exact same spell being channeled by the same entity under identical conditions.
  parameters:
  - name: right
    description: The UnitChannel object to compare against this instance for equality.
  returns: Returns true if both UnitChannel objects represent the same channeling
    state; otherwise, returns false.
  examples:
  - title: Comparing Two Channeling States
    code: 'UnitChannel* channel1 = unit->GetChannel();

      UnitChannel* channel2 = anotherUnit->GetChannel();

      bool areEqual = (*channel1 == *channel2);

      // ''areEqual'' will be true if both units are channeling the same spell with
      identical parameters.'
    language: cpp
  notes: This operator likely compares internal identifiers such as spell ID, caster
    GUID, and target GUID to ensure accurate equality checks. It's important to note
    that even if two spells appear to be the same, they may not be equal if they are
    channeling from different sources or targets.
  warnings: null
  related:
  - operator!=
  - UnitChannel::GetSpellId
  - UnitChannel::GetCaster
  - UnitChannel::GetTarget
metadata:
  confidence: 0.9
  generated_at: '2025-11-08T20:59:51.761930'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
