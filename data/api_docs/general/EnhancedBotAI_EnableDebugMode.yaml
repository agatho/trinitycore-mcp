api:
  class: EnhancedBotAI
  method: EnableDebugMode
  signature: void EnhancedBotAI::EnableDebugMode(bool enable)
documentation:
  brief: Enables or disables debug mode for the EnhancedBotAI instance.
  description: The EnableDebugMode method toggles the debug state of an EnhancedBotAI
    instance. When enabled, this mode typically provides additional logging, verbose
    output, or enhanced diagnostic information to assist developers in troubleshooting
    bot behavior and AI logic. This functionality is commonly used during development
    and testing phases to gain deeper insights into how the bot interacts with the
    game environment and makes decisions. The method does not require any specific
    preconditions but may affect performance when enabled due to increased logging
    overhead.
  parameters:
  - name: enable
    description: A boolean value that determines whether debug mode should be enabled
      (true) or disabled (false).
  returns: null
  examples:
  - title: Enable Debug Mode
    code: 'EnhancedBotAI* bot = new EnhancedBotAI();

      bot->EnableDebugMode(true);'
    language: cpp
  - title: Disable Debug Mode
    code: 'EnhancedBotAI* bot = GetBotByGuid(botGuid);

      bot->EnableDebugMode(false);'
    language: cpp
  notes: Enabling debug mode may significantly increase CPU and memory usage due to
    additional logging operations. It is recommended to disable this feature in production
    environments or when performance is critical.
  warnings: null
  related:
  - IsDebugModeEnabled
  - ToggleDebugMode
metadata:
  confidence: 0.9
  generated_at: '2025-11-06T14:49:40.752929'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
