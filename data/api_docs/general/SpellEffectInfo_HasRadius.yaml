api:
  class: SpellEffectInfo
  method: HasRadius
  signature: bool SpellEffectInfo::HasRadius(int targetIndex) const
documentation:
  brief: Determines whether a spell effect has a radius parameter for the specified
    target index.
  description: The HasRadius method checks if a given spell effect possesses a defined
    radius for a specific target index. This is commonly used in spell processing
    to determine if area-of-effect mechanics should be applied. The method evaluates
    the spell effect's properties and returns true if a radius is set, false otherwise.
    In World of Warcraft's TrinityCore implementation, this helps determine how spells
    interact with multiple targets or areas, particularly for effects like AoE damage
    or healing. This check is essential for proper spell behavior validation before
    applying radius-based effects.
  parameters: []
  returns: Returns true if the spell effect has a radius defined for the specified
    target index; otherwise returns false.
  examples:
  - title: Basic Usage Check
    code: "SpellEffectInfo effect;\n// Assume effect is initialized with a spell\n\
      if (effect.HasRadius(0)) {\n    // Apply area-of-effect logic\n    printf(\"\
      Spell has radius for target 0\\n\");\n} else {\n    // Apply single-target logic\n\
      \    printf(\"Spell does not have radius for target 0\\n\");\n}"
    language: cpp
  notes: This method is typically used during spell validation and execution phases.
    It's important to note that the targetIndex parameter refers to which target in
    a multi-target spell the radius check applies to. The implementation relies on
    internal spell effect data structures and may vary based on spell attributes.
  warnings: null
  related:
  - SpellEffectInfo::GetRadius
  - SpellEffectInfo::HasTarget
metadata:
  confidence: 0.9
  generated_at: '2025-11-03T18:08:27.863730'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
