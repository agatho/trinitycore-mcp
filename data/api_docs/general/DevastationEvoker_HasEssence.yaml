api:
  class: DevastationEvoker
  method: HasEssence
  signature: bool DevastationEvoker::HasEssence(uint8 charges) const
documentation:
  brief: Checks if the DevastationEvoker has the specified number of essence charges
    available.
  description: The HasEssence method determines whether the DevastationEvoker instance
    possesses a minimum required number of essence charges. This functionality is
    typically used in game mechanics where evokers must consume or expend essence
    to cast spells or perform abilities. The method evaluates the internal charge
    count against the provided parameter and returns true if sufficient charges are
    present, otherwise false. It's commonly invoked before executing essence-dependent
    actions to ensure valid resource usage.
  parameters:
  - name: charges
    description: The number of essence charges required to be present for the method
      to return true. Must be a non-negative integer value representing the charge
      count.
  returns: Returns true if the DevastationEvoker has at least the specified number
    of essence charges; otherwise returns false.
  examples:
  - title: Basic Usage Check
    code: "if (evoker->HasEssence(3)) {\n    // Proceed with an ability that costs\
      \ 3 essence\n    evoker->CastSpell(SPELL_DEVASTATION_SPELL);\n}"
    language: cpp
  - title: Conditional Essence Consumption
    code: "uint8 requiredCharges = GetRequiredEssenceForAbility();\nif (evoker->HasEssence(requiredCharges))\
      \ {\n    evoker->ConsumeEssence(requiredCharges);\n    // Execute ability\n\
      } else {\n    // Handle insufficient essence case\n    SendErrorMessage(\"Not\
      \ enough essence!\");\n}"
    language: cpp
  notes: This method does not modify the internal state of the DevastationEvoker instance.
    It is a read-only check that simply compares the current essence charge count
    with the requested amount. The implementation likely accesses an internal member
    variable storing the essence charge value.
  warnings: null
  related:
  - GetEssenceCharges
  - ConsumeEssence
  - AddEssence
metadata:
  confidence: 0.9
  generated_at: '2025-11-08T18:31:04.150830'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
