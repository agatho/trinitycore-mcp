api:
  class: Aura
  method: CanPeriodicTickCrit
  signature: bool Aura::CanPeriodicTickCrit() const
documentation:
  brief: Determines whether a periodic aura effect can crit.
  description: The CanPeriodicTickCrit method checks if a periodic aura (such as a
    damage-over-time or healing-over-time effect) is capable of generating a critical
    hit. This method evaluates the aura's properties and the applicable spell mechanics
    to determine if critical strikes are possible for the periodic tick. It is used
    internally by the game engine to calculate damage or healing outcomes when an
    aura's periodic effect triggers.
  parameters: []
  returns: Returns true if the periodic aura effect can crit, false otherwise. This
    indicates whether a critical hit is possible for the current periodic tick of
    the aura.
  examples:
  - title: Check if Aura Can Crit
    code: "Aura* aura = player->GetAura(12345);\nif (aura && aura->CanPeriodicTickCrit())\
      \ {\n    // This aura's periodic tick can crit\n    // Perform additional logic\
      \ for critical hits\n}"
    language: cpp
  notes: This method typically relies on the aura's underlying spell data and the
    spell's critical strike chance properties. It does not account for real-time modifiers
    or temporary effects that might alter crit chances during a tick.
  warnings: Do not assume all periodic auras can crit; only certain spells and effects
    are configured to support critical hits on their periodic ticks. Misusing this
    method may lead to incorrect damage calculations.
  related:
  - Aura::GetSpellInfo
  - Aura::IsPeriodic
  - Aura::GetCritChance
metadata:
  confidence: 0.9
  generated_at: '2025-10-31T09:28:37.399703'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
