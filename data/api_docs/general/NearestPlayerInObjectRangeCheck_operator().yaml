api:
  class: NearestPlayerInObjectRangeCheck
  method: operator()
  signature: bool NearestPlayerInObjectRangeCheck::operator()(int * u)
documentation:
  brief: Evaluates whether a player is within the specified object range and returns
    true if so.
  description: The operator() method of NearestPlayerInObjectRangeCheck is used to
    determine if a player exists within a defined range of an object. This check is
    typically employed in AI logic or targeting systems where it's necessary to identify
    nearby players for actions such as casting spells, performing attacks, or triggering
    events. The method internally uses the stored range and object context to evaluate
    proximity against all players in the area. It returns true when at least one player
    meets the range criteria, otherwise false. This check is commonly used in creature
    AI behavior trees to make decisions based on player proximity.
  parameters: []
  returns: Returns a boolean value indicating whether any player is within the specified
    range of the object. True indicates that a player was found within range; false
    means no players were detected within the defined boundaries.
  examples:
  - title: Basic Usage in AI Decision Making
    code: "class MyCreatureAI : public CreatureAI\n{\npublic:\n    void UpdateAI(uint32\
      \ diff) override\n    {\n        if (NearestPlayerInObjectRangeCheck check(this,\
      \ 10.0f); check())\n        {\n            // Player is within 10 yards, perform\
      \ action\n            DoCast(SPELL_SHOOT);\n        }\n    }\n};"
    language: cpp
  notes: This method relies on the object's current position and the range parameter
    set during initialization. The check is performed against all players in the same
    map instance as the object. Performance is optimized by using spatial partitioning
    techniques within TrinityCore's core systems.
  warnings: Be cautious when using this check in high-frequency update loops, as it
    may impact performance if not properly cached or limited to specific conditions.
    Ensure that the range value is appropriate for your intended use case to avoid
    unintended targeting behavior.
  related:
  - NearestPlayerInObjectRangeCheck::NearestPlayerInObjectRangeCheck
  - NearestPlayerInObjectRangeCheck::SetRange
metadata:
  confidence: 0.9
  generated_at: '2025-11-08T23:14:50.223398'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
