api:
  class: BaselineRotationManager
  method: GetBaselineOptimalRange
  signature: float BaselineRotationManager::GetBaselineOptimalRange(int * bot)
documentation:
  brief: Calculates and returns the optimal attack range for a bot based on its baseline
    rotation settings.
  description: The GetBaselineOptimalRange method determines the ideal distance at
    which a bot should engage targets when using its baseline rotation strategy. This
    value is typically derived from the bot's class-specific mechanics, weapon type,
    and spell ranges. The method takes an optional pointer to a bot identifier and
    computes the optimal range accordingly. It serves as a core component in pathfinding
    and targeting logic for automated gameplay behavior.
  parameters:
  - name: bot
    description: Pointer to an integer representing the bot's unique identifier. If
      null, the method uses the default bot context.
  returns: A float value representing the optimal attack range in yards. Returns 0.0f
    if no valid range can be determined or if the input bot is invalid.
  examples:
  - title: Basic Usage
    code: float optimalRange = rotationManager.GetBaselineOptimalRange(nullptr);
    language: cpp
  - title: With Specific Bot Identifier
    code: 'int botId = 12345;

      float range = rotationManager.GetBaselineOptimalRange(&botId);'
    language: cpp
  notes: This method relies on internal bot configuration data and may vary depending
    on the class, equipped weapons, and active spells. The returned value should be
    used in conjunction with other range-checking functions for complete targeting
    logic.
  warnings: Ensure that the bot parameter is valid before calling this method to avoid
    undefined behavior or incorrect range calculations. This function does not validate
    whether the bot is currently in a valid state for combat.
  related:
  - GetBaselineRotation
  - SetBaselineOptimalRange
  - GetBotCombatRange
metadata:
  confidence: 0.85
  generated_at: '2025-11-01T18:06:51.741891'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
