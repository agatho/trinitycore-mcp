api:
  class: LfgUpdateData
  method: LfgUpdateData
  signature: void LfgUpdateData::LfgUpdateData(LfgUpdateType _type, LfgState _state,
    const LfgDungeonSet & _dungeons)
documentation:
  brief: Initializes a new LfgUpdateData object with specified update type, state,
    and dungeons.
  description: The LfgUpdateData constructor initializes a data structure used for
    handling Looking for Group (LFG) updates within the TrinityCore framework. This
    method sets up internal state variables including the update type which indicates
    the kind of LFG update being processed, the current LFG state representing the
    player's LFG status, and a collection of dungeons available for grouping. The
    constructor is typically used when creating LFG update notifications sent to players
    or when preparing data structures for LFG-related operations. This initialization
    is crucial for maintaining consistency in LFG system communications and ensuring
    proper handling of dungeon selection and group matching mechanics.
  parameters:
  - name: _type
    description: Specifies the type of LFG update being initialized. Valid values
      include LFG_UPDATETYPE_DEFAULT, LFG_UPDATETYPE_JOIN, LFG_UPDATETYPE_LEAVE, etc.,
      indicating whether this is a default update, join notification, or leave notification.
  - name: _state
    description: Represents the current LFG state of the player. Valid values include
      LFG_STATE_NONE, LFG_STATE_ROLECHECK, LFG_STATE_QUEUED, LFG_STATE_SUGGESTION,
      etc., indicating the player's position in the LFG workflow.
  - name: _dungeons
    description: A set containing the dungeons available for selection. This collection
      typically includes dungeon IDs and their properties that are valid for the current
      LFG operation or player context.
  returns: null
  examples:
  - title: Basic LfgUpdateData Initialization
    code: 'LfgUpdateData updateData(LFG_UPDATETYPE_DEFAULT, LFG_STATE_QUEUED, dungeonSet);

      // Initializes an update data structure for a queued player with default update
      type'
    language: cpp
  - title: Join Notification Update Data
    code: 'LfgDungeonSet dungeons;

      // ... populate dungeon set with valid dungeon IDs

      LfgUpdateData joinUpdate(LFG_UPDATETYPE_JOIN, LFG_STATE_SUGGESTION, dungeons);

      // Creates update data for a join notification in suggestion state'
    language: cpp
  notes: This constructor is part of the LFG system's internal data management and
    should only be called when preparing LFG update notifications. The dungeon set
    parameter must contain valid dungeon identifiers that exist in the game database.
    This class is typically used internally by the LFG manager and not directly by
    external modules.
  warnings: Incorrect initialization parameters may lead to inconsistent LFG behavior
    or client-side display issues. Ensure that dungeon sets contain only valid dungeon
    entries, and that update types match expected LFG workflow states to prevent system
    errors.
  related:
  - LfgUpdateData::GetType
  - LfgUpdateData::GetState
  - LfgUpdateData::GetDungeons
metadata:
  confidence: 0.95
  generated_at: '2025-11-04T18:35:27.874696'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
