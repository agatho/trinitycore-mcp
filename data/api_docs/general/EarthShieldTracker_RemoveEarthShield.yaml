api:
  class: EarthShieldTracker
  method: RemoveEarthShield
  signature: void EarthShieldTracker::RemoveEarthShield()
documentation:
  brief: Removes the earth shield effect from the target.
  description: The RemoveEarthShield method is responsible for eliminating the Earth
    Shield buff that has been applied to a target unit. This method typically gets
    invoked when the shield is broken, consumed, or when the caster's aura expires.
    It ensures proper cleanup of the shield state and may trigger related effects
    such as damage absorption resets or visual updates. The method does not take any
    parameters and operates on the internal state of the EarthShieldTracker instance.
  parameters: []
  returns: null
  examples:
  - title: Basic Usage in Spell Effect
    code: "void SpellEffectEarthShield(SpellEffIndex effIndex)\n{\n    if (Unit* target\
      \ = GetHitUnit())\n    {\n        if (EarthShieldTracker* tracker = target->GetEarthShieldTracker())\n\
      \        {\n            tracker->RemoveEarthShield();\n        }\n    }\n}"
    language: cpp
  notes: This method is usually called internally by the TrinityCore spell system
    when an Earth Shield aura is removed or expired. It's important to ensure that
    the target has an active Earth Shield before calling this method to avoid unintended
    behavior.
  warnings: null
  related:
  - GetEarthShield
  - ApplyEarthShield
  - HasEarthShield
metadata:
  confidence: 0.9
  generated_at: '2025-11-09T02:21:10.184564'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
