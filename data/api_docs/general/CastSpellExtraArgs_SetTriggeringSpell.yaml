api:
  class: CastSpellExtraArgs
  method: SetTriggeringSpell
  signature: CastSpellExtraArgs & CastSpellExtraArgs::SetTriggeringSpell(const Spell
    * triggeringSpell)
documentation:
  brief: Sets the triggering spell for a spell cast extra arguments object.
  description: The SetTriggeringSpell method assigns a Spell object to the CastSpellExtraArgs
    instance, which represents the spell that initiated or triggered the current spell
    cast. This is commonly used in spell systems where one spell triggers another,
    such as in combo point generation, proc effects, or spell linking. The method
    returns a reference to the same CastSpellExtraArgs object to allow for method
    chaining. This functionality is essential for maintaining proper spell context
    and ensuring correct behavior in complex spell interactions within TrinityCore's
    spell system.
  parameters: []
  returns: A reference to the CastSpellExtraArgs object, enabling method chaining.
  examples:
  - title: Basic Usage
    code: 'Spell* triggeringSpell = ...; // Some existing spell

      CastSpellExtraArgs args;

      args.SetTriggeringSpell(triggeringSpell);'
    language: cpp
  - title: Method Chaining
    code: 'Spell* triggeringSpell = ...;

      Spell* targetSpell = ...;

      CastSpellExtraArgs args;

      args.SetTriggeringSpell(triggeringSpell).SetTarget(targetSpell);'
    language: cpp
  notes: This method is typically used in spell handling code where spells need to
    track their origin or triggering mechanism. The Spell pointer passed should be
    valid and not null, as invalid pointers may lead to undefined behavior during
    spell execution.
  warnings: Passing a null Spell pointer to this method will likely result in crashes
    or undefined behavior during spell processing. Always ensure the Spell object
    is properly initialized before calling SetTriggeringSpell.
  related:
  - GetTriggeringSpell
  - SetTarget
  - SetCastItem
metadata:
  confidence: 0.95
  generated_at: '2025-11-04T12:15:44.580107'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
