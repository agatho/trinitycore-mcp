api:
  class: DispelCoordinator
  method: GetDispelSpell
  signature: uint32 DispelCoordinator::GetDispelSpell(int dispelType) const
documentation:
  brief: Retrieves the spell ID associated with a specific dispel type from the DispelCoordinator.
  description: The GetDispelSpell method retrieves the unique spell identifier (spell
    ID) that corresponds to a given dispel type. This method is typically used within
    the context of World of Warcraft's spell system, particularly when handling magical
    dispels such as removing curses, poisons, diseases, or magic effects from targets.
    It allows the game engine to determine which specific spell should be applied
    when a dispel action occurs based on the type of effect being removed. The method
    is read-only and does not modify any internal state of the DispelCoordinator object.
  parameters: []
  returns: Returns a uint32 representing the spell ID associated with the specified
    dispel type. If no matching dispel type exists, the method may return 0 or an
    invalid spell ID, depending on implementation details.
  examples:
  - title: Retrieve Spell ID for Magic Dispel
    code: 'uint32 magicDispelSpellId = dispelCoordinator.GetDispelSpell(DISPEL_MAGIC);

      // Returns the spell ID used for dispelling magic effects'
    language: cpp
  - title: Check if Dispel Type Has Valid Spell ID
    code: "uint32 poisonDispelId = dispelCoordinator.GetDispelSpell(DISPEL_POISON);\n\
      if (poisonDispelId != 0)\n{\n    // Proceed with dispel logic using the returned\
      \ spell ID\n    Spell* spell = sSpellMgr->GetSpellInfo(poisonDispelId);\n}"
    language: cpp
  notes: This method likely relies on internal mappings between dispel types and spell
    IDs, which are defined in TrinityCore's spell configuration files or databases.
    It is important to note that the dispel type parameter should correspond to predefined
    constants such as DISPEL_MAGIC, DISPEL_CURSE, DISPEL_DISEASE, etc., as used throughout
    the TrinityCore codebase.
  warnings: Ensure that the dispelType parameter passed to this method is a valid
    enum value recognized by the DispelCoordinator. Passing an invalid or unexpected
    value may result in returning an incorrect or zero spell ID, potentially causing
    undefined behavior during spell application.
  related:
  - GetDispelType
  - IsDispelTypeValid
  - AddDispelSpell
metadata:
  confidence: 0.9
  generated_at: '2025-11-04T09:39:52.915216'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
