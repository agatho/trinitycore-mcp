api:
  class: PositionUpdateInfo
  method: Reset
  signature: void PositionUpdateInfo::Reset()
documentation:
  brief: Resets all position update information to its default state
  description: The Reset method clears and initializes all internal position update
    data members within the PositionUpdateInfo class. This method is typically called
    when a unit's position needs to be fully reset, such as during object creation,
    teleportation, or when preparing for a new movement update cycle. It ensures that
    all previous position-related data is cleared and that the structure is in a clean
    state for subsequent updates.
  parameters: []
  returns: null
  examples:
  - title: Basic Reset Usage
    code: 'PositionUpdateInfo updateInfo;

      updateInfo.Reset(); // Clears all position update data'
    language: cpp
  - title: Reset Before Movement Update
    code: 'Unit* unit = ...;

      PositionUpdateInfo& posInfo = unit->GetPositionUpdateInfo();

      posInfo.Reset(); // Prepare for new movement data

      unit->UpdatePosition(...);'
    language: cpp
  notes: This method should be called before populating position update information
    to ensure no stale data remains. It's commonly used in conjunction with movement
    handlers and object initialization routines.
  warnings: null
  related:
  - GetPositionUpdateInfo
  - UpdatePosition
  - SetPosition
metadata:
  confidence: 0.9
  generated_at: '2025-11-08T22:49:13.350245'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
