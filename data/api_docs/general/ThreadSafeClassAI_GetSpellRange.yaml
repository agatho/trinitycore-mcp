api:
  class: ThreadSafeClassAI
  method: GetSpellRange
  signature: float ThreadSafeClassAI::GetSpellRange(uint32 spellId) const
documentation:
  brief: Retrieves the range of a specified spell for use in AI decision-making.
  description: The GetSpellRange method retrieves the maximum casting range for a
    given spell ID, which is used by the AI system to determine whether a target is
    within reach for spell casting. This value is typically defined in the spell database
    and represents the distance in yards from the caster to the target. The method
    is thread-safe and can be called from any thread context. It's commonly used during
    AI behavior calculations to evaluate spell viability based on distance constraints.
  parameters: []
  returns: Returns a float value representing the maximum range of the specified spell
    in yards. If the spell ID is invalid or not found, the method may return 0.0f
    or a default range value depending on implementation.
  examples:
  - title: Basic usage to check spell range
    code: "float range = ai->GetSpellRange(12345);\nif (range > 0.0f)\n{\n    // Spell\
      \ has a valid range, proceed with AI logic\n    if (targetDistance <= range)\n\
      \    {\n        // Target is within spell range\n    }\n}"
    language: cpp
  - title: Using spell range in AI decision making
    code: "uint32 fireballSpell = 133;\nfloat fireballRange = ai->GetSpellRange(fireballSpell);\n\
      if (fireballRange > 0.0f && targetDistance <= fireballRange)\n{\n    // Cast\
      \ fireball if target is within range\n    ai->CastSpell(target, fireballSpell);\n\
      }\nelse\n{\n    // Move closer or choose different spell\n    ai->MoveTo(target,\
      \ fireballRange * 0.8f);\n}"
    language: cpp
  notes: This method accesses spell data from the global spell database and should
    be used carefully in performance-critical AI loops. The returned value is cached
    internally for performance reasons. Since this is a const method, it does not
    modify any internal state of the AI object.
  warnings: Do not assume that all spell IDs will return valid ranges; invalid spell
    IDs may return 0.0f or default values. This can cause incorrect AI behavior if
    not properly handled in calling code.
  related:
  - GetSpellCooldown
  - IsSpellReady
  - CastSpell
metadata:
  confidence: 0.9
  generated_at: '2025-11-09T04:40:41.087520'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
