api:
  class: Player
  method: AddIllusionFlag
  signature: void Player::AddIllusionFlag(uint32 slot, uint32 flag)
documentation:
  brief: Adds an illusion flag to a specified slot in the player's inventory.
  description: This method adds an illusion flag to a specific slot in the player's
    inventory, allowing the player to apply temporary enchantment effects. The method
    operates on the player's item slots and is typically used for managing enchantment
    flags within the game mechanics. It requires valid slot and flag parameters to
    function correctly.
  parameters:
  - name: slot
    description: The inventory slot where the illusion flag should be added. Valid
      values are typically item slots such as head, chest, legs, etc., with specific
      ranges depending on the game version.
  - name: flag
    description: The illusion flag value to add to the specified slot. This represents
      a unique identifier for the enchantment effect, typically used in conjunction
      with other enchantment-related methods.
  returns: null
  examples:
  - title: Basic Usage Example
    code: 'Player* player = GetPlayer();

      player->AddIllusionFlag(EQUIPMENT_SLOT_HEAD, 12345);

      // Adds illusion flag 12345 to head slot'
    language: cpp
  - title: Advanced Usage Example
    code: 'Player* player = GetPlayer();

      uint32 slot = EQUIPMENT_SLOT_HEAD;

      uint32 flag = 67890;

      player->AddIllusionFlag(slot, flag);

      // Adds illusion flag to head slot with specific flag value'
    language: cpp
  notes: This method is typically used in conjunction with other enchantment-related
    methods. The implementation may vary depending on the game version and player's
    inventory state. It should be used carefully as it affects item enchantment mechanics.
  warnings: Improper use of this method can lead to incorrect enchantment application
    or invalid slot values. Care should be taken when using this method with different
    slot types.
  related:
  - AddEnchant
  - RemoveEnchant
  - GetItemBySlot
metadata:
  confidence: 0.85
  generated_at: '2025-10-31T01:34:38.354319'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
