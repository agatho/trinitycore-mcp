api:
  class: Creature
  method: GetRespawnCompatibilityMode
  signature: bool Creature::GetRespawnCompatibilityMode() const
documentation:
  brief: Returns whether the creature is in respawn compatibility mode, allowing for
    proper respawn behavior in World of Warcraft.
  description: This method checks if the creature is currently operating under respawn
    compatibility mode, which affects how the creature's respawn behavior is handled
    in the game world. When enabled, this mode ensures that creatures follow specific
    respawn rules and mechanics that are compatible with the server's respawn system.
    The method is typically used to determine if a creature should adhere to standard
    respawn behavior or if it needs special handling for respawn compatibility. This
    functionality is essential for maintaining proper game balance and ensuring that
    creatures behave correctly during server restarts or when respawn conditions change.
  parameters: []
  returns: Returns true if the creature is in respawn compatibility mode, false otherwise.
    The value indicates whether the creature's respawn behavior follows compatibility
    rules rather than standard respawn mechanics.
  examples:
  - title: Basic Usage Example
    code: "Creature* creature = ...;\nif (creature->GetRespawnCompatibilityMode())\
      \ {\n    // Creature is in respawn compatibility mode\n}\nelse {\n    // Creature\
      \ is not in respawn compatibility mode\n}"
    language: cpp
  notes: This method is typically used internally by TrinityCore's respawn system
    to determine if a creature should follow specific respawn rules. It may be affected
    by server configuration settings and should be used carefully when modifying respawn
    behavior.
  warnings: This method should not be used directly in most cases, as it's primarily
    intended for internal use within the TrinityCore framework. Using this method
    without understanding its implications could lead to incorrect respawn behavior
    or game mechanics issues.
  related:
  - GetRespawnTime
  - SetRespawnCompatibilityMode
  - IsRespawnCompatible
metadata:
  confidence: 0.95
  generated_at: '2025-10-30T19:11:37.145289'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
