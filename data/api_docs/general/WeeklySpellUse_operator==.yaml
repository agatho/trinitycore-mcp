api:
  class: WeeklySpellUse
  method: operator==
  signature: bool WeeklySpellUse::operator==(const WeeklySpellUse & right) const
documentation:
  brief: Compares two WeeklySpellUse objects for equality based on their internal
    data members.
  description: The operator== method performs a component-wise comparison between
    this WeeklySpellUse object and another WeeklySpellUse object provided as a parameter.
    It evaluates whether both objects contain identical data, typically used to determine
    if two weekly spell usage records represent the same spell with the same usage
    statistics. This comparison is essential in managing player spell cooldowns and
    tracking weekly spell usage within the TrinityCore framework. The method does
    not modify either object's state and returns true only when all relevant fields
    match exactly.
  parameters:
  - name: right
    description: A const reference to another WeeklySpellUse object that this instance
      will be compared against for equality.
  returns: Returns a boolean value indicating whether the current WeeklySpellUse object
    is equal to the provided 'right' WeeklySpellUse object. True if all data members
    are identical; false otherwise.
  examples:
  - title: Basic Equality Check
    code: 'WeeklySpellUse spell1;

      WeeklySpellUse spell2;

      bool isEqual = (spell1 == spell2); // Returns true if both objects have same
      data

      '
    language: cpp
  - title: Usage in Container Comparison
    code: 'std::vector<WeeklySpellUse> weeklySpells;

      WeeklySpellUse targetSpell;

      // ... populate weeklySpells ...

      bool found = std::find(weeklySpells.begin(), weeklySpells.end(), targetSpell)
      != weeklySpells.end();

      '
    language: cpp
  notes: This method likely compares internal fields such as spell ID, usage count,
    and time stamps. It's commonly used in containers or when checking for duplicate
    entries in player spell tracking systems.
  warnings: null
  related:
  - operator!=
  - WeeklySpellUse::operator<
  - WeeklySpellUse::operator>
metadata:
  confidence: 0.95
  generated_at: '2025-11-08T20:43:24.280195'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
