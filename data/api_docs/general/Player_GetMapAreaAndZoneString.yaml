api:
  class: Player
  method: GetMapAreaAndZoneString
  signature: std::string Player::GetMapAreaAndZoneString() const
documentation:
  brief: Returns the map and zone information string for the player's current location
  description: This method retrieves the map area and zone string representation for
    the player's current position in the game world. It combines the map identifier
    with the zone identifier to provide a unique location identifier that can be used
    for various game mechanics purposes such as tracking player locations, determining
    quest areas, or identifying specific zones within the game world. The returned
    string typically follows the format 'MapID-ZoneID' and is useful for debugging,
    logging, or when working with location-based systems.
  parameters: []
  returns: Returns a std::string containing the map area and zone identifier in the
    format 'MapID-ZoneID'. The string represents the current player's location within
    the game world, where MapID refers to the map identifier and ZoneID refers to
    the zone identifier. This value is used for various game mechanics including tracking
    player locations, determining quest areas, or identifying specific zones.
  examples:
  - title: Basic usage example
    code: 'Player* player = GetPlayer();

      std::string locationString = player->GetMapAreaAndZoneString();

      std::cout << "Current location: " << locationString << std::endl;'
    language: cpp
  notes: This method is primarily used for debugging purposes and when working with
    location-based systems. It returns a string that represents the current player's
    map and zone identifiers, which can be useful for tracking player movement or
    determining where quests should be completed.
  warnings: null
  related:
  - GetMapId
  - GetZoneId
  - GetAreaId
  - GetMapAreaAndZoneString
metadata:
  confidence: 0.9
  generated_at: '2025-10-30T16:50:09.576657'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
