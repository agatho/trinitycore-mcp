api:
  class: Channel
  method: IsLFG
  signature: bool Channel::IsLFG() const
documentation:
  brief: Determines whether the channel is designated as a Looking for Group (LFG)
    channel.
  description: The IsLFG method checks if the current channel has been marked as a
    Looking for Group channel. In World of Warcraft's TrinityCore implementation,
    LFG channels are typically used for players to find group members for dungeons,
    raids, or other activities. This method returns true if the channel is specifically
    configured for LFG purposes, and false otherwise. The determination is based on
    internal channel flags or properties that indicate its purpose within the game's
    grouping system.
  parameters: []
  returns: Returns true if the channel is an LFG channel; otherwise returns false.
  examples:
  - title: Check if a channel is LFG
    code: "Channel* myChannel = sChannelMgr->GetChannel(\"MyLFGChannel\", player);\n\
      if (myChannel && myChannel->IsLFG()) {\n    // This channel is used for Looking\
      \ for Group activities\n    ChatHandler(player->GetSession()).PSendSysMessage(\"\
      This is an LFG channel.\");\n}"
    language: cpp
  notes: This method relies on internal channel state management. It's typically used
    in conjunction with channel creation or modification logic to ensure proper categorization
    of channels for game mechanics such as group finding systems.
  warnings: null
  related:
  - Channel::SetLFG
  - Channel::IsBanned
  - Channel::IsOwner
metadata:
  confidence: 0.9
  generated_at: '2025-11-04T12:08:09.310409'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
