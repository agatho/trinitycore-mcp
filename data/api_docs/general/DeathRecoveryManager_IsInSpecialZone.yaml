api:
  class: DeathRecoveryManager
  method: IsInSpecialZone
  signature: bool DeathRecoveryManager::IsInSpecialZone() const
documentation:
  brief: Determines whether the player is located in a special zone where death recovery
    mechanics are active.
  description: The IsInSpecialZone method checks if the current player's location
    falls within a designated special zone that enables death recovery mechanics.
    These zones typically include areas such as sanctuaries, battlegrounds, or specific
    quest locations where players can recover from death without losing experience
    or items. The method evaluates the player's current position against internal
    zone definitions and returns true if the player is inside a valid special zone,
    false otherwise. This functionality is crucial for implementing game mechanics
    that protect players in certain areas during combat or while completing specific
    objectives.
  parameters: []
  returns: Returns true if the player is currently located within a special zone that
    activates death recovery mechanics; returns false if the player is outside such
    zones or if no special zone definitions exist for the current map.
  examples:
  - title: Basic Usage Check
    code: "if (deathRecoveryManager->IsInSpecialZone()) {\n    // Player is in a special\
      \ zone, enable death recovery\n    player->SetDeathRecoveryEnabled(true);\n\
      } else {\n    // Standard death mechanics apply\n    player->SetDeathRecoveryEnabled(false);\n\
      }"
    language: cpp
  - title: Integration with Death Handling
    code: "void Player::HandleDeath() {\n    if (deathRecoveryManager->IsInSpecialZone())\
      \ {\n        // Apply special death recovery logic\n        ApplyDeathRecovery();\n\
      \    } else {\n        // Standard death processing\n        ApplyStandardDeathPenalty();\n\
      \    }\n}"
    language: cpp
  notes: This method relies on the internal zone management system of TrinityCore
    and may require specific zone definitions to be properly configured in the database.
    The performance impact is minimal as it only involves coordinate comparisons against
    predefined zone boundaries.
  warnings: Ensure that special zones are properly defined in the game database; otherwise,
    this method may return incorrect results. The method should not be called during
    critical path operations where performance is paramount.
  related:
  - GetSpecialZoneList
  - IsDeathRecoveryEnabled
  - SetDeathRecoveryEnabled
metadata:
  confidence: 0.85
  generated_at: '2025-11-06T15:46:49.134238'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
