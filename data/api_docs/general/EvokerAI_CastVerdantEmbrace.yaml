api:
  class: EvokerAI
  method: CastVerdantEmbrace
  signature: void EvokerAI::CastVerdantEmbrace(int * target)
documentation:
  brief: Casts the Verdant Embrace spell on a target specified by the input parameter.
  description: The CastVerdantEmbrace method is responsible for executing the Verdant
    Embrace spell cast by an Evoker AI character. This spell typically provides healing
    or beneficial effects to the targeted unit. The method takes an integer pointer
    as input which represents the target unit's identifier. It performs necessary
    checks such as spell availability, target validity, and line of sight before initiating
    the spell cast. The method is part of the Evoker's core AI behavior system and
    integrates with the TrinityCore spell system for proper execution and cooldown
    management.
  parameters:
  - name: target
    description: Pointer to an integer representing the target unit's identifier (GUID).
      This parameter specifies which unit will receive the Verdant Embrace effect.
      The value should be a valid GameObject or Creature GUID, or 0 if no target is
      specified.
  returns: null
  examples:
  - title: Basic usage with valid target
    code: "void EvokerAI::CastVerdantEmbrace(int* target)\n{\n    if (target && *target\
      \ != 0)\n    {\n        Unit* unitTarget = ObjectAccessor::GetUnit(*m_owner,\
      \ *target);\n        if (unitTarget && unitTarget->IsAlive())\n        {\n \
      \           // Execute spell cast logic\n            m_owner->CastSpell(unitTarget,\
      \ VERDANT_EMBRACE_SPELL_ID, true);\n        }\n    }\n}"
    language: cpp
  notes: This method assumes that the Evoker AI has proper access to the owner unit
    and target validation logic. The spell ID VERDANT_EMBRACE_SPELL_ID should be defined
    in the TrinityCore spell database. Performance considerations include checking
    target validity before casting to avoid unnecessary operations.
  warnings: Ensure that the target pointer is not null and points to a valid GUID
    before attempting to cast the spell. Improper handling of the target parameter
    may result in invalid target errors or crashes.
  related:
  - EvokerAI::UpdateAI
  - EvokerAI::DoCastSpell
  - EvokerAI::SelectTarget
metadata:
  confidence: 0.85
  generated_at: '2025-11-08T05:07:16.547192'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
