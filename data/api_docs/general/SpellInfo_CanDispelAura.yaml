api:
  class: SpellInfo
  method: CanDispelAura
  signature: bool SpellInfo::CanDispelAura(const SpellInfo * auraSpellInfo) const
documentation:
  brief: Determines whether the current spell can dispel the specified aura.
  description: This method evaluates whether the current SpellInfo instance can dispel
    a given aura from the provided SpellInfo. It checks if the spell's properties
    allow it to remove specific aura types, such as magic effects or debuffs. The
    method is typically used in spell mechanics to determine if a spell can remove
    certain effects from targets. This functionality is crucial for determining spell
    effectiveness and compatibility with various game mechanics, including aura removal
    and dispel systems.
  parameters:
  - name: auraSpellInfo
    description: The SpellInfo of the aura to be checked for removal. This parameter
      represents the specific spell that generates the aura being evaluated for removal.
  returns: Returns true if the current spell can dispel the specified aura, false
    otherwise. The method returns false when the spell cannot remove the aura or when
    the aura type is incompatible with the current spell's properties.
  examples:
  - title: Basic usage example
    code: "if (spellInfo->CanDispelAura(auraSpellInfo)) {\n    // Spell can dispel\
      \ the aura\n    // Perform removal logic\n}\n"
    language: cpp
  - title: Advanced usage example
    code: "SpellInfo* spellInfo = sSpellMgr->GetSpellInfo(12345);\nSpellInfo* auraSpellInfo\
      \ = sSpellMgr->GetSpellInfo(67890);\nif (spellInfo->CanDispelAura(auraSpellInfo))\
      \ {\n    // Handle dispel logic\n    // Check if spell can remove specific aura\
      \ types\n}\n"
    language: cpp
  notes: This method is primarily used in spell handling and game mechanics for determining
    aura removal capabilities. It should be used carefully when evaluating spell effects,
    as it depends on the specific spell properties and aura types being evaluated.
  warnings: The method may return false if the spell does not have dispel properties
    or if the aura type is incompatible with the current spell's mechanics. Care must
    be taken when using this method in conjunction with other spell-related functions
    to ensure proper spell handling.
  related:
  - SpellInfo::CanDispel
  - SpellInfo::HasDispel
  - SpellInfo::IsDispellable
metadata:
  confidence: 0.9
  generated_at: '2025-10-30T14:14:56.864064'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
