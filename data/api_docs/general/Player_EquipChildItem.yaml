api:
  class: Player
  method: EquipChildItem
  signature: void Player::EquipChildItem(uint8 parentBag, uint8 parentSlot, Item *
    parentItem)
documentation:
  brief: Equips a child item into a specified parent bag and slot.
  description: This method handles the process of equipping a child item into a designated
    parent bag and slot, typically used when managing item relationships in the player's
    inventory. It ensures proper item positioning within the game's inventory system,
    allowing for correct item handling and display. The method is commonly invoked
    during item creation or when items are being moved between different inventory
    slots. This function is crucial for maintaining proper item state and ensuring
    that child items are correctly associated with their parent items.
  parameters:
  - name: parentBag
    description: The bag number where the parent item resides, typically ranging from
      0 to 11 for normal inventory bags.
  - name: parentSlot
    description: The slot within the parent bag where the parent item is located,
      ranging from 0 to 36 for standard inventory slots.
  - name: parentItem
    description: Pointer to the parent item object that will contain the child item.
      This item must be valid and exist in the player's inventory.
  returns: null
  examples:
  - title: Basic usage example
    code: 'Player* player = GetPlayer();

      Item* parentItem = player->GetItemByBagAndSlot(0, 0);

      player->EquipChildItem(0, 0, parentItem);'
    language: cpp
  notes: This method is typically called internally by the game engine during item
    management processes. It should be used carefully as it affects the item's position
    within the inventory system and may trigger updates to the player's UI or inventory
    state.
  warnings: Improper use of this method can lead to incorrect item positioning, potentially
    causing display issues or invalid item states in the player's inventory. Care
    must be taken when calling this method with invalid bag/slot combinations.
  related:
  - Player::GetItemByBagAndSlot
  - Player::SetInventoryItem
  - Player::EquipItem
metadata:
  confidence: 0.95
  generated_at: '2025-10-31T00:51:29.039145'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
