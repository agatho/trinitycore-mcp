api:
  class: AccountMgr
  method: ChangeEmail
  signature: AccountOpResult AccountMgr::ChangeEmail(uint32 accountId, std::string
    newEmail)
documentation:
  brief: Changes the email address associated with a specified account ID.
  description: The ChangeEmail method updates the email address stored for a given
    account in the database. This function is typically used by account management
    systems or administrative tools to allow players to update their registered email
    addresses. The method performs validation on the new email format and ensures
    that the account exists before attempting the update. It returns an AccountOpResult
    indicating success or failure of the operation, which can be used to provide appropriate
    feedback to the user or log the outcome for auditing purposes.
  parameters:
  - name: accountId
    description: The unique identifier of the account for which the email address
      is being changed. Must correspond to an existing account in the database.
  - name: newEmail
    description: The new email address to associate with the account. Should be a
      valid email format (e.g., user@example.com).
  returns: An AccountOpResult enum value indicating the outcome of the operation.
    Possible values include SUCCESS, FAILED, INVALID_EMAIL_FORMAT, ACCOUNT_NOT_FOUND,
    or other relevant error codes depending on implementation.
  examples:
  - title: Basic Email Change Example
    code: "AccountOpResult result = AccountMgr::ChangeEmail(12345, \"newemail@example.com\"\
      );\nif (result == AccountOpResult::SUCCESS) {\n    printf(\"Email updated successfully.\\\
      n\");\n} else {\n    printf(\"Failed to update email. Error code: %d\\n\", result);\n\
      }"
    language: cpp
  notes: This method likely performs database operations and should be used with caution
    in high-frequency scenarios. Ensure proper input validation before calling this
    function to prevent potential security issues such as SQL injection or malformed
    email addresses.
  warnings: Do not call this method without verifying that the accountId exists and
    that newEmail is properly formatted. Improper usage may lead to data inconsistency
    or database errors.
  related:
  - GetAccountEmail
  - ChangePassword
  - CreateAccount
metadata:
  confidence: 0.9
  generated_at: '2025-11-02T14:39:41.256383'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
