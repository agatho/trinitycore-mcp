api:
  class: AnyPlayerInPositionRangeCheck
  method: AnyPlayerInPositionRangeCheck
  signature: void AnyPlayerInPositionRangeCheck::AnyPlayerInPositionRangeCheck(const
    int * pos, float range, bool reqAlive)
documentation:
  brief: Initializes a position range check for players within a specified area.
  description: The AnyPlayerInPositionRangeCheck constructor initializes a check to
    determine if any player exists within a given positional range. This utility is
    commonly used in AI behavior scripting and zone-based event triggers within the
    TrinityCore framework. The class likely serves as a predicate or functor that
    can be used with container iteration methods to filter players based on their
    proximity to a defined point. It supports an optional alive status requirement,
    allowing for checks that only consider living players.
  parameters:
  - name: pos
    description: Pointer to an array of three integers representing the X, Y, and
      Z coordinates of the center point for range checking.
  - name: range
    description: Floating-point value defining the maximum distance from the center
      point within which players are considered to be in range.
  - name: reqAlive
    description: Boolean flag indicating whether only alive players should be considered
      (true) or all players including dead ones (false).
  returns: null
  examples:
  - title: Basic usage to check for nearby players
    code: 'int centerPos[3] = {100, 200, 300};

      AnyPlayerInPositionRangeCheck checker(centerPos, 50.0f, true);

      // Use checker with a container of players to find those in range'
    language: cpp
  - title: Checking for any player (alive or dead) within range
    code: 'int pos[3] = {-1500, 3000, 75};

      AnyPlayerInPositionRangeCheck check(pos, 20.0f, false);

      // This will include both alive and dead players in the check'
    language: cpp
  notes: This class is typically used as part of a larger system involving AI decision-making
    or event triggering based on player proximity. It's designed to be efficient for
    frequent checks, though performance may degrade with many players in close proximity.
    The implementation likely uses spatial partitioning or similar optimization techniques
    internally.
  warnings: Ensure that the pos array contains valid coordinate values; invalid coordinates
    can lead to unexpected behavior or crashes. Be cautious when using large ranges
    with many players, as this could impact server performance during frequent checks.
  related:
  - operator()
  - IsInRange
  - CheckPlayer
metadata:
  confidence: 0.85
  generated_at: '2025-11-08T13:34:24.876338'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
