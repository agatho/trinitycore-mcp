api:
  class: ItemPosCount
  method: isContainedIn
  signature: bool ItemPosCount::isContainedIn(const int & vec) const
documentation:
  brief: Checks if the item position count is contained within a given vector.
  description: The isContainedIn method determines whether the current ItemPosCount
    object's position and count values are contained within a specified vector. This
    method is typically used in inventory management systems to verify if an item
    can be placed or moved into a specific container or slot. The check involves validating
    that the item's position falls within the bounds of the provided vector, ensuring
    proper game mechanics for item placement and stacking.
  parameters:
  - name: vec
    description: Reference to an integer vector representing the container or slot
      range where the item position should be checked. This typically represents the
      maximum allowed position or size of a container.
  returns: Returns true if the item's position and count are within the bounds of
    the provided vector, false otherwise. A return value of true indicates that the
    item can be safely placed in the specified container without violating game rules.
  examples:
  - title: Basic usage to check item containment
    code: "ItemPosCount itemPos;\n// Assume itemPos has been initialized with position\
      \ and count\nint containerSize = 36; // Example: 36-slot backpack\nbool canContain\
      \ = itemPos.isContainedIn(containerSize);\nif (canContain) {\n    // Item can\
      \ be placed in the container\n}"
    language: cpp
  - title: Using with inventory slot validation
    code: 'ItemPosCount itemSlot;

      itemSlot.m_pos = 5; // Example position

      itemSlot.m_count = 2; // Example count

      int maxSlots = 10;

      bool isValid = itemSlot.isContainedIn(maxSlots);

      // isValid will be true if the item fits within the slot range'
    language: cpp
  notes: This method is commonly used in TrinityCore's inventory system to validate
    item placement before performing operations like item transfer, stacking, or container
    management. The implementation likely compares the item's position against the
    vector size to ensure no out-of-bounds access occurs.
  warnings: Ensure that the vector parameter represents a valid container size or
    maximum position limit. Passing an invalid or zero value may lead to incorrect
    containment checks and unexpected behavior in inventory operations.
  related:
  - ItemPosCount::isValid
  - ItemPosCount::isInBag
  - ItemPosCount::GetContainer
metadata:
  confidence: 0.85
  generated_at: '2025-11-09T00:20:57.740077'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
