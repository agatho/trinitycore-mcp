api:
  class: Player
  method: GetDataElementCharacter
  signature: std::variant<int64, float> Player::GetDataElementCharacter(uint32 dataElementId)
    const
documentation:
  brief: Retrieves the value of a specific data element from the player character.
  description: This method retrieves the value associated with a given data element
    ID from the player's character data. It returns either an int64 or float depending
    on the data element type, allowing for flexible access to various player attributes
    such as level, experience points, or stats. The method is commonly used in game
    mechanics to fetch specific player information without requiring direct access
    to internal data structures.
  parameters: []
  returns: Returns either an int64 or float value depending on the data element type.
    The value represents a specific attribute of the player character such as level,
    experience points, or stat values, and may vary based on the dataElementId parameter.
  examples:
  - title: Basic Usage Example
    code: int64 value = player->GetDataElementCharacter(123); // Retrieves a specific
      data element
    language: cpp
  notes: The method is thread-safe and can be used in both single-threaded and multi-threaded
    contexts. Performance considerations include avoiding frequent calls to this method
    for non-critical operations.
  warnings: Be cautious when using this method as it may return different types depending
    on the data element ID, potentially leading to type mismatches if not properly
    handled.
  related:
  - GetDataElementCharacter
  - GetPlayerData
metadata:
  confidence: 0.85
  generated_at: '2025-10-30T17:10:31.095081'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
