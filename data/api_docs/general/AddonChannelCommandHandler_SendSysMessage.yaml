api:
  class: AddonChannelCommandHandler
  method: SendSysMessage
  signature: void AddonChannelCommandHandler::SendSysMessage(int param0, bool escapeCharacters)
documentation:
  brief: Sends a system message to the player's addon channel with optional character
    escaping.
  description: The SendSysMessage method is used within the AddonChannelCommandHandler
    class to transmit a system message to the player's addon communication channel.
    This method allows for sending messages that are typically used by addons or command
    handlers to communicate status updates, errors, or informational content directly
    to the client. The escapeCharacters parameter controls whether special characters
    in the message should be escaped before transmission to prevent potential parsing
    issues on the client side. This functionality is essential for maintaining proper
    communication between server-side command handlers and client-side addons in TrinityCore's
    architecture.
  parameters:
  - name: param0
    description: The integer identifier or index used to determine which system message
      template to send. This parameter likely corresponds to a predefined message
      ID or index within the addon channel's message system.
  - name: escapeCharacters
    description: Boolean flag that determines whether special characters in the message
      content should be escaped before transmission. When true, characters like quotes
      and backslashes are escaped to prevent parsing errors on the client side.
  returns: void
  examples:
  - title: Basic System Message Sending
    code: 'AddonChannelCommandHandler handler;

      handler.SendSysMessage(1001, true); // Sends message ID 1001 with character
      escaping enabled'
    language: cpp
  - title: Sending Unescaped Message
    code: 'AddonChannelCommandHandler handler;

      handler.SendSysMessage(2005, false); // Sends message ID 2005 without escaping
      special characters'
    language: cpp
  notes: This method is typically used in command handler implementations where addons
    need to communicate status information back to players. The param0 parameter likely
    maps to internal message templates defined in the addon channel system. Ensure
    that the message ID exists in the system's template registry to avoid silent failures.
  warnings: Incorrect usage of the param0 value may result in undefined behavior or
    failed message delivery. When escapeCharacters is set to false, be cautious with
    special characters that might break client-side parsing of the message content.
  related:
  - SendSysMessage
  - SendChannelMessage
  - SendMessage
metadata:
  confidence: 0.85
  generated_at: '2025-11-02T14:15:09.025534'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
