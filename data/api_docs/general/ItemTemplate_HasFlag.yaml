api:
  class: ItemTemplate
  method: HasFlag
  signature: bool ItemTemplate::HasFlag(ItemFlagsCustom customFlag) const
documentation:
  brief: Checks if the item template has a specified custom flag set.
  description: The HasFlag method determines whether a specific custom flag is enabled
    for the item template. This method is commonly used in World of Warcraft's TrinityCore
    to evaluate item properties and behavior based on custom flags defined in the
    game's database or code. It allows developers to programmatically check if an
    item has certain special attributes or behaviors that are flagged during item
    creation or loading. The method performs a bitwise comparison against the item's
    stored custom flags, returning true if the specified flag is present and false
    otherwise.
  parameters: []
  returns: Returns true if the specified ItemFlagsCustom flag is set in the item template;
    otherwise returns false.
  examples:
  - title: Basic Flag Check
    code: "if (itemTemplate->HasFlag(ItemFlagsCustom::ITEM_FLAG_CRAFTED)) {\n    //\
      \ Handle crafted item logic\n    printf(\"Item is crafted\\n\");\n}"
    language: cpp
  - title: Conditional Item Behavior
    code: "if (itemTemplate->HasFlag(ItemFlagsCustom::ITEM_FLAG_NO_EQUIP_COOLDOWN))\
      \ {\n    // Apply special cooldown handling\n    player->SetCooldown(0, 0);\n\
      } else {\n    // Apply normal cooldown\n    player->SetCooldown(30000, 0);\n\
      }"
    language: cpp
  notes: This method relies on the internal flag storage mechanism of ItemTemplate.
    The custom flags are typically defined in the database or through code constants
    and should be carefully managed to avoid conflicts. Performance is generally good
    as it's a simple bitwise operation, but frequent calls should still be considered
    in performance-critical sections.
  warnings: Ensure that the ItemFlagsCustom enum values passed to this method are
    valid and correspond to actual defined flags. Using invalid flag values may lead
    to unexpected behavior or false results. Be aware that flag checking is case-sensitive
    and requires exact flag definitions.
  related:
  - GetFlags
  - SetFlag
  - HasFlag
metadata:
  confidence: 0.9
  generated_at: '2025-11-04T02:14:10.849083'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
