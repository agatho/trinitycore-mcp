api:
  class: ShamanAI
  method: IsTotemSpell
  signature: bool ShamanAI::IsTotemSpell(int spellId) const
documentation:
  brief: Determines whether a given spell ID corresponds to a totem spell used by
    a Shaman.
  description: The IsTotemSpell method checks if a specified spell ID represents a
    valid totem spell within the context of the Shaman class AI. This utility function
    is typically used during spell selection or execution logic to identify which
    spells should be handled as totem-related abilities. It evaluates the spell ID
    against known totem spell identifiers and returns true if the spell is recognized
    as a totem spell, false otherwise. This functionality is essential for proper
    Shaman AI behavior when managing totems such as Healing Stream Totem, Stoneclaw
    Totem, or other totem-based abilities in World of Warcraft's TrinityCore framework.
  parameters: []
  returns: Returns true if the provided spell ID corresponds to a known totem spell;
    otherwise returns false.
  examples:
  - title: Basic Usage
    code: "if (shamanAI->IsTotemSpell(58759)) {\n    // Handle totem spell logic\n\
      \    printf(\"This is a totem spell\\n\");\n}"
    language: cpp
  - title: Integration with Spell Casting Logic
    code: "void ShamanAI::OnSpellCast(int spellId) {\n    if (IsTotemSpell(spellId))\
      \ {\n        // Special handling for totem spells\n        HandleTotemSpell(spellId);\n\
      \    } else {\n        // Standard spell handling\n        HandleRegularSpell(spellId);\n\
      \    }\n}"
    language: cpp
  notes: This method likely relies on internal totem spell ID mappings or lookup tables
    maintained by the Shaman AI system. The implementation may cache results for performance
    optimization, especially since this check could be performed frequently during
    combat logic.
  warnings: Ensure that the spellId parameter is validated before calling this method
    to avoid unexpected behavior from invalid or out-of-range spell identifiers. Also
    note that this function only checks if a spell is recognized as a totem spell;
    it does not verify whether the spell can actually be cast by the current Shaman
    instance.
  related:
  - ShamanAI::GetTotemSpells
  - ShamanAI::IsHealingTotemSpell
  - ShamanAI::IsCombatTotemSpell
metadata:
  confidence: 0.9
  generated_at: '2025-11-07T10:02:50.252085'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
