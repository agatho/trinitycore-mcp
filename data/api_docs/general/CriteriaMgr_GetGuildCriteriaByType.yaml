api:
  class: CriteriaMgr
  method: GetGuildCriteriaByType
  signature: const CriteriaList & CriteriaMgr::GetGuildCriteriaByType(int type) const
documentation:
  brief: Retrieves a list of guild criteria associated with a specified type.
  description: The GetGuildCriteriaByType method fetches all guild criteria entries
    that match the given type identifier. This function is typically used within the
    context of World of Warcraft's achievement and criterion system, where criteria
    define specific conditions or requirements for earning achievements. The returned
    CriteriaList is a constant reference to ensure data integrity and prevent modification
    of the internal criteria collection. The method does not perform any validation
    on the input type parameter, so callers should ensure that the provided type corresponds
    to a valid guild criterion type defined in the game's database.
  parameters: []
  returns: A constant reference to a CriteriaList containing all guild criteria entries
    matching the specified type. The list may be empty if no criteria of the given
    type exist.
  examples:
  - title: Retrieve and iterate over guild criteria of a specific type
    code: "const CriteriaList& criteriaList = criteriaMgr.GetGuildCriteriaByType(123);\n\
      for (const auto& criterion : criteriaList) {\n    // Process each criterion\n\
      \    std::cout << \"Criterion ID: \" << criterion->ID << std::endl;\n}"
    language: cpp
  notes: This method returns a constant reference to an internal data structure, so
    modifications to the returned list will not affect the original criteria collection.
    The CriteriaList is expected to be populated during server initialization or when
    loading guild data from the database.
  warnings: Ensure that the type parameter passed to this method corresponds to a
    valid guild criterion type; invalid types may return empty lists without error.
    Since this returns a constant reference, avoid storing pointers or references
    to elements in the list beyond the scope of use.
  related:
  - GetCriteriaByType
  - GetGuildCriteria
  - LoadGuildCriteria
metadata:
  confidence: 0.9
  generated_at: '2025-11-01T15:26:49.746957'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
