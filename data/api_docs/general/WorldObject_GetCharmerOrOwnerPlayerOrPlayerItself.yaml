api:
  class: WorldObject
  method: GetCharmerOrOwnerPlayerOrPlayerItself
  signature: Player * WorldObject::GetCharmerOrOwnerPlayerOrPlayerItself() const
documentation:
  brief: Returns the player who owns or controls the object, or the player itself
    if no owner exists.
  description: This method retrieves the player who is either the owner of the WorldObject
    or the charmer of the object. If the object is not owned by a player and has no
    charmer, it returns the player itself. This method is commonly used in game mechanics
    to determine the player who controls an object such as a pet or vehicle. The method
    handles both direct ownership and charmer relationships, making it useful for
    determining control of various game entities.
  parameters: []
  returns: Returns a pointer to the Player object that owns or controls this WorldObject,
    or nullptr if no such player exists. If the object is not owned by a player but
    has a charmer, the charmer's player is returned. If the object itself is a player,
    it returns itself.
  examples:
  - title: Basic usage example
    code: "Player* owner = this->GetCharmerOrOwnerPlayerOrPlayerItself();\nif (owner)\n\
      {\n    // Do something with the owner\n    std::cout << \"Owner: \" << owner->GetName()\
      \ << std::endl;\n}\nelse\n{\n    // No owner found\n    std::cout << \"No owner\
      \ found\" << std::endl;\n}"
    language: cpp
  notes: This method is commonly used in pet AI logic and vehicle control scenarios.
    It should be used carefully as it may return nullptr if no player controls the
    object, which can lead to null pointer dereferences if not handled properly.
  warnings: Be cautious when using this method as it may return nullptr if no player
    owns or controls the object. Always check for null before using the returned pointer
    to avoid crashes.
  related:
  - GetCharmerOrOwner
  - GetOwner
  - GetPlayer
metadata:
  confidence: 0.95
  generated_at: '2025-10-30T00:36:13.862716'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
