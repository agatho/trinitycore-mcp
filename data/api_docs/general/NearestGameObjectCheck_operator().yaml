api:
  class: NearestGameObjectCheck
  method: operator()
  signature: bool NearestGameObjectCheck::operator()(int * go)
documentation:
  brief: Checks if a game object is the nearest valid target for the current context
  description: The NearestGameObjectCheck::operator() method evaluates whether a given
    game object meets the criteria for being the closest valid target within the current
    search scope. This method is typically used in AI behavior trees or target selection
    logic to determine if an object should be considered as the nearest valid target.
    The method operates on a pointer to an integer representing the game object's
    identifier and returns true if the object satisfies the nearest target conditions,
    false otherwise. It's commonly used in TrinityCore's creature AI systems for selecting
    appropriate targets during combat or interaction scenarios.
  parameters: []
  returns: Returns true if the game object pointed to by 'go' is the nearest valid
    target according to the check criteria; returns false otherwise. The method may
    also return false if the object pointer is invalid or if the object does not meet
    the required conditions for being considered a valid target.
  examples:
  - title: Basic Usage in AI Target Selection
    code: "class MyCreatureAI : public CreatureAI {\npublic:\n    bool UpdateAI(uint32\
      \ diff) override {\n        if (Unit* target = SelectTarget(SELECT_TARGET_NEAREST,\
      \ 0, 100.0f, true)) {\n            // Use NearestGameObjectCheck to validate\
      \ the target\n            NearestGameObjectCheck check;\n            if (check(&target->GetGUID().GetRawValue()))\
      \ {\n                // Proceed with attack logic\n                AttackStart(target);\n\
      \            }\n        }\n        return true;\n    }\n};"
    language: cpp
  notes: This method is part of TrinityCore's internal AI target selection system.
    It is designed to work in conjunction with other target selection utilities and
    should not be called directly without proper context setup. The operator() method
    typically performs checks related to object validity, distance, and visibility
    conditions.
  warnings: The method expects a valid pointer to an integer representing a game object's
    GUID. Passing null or invalid pointers may result in undefined behavior. This
    method is intended for internal use within AI systems and should not be used as
    a standalone utility without proper initialization of the check context.
  related:
  - SelectTarget
  - IsValidTarget
  - IsInRange
metadata:
  confidence: 0.85
  generated_at: '2025-11-08T23:17:40.106670'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
