api:
  class: BotTalentManager
  method: GetStats
  signature: TalentStats BotTalentManager::GetStats() const
documentation:
  brief: Retrieves the current talent statistics for the bot.
  description: The GetStats method returns a TalentStats structure containing detailed
    information about the bot's current talent configuration. This includes data such
    as the number of points invested in each talent, the total spent, and other relevant
    metrics that define the bot's specialization and build. The returned statistics
    are typically used for display purposes, decision making in AI behavior, or synchronization
    with client-side UI elements. This method does not modify any internal state and
    is safe to call multiple times.
  parameters: []
  returns: A TalentStats object containing all relevant talent data for the bot, including
    invested points, total spent, and other statistical information related to the
    bot's current talent tree configuration.
  examples:
  - title: Basic Usage
    code: 'TalentStats stats = botTalentManager.GetStats();

      uint32 totalPoints = stats.totalSpent;

      uint32 investedPoints = stats.talentPoints[0]; // First talent''s points'
    language: cpp
  - title: Display Talent Information
    code: "TalentStats stats = botTalentManager.GetStats();\nstd::cout << \"Total\
      \ talent points spent: \" << stats.totalSpent << std::endl;\nfor (int i = 0;\
      \ i < MAX_TALENTS; ++i)\n{\n    if (stats.talentPoints[i] > 0)\n        std::cout\
      \ << \"Talent \" << i << \": \" << stats.talentPoints[i] << \" points\" << std::endl;\n\
      }"
    language: cpp
  notes: This method is read-only and does not perform any calculations or updates.
    It returns a snapshot of the current talent state at the time of invocation. The
    returned TalentStats object should be treated as immutable for external use.
  warnings: null
  related:
  - SetTalentPoints
  - ResetTalents
  - GetTalentPointCount
metadata:
  confidence: 0.9
  generated_at: '2025-11-01T16:49:37.782143'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
