api:
  class: ItemTemplate
  method: IsCurrencyToken
  signature: bool ItemTemplate::IsCurrencyToken() const
documentation:
  brief: Determines whether the item template represents a currency token used in
    World of Warcraft's economy system.
  description: The IsCurrencyToken method checks if the current item template is classified
    as a currency token within the TrinityCore database. Currency tokens are special
    items that represent in-game currencies such as Honor Points, Arena Points, or
    other tradable resources. This method returns true if the item has the appropriate
    flags or entry type that designate it as a currency token, and false otherwise.
    It is commonly used during item processing, trading, and auction house operations
    to distinguish between regular items and currency tokens.
  parameters: []
  returns: Returns true if the item template represents a currency token; otherwise
    returns false.
  examples:
  - title: Check if an item is a currency token
    code: "ItemTemplate* itemTemplate = sObjectMgr->GetItemTemplate(12345);\nif (itemTemplate\
      \ && itemTemplate->IsCurrencyToken()) {\n    printf(\"This item is a currency\
      \ token.\\n\");\n} else {\n    printf(\"This item is not a currency token.\\\
      n\");\n}"
    language: cpp
  notes: This method relies on internal flags or database entries that define an item
    as a currency token. It does not perform any external lookups or complex logic,
    making it efficient for frequent checks during gameplay operations like trading
    or auction processing.
  warnings: null
  related:
  - GetItemId
  - GetItemType
  - IsGem
  - IsWeapon
metadata:
  confidence: 0.95
  generated_at: '2025-11-04T02:10:20.688949'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
