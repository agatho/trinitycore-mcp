api:
  class: ObjectMgr
  method: GetPlayerChoiceLocale
  signature: const PlayerChoiceLocale * ObjectMgr::GetPlayerChoiceLocale(int32 ChoiceID)
    const
documentation:
  brief: Retrieves the locale-specific data for a player choice by its unique identifier.
  description: The GetPlayerChoiceLocale method fetches the localized data associated
    with a specific player choice identified by its ChoiceID. This method is used
    in the context of player choices, which are typically part of quest systems or
    interactive dialogues within World of Warcraft. It returns a pointer to a PlayerChoiceLocale
    structure that contains locale-specific text and options for the given choice
    ID. The method performs a lookup in an internal data structure that maps choice
    IDs to their respective locale data. If no matching choice is found, it returns
    a null pointer.
  parameters: []
  returns: A const pointer to a PlayerChoiceLocale structure containing the locale-specific
    data for the specified ChoiceID. Returns nullptr if no player choice with the
    given ID exists in the database.
  examples:
  - title: Basic Usage
    code: "const PlayerChoiceLocale* choiceLocale = sObjectMgr->GetPlayerChoiceLocale(12345);\n\
      if (choiceLocale)\n{\n    // Process the locale data\n    printf(\"Choice text:\
      \ %s\\n\", choiceLocale->ChoiceText.c_str());\n}"
    language: cpp
  notes: This method accesses internal game data structures and should only be called
    after the object manager has been initialized with player choice data. The returned
    pointer is valid only as long as the ObjectMgr instance remains alive and the
    data has not been modified.
  warnings: Calling this method with an invalid ChoiceID will return a null pointer,
    which must be checked before dereferencing. Improper handling of the returned
    pointer can lead to segmentation faults or undefined behavior.
  related:
  - GetPlayerChoice
  - LoadPlayerChoices
  - GetPlayerChoiceReward
metadata:
  confidence: 0.9
  generated_at: '2025-11-01T09:23:24.657501'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
