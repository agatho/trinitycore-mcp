api:
  class: WorldSession
  method: HandleChatUpdateAADCStatus
  signature: void WorldSession::HandleChatUpdateAADCStatus(const WorldPackets::Chat::UpdateAADCStatus
    & updateAADCStatus)
documentation:
  brief: Updates the AAD (Always Active Display) status for the player's chat channels
  description: The HandleChatUpdateAADCStatus method processes incoming client requests
    to modify the Always Active Display status of chat channels. This functionality
    allows players to control whether certain chat channels remain visible at all
    times, regardless of other display settings or UI states. The method typically
    handles synchronization between the client's local chat channel preferences and
    the server's authoritative state. It may also trigger updates to the player's
    chat channel visibility settings across the game world.
  parameters: []
  returns: null
  examples:
  - title: Basic usage in packet handling
    code: "void WorldSession::HandleChatUpdateAADCStatus(const WorldPackets::Chat::UpdateAADCStatus&\
      \ updateAADCStatus)\n{\n    // Process the AAD status update from client\n \
      \   _player->GetChatMgr()->SetChannelAADCStatus(updateAADCStatus.ChannelId,\
      \ updateAADCStatus.IsEnabled);\n}"
    language: cpp
  notes: This method is part of the chat system's internal packet handling and should
    only be called as a result of receiving a valid UpdateAADCStatus packet from a
    client. The implementation likely involves updating in-memory channel state and
    potentially broadcasting changes to other players in the same channel.
  warnings: null
  related:
  - HandleChatMessage
  - HandleChannelJoin
  - HandleChannelLeave
metadata:
  confidence: 0.85
  generated_at: '2025-11-01T19:23:45.083343'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
