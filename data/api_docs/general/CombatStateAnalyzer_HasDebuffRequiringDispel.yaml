api:
  class: CombatStateAnalyzer
  method: HasDebuffRequiringDispel
  signature: bool CombatStateAnalyzer::HasDebuffRequiringDispel() const
documentation:
  brief: Determines whether the current combat state includes any debuffs that require
    dispelling.
  description: The HasDebuffRequiringDispel method evaluates the active debuffs on
    the target entity within the combat state and checks if any of them are classified
    as requiring a dispel effect. This is commonly used in AI decision-making logic
    to determine when to apply dispel spells or abilities. The method typically examines
    the debuff type, duration, and dispel resistance properties to make this determination.
    It's an essential part of intelligent combat behavior in TrinityCore's AI system.
  parameters: []
  returns: Returns true if at least one active debuff on the target requires a dispel
    effect; otherwise returns false.
  examples:
  - title: Basic Usage in Combat AI
    code: "if (analyzer.HasDebuffRequiringDispel()) {\n    // Apply dispel spell\n\
      \    caster.CastSpell(target, DISPEL_SPELL_ID);\n}"
    language: cpp
  - title: Conditional Dispel Logic
    code: "CombatStateAnalyzer analyzer = CombatStateAnalyzer(unit);\nif (analyzer.HasDebuffRequiringDispel())\
      \ {\n    // Check if dispel is available\n    if (caster.HasSpell(DISPEL_SPELL_ID))\
      \ {\n        caster.CastSpell(target, DISPEL_SPELL_ID);\n    }\n}"
    language: cpp
  notes: This method likely performs a lookup in the target's aura state or debuff
    list and filters for specific dispel types such as magic, curse, disease, or poison.
    Performance is generally low impact as it only checks existing debuffs rather
    than recalculating them.
  warnings: Ensure that the CombatStateAnalyzer instance is properly initialized with
    a valid unit reference before calling this method, otherwise behavior may be undefined.
    This method does not account for temporary or conditional dispel requirements
    that might depend on spell effects or combat context.
  related:
  - GetDebuffList
  - HasAuraType
  - IsDispellable
metadata:
  confidence: 0.9
  generated_at: '2025-11-08T19:04:19.106966'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
