api:
  class: Unit
  method: GetVehicleRoot
  signature: Unit * Unit::GetVehicleRoot() const
documentation:
  brief: Returns the root unit of the vehicle the unit is currently occupying.
  description: This method retrieves the root unit that serves as the base for a vehicle
    system. It is commonly used to determine which unit (typically a player or creature)
    is the vehicle's owner or root, especially when dealing with vehicle mechanics
    in World of Warcraft. The returned unit represents the primary entity that controls
    the vehicle's behavior and movement. This method is essential for understanding
    vehicle relationships and can be used to access vehicle-related data such as passenger
    information, vehicle status, or control mechanisms.
  parameters: []
  returns: Returns a pointer to the Unit that serves as the root of the vehicle. If
    the unit is not currently occupying a vehicle, this will return nullptr. The returned
    pointer may be null if no valid vehicle root exists.
  examples:
  - title: Basic Usage Example
    code: "Unit *root = unit->GetVehicleRoot();\nif (root != nullptr)\n{\n    // Vehicle\
      \ is occupied\n    // Do something with the root unit\n}\nelse\n{\n    // No\
      \ vehicle is occupied\n}"
    language: cpp
  notes: This method should be used carefully as it may return nullptr if the unit
    is not currently in a vehicle. It is recommended to verify that the unit actually
    occupies a vehicle before calling this method, especially when dealing with vehicle-related
    functionality.
  warnings: The returned pointer may be invalid or null if the unit is not currently
    occupying a vehicle, so developers should always check for nullptr before using
    the result. Also, this method does not perform any validation checks on the vehicle
    itself; it simply returns the root unit of the vehicle.
  related:
  - GetVehicleBase
  - GetVehicle
metadata:
  confidence: 0.85
  generated_at: '2025-10-30T12:28:50.263481'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
