api:
  class: SpellMgr
  method: IsSpellRequiringSpell
  signature: bool SpellMgr::IsSpellRequiringSpell(uint32 spellid, uint32 req_spellid)
    const
documentation:
  brief: Checks if a spell requires another spell to be active.
  description: Determines whether a given spell ID requires another specific spell
    ID to be active in order for it to function. This method is used internally by
    the spell system to enforce dependencies between spells, such as requiring a prerequisite
    spell to be learned or active before another spell can be cast. The check typically
    involves examining spell templates and their associated requirements stored in
    the database or spell definitions.
  parameters:
  - name: spellid
    description: The ID of the spell being checked for requiring another spell.
  - name: req_spellid
    description: The ID of the required spell that must be active for the first spell
      to function.
  returns: Returns true if the specified spell requires the required spell to be active;
    otherwise returns false.
  examples:
  - title: 'Check if Shadow Word: Pain requires Mind Flay'
    code: "if (sSpellMgr->IsSpellRequiringSpell(589, 15407)) {\n    // Shadow Word:\
      \ Pain requires Mind Flay to be active\n}"
    language: cpp
  - title: Validate spell dependency in custom logic
    code: "uint32 playerSpell = 12345;\nuint32 requiredSpell = 67890;\nif (sSpellMgr->IsSpellRequiringSpell(playerSpell,\
      \ requiredSpell)) {\n    // Implement logic to ensure requiredSpell is active\n\
      \    // before allowing playerSpell to be cast\n}"
    language: cpp
  notes: This method likely accesses internal spell data structures and may involve
    database lookups or cached spell information. It's commonly used in spell validation
    systems within the TrinityCore core, particularly when checking prerequisites
    for spell casting.
  warnings: Ensure that both spell IDs are valid and exist in the spell database before
    calling this function, as invalid spell IDs could lead to undefined behavior or
    incorrect results.
  related:
  - GetSpellInfo
  - IsSpellKnown
  - IsSpellAllowed
metadata:
  confidence: 0.9
  generated_at: '2025-11-01T06:08:01.879590'
  generator: lmstudio-qwen3-coder-30b
  version: 1.0.0
  source: core
